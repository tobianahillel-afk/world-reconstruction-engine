schema_version: 1
milestone: V2M0
work_items:
  - id: V2L0.1
    lot: V2L0
    title: Inventory and migration map
    status: done
    objective: Define the executable v2 roadmap and classify every implemented/planned v1 capability before machine-state migration.
    allowed_scope:
      - docs/25_V2_ROADMAP.md
      - registry/v1-v2-migration.yaml
      - docs/07_ROADMAP.md
    out_of_scope: [source-code changes, dependency changes, machine-state replacement]
    depends_on: []
    read_before: [docs/00_START_HERE.md, docs/23_ENGINEERING_EXECUTION.md, docs/24_SYSTEM_INVARIANTS.md]
    reuse: [existing Git/PR/review history]
    acceptance:
      - Every implemented v1 component has a forward migration decision.
      - The v2 roadmap covers all canonical blueprint capability families.
      - Work-item identity is capability-oriented rather than tied to one research model.
      - No implementation state or source behavior changes.
    tests: [fast CI, documentation/inventory review]
  - id: V2L0.2
    lot: V2L0
    title: Machine roadmap and state migration
    status: done
    objective: Replace legacy machine roadmap/state/component/review metadata with v2.0 identifiers and validation rules while preserving historical evidence and source behavior.
    allowed_scope:
      - registry/work-items.yaml and registry/work-items/v2m*.yaml
      - PROJECT_STATE.yaml
      - registry/components.yaml
      - registry/reviews.yaml
      - repository validator and its focused tests
      - canonical startup documentation needed to point agents at the v2 roadmap
    out_of_scope:
      - product feature implementation
      - changes to ingestion/reconstruction/retrieval behavior
      - dependency/model installation
      - deletion or rewrite of retained v1 source modules
    depends_on: [V2L0.1]
    read_before:
      - docs/25_V2_ROADMAP.md
      - registry/v1-v2-migration.yaml
      - docs/23_ENGINEERING_EXECUTION.md
      - docs/24_SYSTEM_INVARIANTS.md
      - src/wre/repo_validation.py
      - tests/test_repo_validation.py
    reuse: [existing repository validator and review-gate architecture]
    acceptance:
      - V2 milestones, lots and every planned work-item ID are machine-readable.
      - Exactly one active v2 item exists and legacy L4.3 is no longer active.
      - Planned work may stay concise, but any ready/in_progress/in_review/done item must have a full executable contract.
      - Validator enforces deny-by-default activation metadata and maximum six work items per lot.
      - Component and review registries cover every v2 lot/milestone.
      - Historical v1 evidence remains referenced and recoverable.
      - Fast CI and validator tests pass without changing product code behavior.
    tests: [repository validator tests, full fast CI]
  - id: V2L0.3
    lot: V2L0
    title: Retained baseline compatibility review
    status: done
    objective: Prove that the v2 governance migration preserved the retained v1 implementation, tests and external integration behavior, close the V2L0 migration lot, and prepare the first implementation item without adding product functionality.
    allowed_scope:
      - compatibility and migration review evidence under docs/reviews
      - registry/reviews.yaml V2L0 review state
      - registry/components.yaml migration-governance completion state
      - PROJECT_STATE.yaml and v2m0 work-item handoff metadata
      - focused validation/test metadata needed to prove retained baseline compatibility
    out_of_scope:
      - SceneProject or artifact-domain implementation
      - refactoring retained ingestion persistence reconstruction or retrieval modules
      - new external dependencies models or reconstruction algorithms
      - changing accepted geometry retrieval or metadata semantics
      - starting V2L1.1 implementation in the compatibility-review PR
    depends_on: [V2L0.2]
    read_before:
      - registry/v1-v2-migration.yaml
      - docs/25_V2_ROADMAP.md
      - docs/23_ENGINEERING_EXECUTION.md
      - docs/24_SYSTEM_INVARIANTS.md
      - docs/reviews/L0-M0-bootstrap-review.md
      - docs/reviews/L1-core-domain-review.md
      - docs/reviews/L2-media-ingestion-review.md
      - docs/reviews/L3-M1-colmap-baseline-review.md
      - .github/workflows/ci.yml
      - .github/workflows/colmap-integration.yml
    reuse:
      - existing fast CI and full pytest suite
      - retained native PyCOLMAP integration lane
      - existing deterministic COLMAP end-to-end fixture
      - Git history and V1-to-V2 migration map
    acceptance:
      - Fast CI passes under the v2 roadmap and validator.
      - The exact retained PyCOLMAP integration lane passes without weakening solver or fixture semantics.
      - No product source module was deleted or behaviorally rewritten by V2L0 migration.
      - V1 review and regression evidence remains traceable from the migration map and Git history.
      - V2L0 lot review records a PASS only if governance and retained baseline compatibility are both evidenced.
      - V2L1.1 is expanded to a complete executable contract before becoming ready.
      - Final handoff makes V2L1.1 the sole active item and does not implement it.
    tests:
      - repository validator and fast CI
      - retained PyCOLMAP integration workflow
      - source-tree migration diff audit
      - V2L0 lot review checklist
  - id: V2L1.1
    lot: V2L1
    title: SceneProject contract
    status: done
    objective: Define the minimal immutable SceneProjectId and SceneProject root contract that establishes a distinct project identity boundary for all later V2 project and artifact metadata without pulling later manifest responsibilities forward.
    allowed_scope:
      - src/wre/domain/projects.py
      - focused public-domain exports only if required for the new contract
      - tests/test_scene_projects.py
    out_of_scope:
      - ArtifactId artifact kinds or exact input-artifact references owned by V2L1.2
      - producer model checkpoint or configuration identity owned by V2L1.3
      - provenance classes owned by V2L1.4
      - canonical artifact keys or content addressing owned by V2L1.5
      - codec database or metadata persistence owned by V2L1.6
      - source-media collections coordinate frames quality modes user overrides scene clusters temporal groups artifact roots master-scene versions runtime exports or warnings
      - generic placeholder dictionaries strings or lists standing in for later typed manifest responsibilities
      - ingestion reconstruction retrieval routing solver runtime or external-dependency behavior changes
      - refactoring existing ObservationId or SourceId behavior merely to share implementation code
    depends_on: [V2L0.3]
    read_before:
      - docs/02_ARCHITECTURE.md
      - docs/15_PRODUCTION_RUNTIME.md
      - docs/23_ENGINEERING_EXECUTION.md
      - docs/24_SYSTEM_INVARIANTS.md
      - docs/25_V2_ROADMAP.md
      - src/wre/domain/observations.py
      - tests/test_observations.py
    reuse:
      - existing frozen dataclass and slots value-object conventions in the domain layer
      - existing opaque identifier lexical semantics used by ObservationId and SourceId
      - existing deterministic ValueError-based validation and unit-test style
    acceptance:
      - SceneProjectId is a distinct identity type and is not an alias of observation source camera scene or temporal identity.
      - SceneProjectId uses the established WRE opaque-ID lexical contract and rejects empty whitespace invalid-character and overlength values.
      - Project identity is caller supplied; the contract does not generate random hidden identity.
      - SceneProject is immutable and carries exactly the project identity in this work item, with no mutable catch-all configuration or placeholder future-manifest fields.
      - Equality and hashing are deterministic from the typed immutable project identity.
      - No I/O persistence artifact DAG routing solver media processing or runtime behavior is introduced.
      - Existing domain behavior and tests remain unchanged.
    tests:
      - focused positive construction equality hashing and string/identity tests for SceneProjectId and SceneProject
      - negative tests for invalid IDs and immutability
      - regression tests confirming no existing observation/source identity behavior changes
      - full fast CI
  - id: V2L1.2
    lot: V2L1
    title: Artifact identity and exact input references
    status: done
    objective: Define the minimal immutable ArtifactId, ArtifactKind and ArtifactRef vocabulary required for later artifacts to identify themselves and refer to one exact input artifact without filename path timestamp or fuzzy lookup semantics.
    allowed_scope:
      - src/wre/domain/artifacts.py
      - focused public-domain exports only if required for the new contract
      - tests/test_artifacts.py
    out_of_scope:
      - producer model checkpoint configuration or hardware identity owned by V2L1.3
      - provenance classes owned by V2L1.4
      - canonical artifact keys content hashes or content-addressing rules owned by V2L1.5
      - codec database or project/artifact metadata persistence owned by V2L1.6
      - dependency collections DAG traversal cycle detection invalidation caching or materialization owned by V2L2
      - adapter registry routing quality failure benchmark solver or runtime behavior
      - file URI path timestamp or mutable-name fallback resolution
      - hard-coding a closed list of all future artifact kinds before their owning capabilities exist
      - refactoring retained V1 IDs or provenance contracts merely to share implementation code
    depends_on: [V2L1.1]
    read_before:
      - docs/02_ARCHITECTURE.md
      - docs/15_PRODUCTION_RUNTIME.md
      - docs/23_ENGINEERING_EXECUTION.md
      - docs/24_SYSTEM_INVARIANTS.md
      - docs/25_V2_ROADMAP.md
      - src/wre/domain/projects.py
      - src/wre/domain/observations.py
      - src/wre/domain/runs.py
    reuse:
      - existing frozen dataclass and slots value-object conventions in the domain layer
      - established WRE opaque identifier lexical semantics for ArtifactId
      - explicit typed-reference patterns rather than untyped strings or dictionaries
      - existing deterministic ValueError-based validation and focused pytest style
    acceptance:
      - ArtifactId is an immutable identity type distinct from project observation source camera run scene and temporal identities.
      - ArtifactId follows the established WRE opaque-ID lexical contract and rejects empty whitespace invalid-character and overlength values.
      - ArtifactKind is a distinct immutable symbolic value with a conservative validated token syntax and is not inferred from a filename extension Python class or solver-private type.
      - ArtifactKind remains open to later capability-owned kinds instead of encoding a speculative closed enum in core.
      - ArtifactRef is immutable and identifies exactly one artifact through typed ArtifactId plus declared ArtifactKind.
      - ArtifactRef equality and hashing are deterministic and do not consult filesystem paths timestamps registries or mutable external state.
      - No dependency collection DAG traversal cache key producer provenance persistence or artifact materialization behavior is introduced.
      - Existing SceneProject and retained V1 domain behavior remain unchanged.
    tests:
      - focused positive construction equality hashing and string/identity tests for ArtifactId ArtifactKind and ArtifactRef
      - negative tests for invalid artifact IDs and kind tokens
      - immutability tests for all new value objects
      - regression tests proving artifact identity remains distinct from SceneProjectId and retained domain IDs
      - full fast CI
  - id: V2L1.3
    lot: V2L1
    title: Producer model checkpoint configuration identity
    status: done
    objective: Define the minimal immutable execution-identity composition for artifacts by reusing retained ProducerRef as producer implementation identity and adding only typed model, checkpoint and normalized-configuration identities needed for exact reproducibility metadata.
    allowed_scope:
      - src/wre/domain/producer_identity.py
      - focused public-domain exports only if required for the new contract
      - tests/test_producer_identity.py
    out_of_scope:
      - modifying or replacing retained ProducerRef ReconstructionRun or DerivedArtifactProvenance behavior
      - provenance classes OBSERVED_RECONSTRUCTED INFERRED GENERATED owned by V2L1.4
      - canonical artifact-key composition content addressing or configuration normalization algorithms owned by V2L1.5
      - codec database or metadata persistence owned by V2L1.6
      - adapter capability registries model/download registries shipping status license metadata hardware requirements or benchmark state owned by V2L3
      - artifact DAG cache invalidation checkpoint-resume execution or scheduling behavior
      - downloading resolving loading or executing any model/checkpoint
      - inferring identities from filenames paths package environments or mutable latest aliases
      - refactoring retained V1 producer/run code solely to share helpers
    depends_on: [V2L1.2]
    read_before:
      - docs/02_ARCHITECTURE.md
      - docs/15_PRODUCTION_RUNTIME.md
      - docs/23_ENGINEERING_EXECUTION.md
      - docs/24_SYSTEM_INVARIANTS.md
      - docs/25_V2_ROADMAP.md
      - src/wre/domain/artifacts.py
      - src/wre/domain/runs.py
      - src/wre/domain/observations.py
      - tests/test_runs.py
    reuse:
      - retained ProducerRef as the producer implementation version revision identity rather than creating a competing V2 producer type
      - existing Sha256Digest for exact checkpoint/configuration digest values
      - existing frozen dataclass and slots value-object conventions
      - deterministic fail-closed validation and focused pytest style
    acceptance:
      - Retained ProducerRef remains unchanged and is directly reused inside the new execution identity contract.
      - ModelIdentity is immutable and requires explicit non-blank model name and version with an optional non-blank revision; no floating latest or path inference.
      - CheckpointIdentity is immutable and requires an explicit checkpoint identifier plus exact Sha256Digest content identity.
      - ConfigurationIdentity is immutable and wraps a caller-supplied Sha256Digest representing already-normalized configuration; this item does not define normalization or hashing algorithms.
      - ArtifactProducerIdentity is immutable and composes ProducerRef plus ConfigurationIdentity with optional ModelIdentity and CheckpointIdentity.
      - A checkpoint identity without a model identity is rejected fail-closed; native/non-learned producers may explicitly have neither model nor checkpoint.
      - Equality and hashing are deterministic from typed immutable identity only and perform no filesystem registry network or environment lookup.
      - No provenance class artifact key persistence registry licensing hardware scheduling model loading or execution behavior is introduced.
      - Existing ReconstructionRun ProducerRef artifact identity and retained V1 behavior remain unchanged.
    tests:
      - focused positive construction equality and hashing tests for model checkpoint configuration and aggregate producer identity
      - native producer case with no model/checkpoint
      - learned producer case with model plus checkpoint
      - negative tests for blank model/checkpoint fields wrong digest types and checkpoint-without-model
      - immutability tests for all new value objects
      - regression tests proving retained ProducerRef and ReconstructionRun behavior remains unchanged
      - full fast CI
  - id: V2L1.4
    lot: V2L1
    title: Provenance class contract
    status: done
    objective: Define the minimal closed provenance-class vocabulary that marks whether an artifact is observed/reconstructed, inferred or generated without conflating epistemic classification with retained run/observation lineage.
    allowed_scope:
      - src/wre/domain/provenance.py
      - focused public-domain exports only if required for the new contract
      - tests/test_provenance.py
    out_of_scope:
      - modifying replacing or migrating retained DerivedArtifactProvenance ReconstructionRun or Observation lineage behavior
      - artifact lineage graphs source-observation collections producer identity or dependency edges
      - confidence scores quality metrics review state or evidence strength
      - automatic provenance classification from solver model file path artifact kind or route
      - mixed unknown verified synthetic-confidence or other speculative provenance classes
      - canonical artifact-key composition or content addressing owned by V2L1.5
      - codec database or metadata persistence owned by V2L1.6
      - generated-completion policy user visibility or rendering behavior owned by later capabilities
    depends_on: [V2L1.3]
    read_before:
      - docs/01_PRODUCT.md
      - docs/02_ARCHITECTURE.md
      - docs/15_PRODUCTION_RUNTIME.md
      - docs/23_ENGINEERING_EXECUTION.md
      - docs/24_SYSTEM_INVARIANTS.md
      - docs/25_V2_ROADMAP.md
      - src/wre/domain/runs.py
      - src/wre/domain/producer_identity.py
      - tests/test_runs.py
    reuse:
      - retained DerivedArtifactProvenance for run and source-observation lineage rather than replacing it
      - existing explicit symbolic-domain conventions in wre.domain
      - focused deterministic pytest style
    acceptance:
      - ProvenanceClass is a closed symbolic type with exactly OBSERVED_RECONSTRUCTED INFERRED and GENERATED values.
      - The three values have stable explicit lowercase wire tokens observed_reconstructed inferred and generated.
      - No UNKNOWN MIXED VERIFIED or solver-specific class is admitted by the core contract.
      - ProvenanceClass carries classification only and does not contain confidence lineage producer artifact dependency or review metadata.
      - Callers must supply the class explicitly; no filename artifact-kind solver model route or environment inference is performed.
      - Retained DerivedArtifactProvenance remains unchanged and continues to represent run/source-observation lineage independently of epistemic class.
      - No artifact key persistence cache routing generation policy or runtime behavior is introduced.
    tests:
      - focused tests for exact member set names and stable wire tokens
      - construction equality hashing and iteration behavior
      - negative tests proving arbitrary unknown and speculative classes are rejected
      - regression test proving retained DerivedArtifactProvenance behavior is unchanged
      - full fast CI
  - id: V2L1.5
    lot: V2L1
    title: Canonical artifact key
    status: done
    objective: Define a deterministic versioned artifact-computation key from explicit immutable key material so identical computation inputs can share an identity without introducing cache lookup, dependency traversal or persistence behavior.
    allowed_scope:
      - src/wre/domain/artifact_keys.py
      - focused public-domain exports only if required for the new contract
      - tests/test_artifact_keys.py
    out_of_scope:
      - reading files or computing input content hashes from paths bytes URIs or artifact materializations
      - ArtifactId SceneProjectId timestamps paths mtimes random values or mutable names as key material
      - ProvenanceClass confidence review state or lineage metadata as computation-key material
      - configuration normalization or hashing algorithms; ConfigurationIdentity is already caller supplied
      - hardware-sensitive identity fields before the capability owned by V2L3.2
      - project/artifact metadata persistence owned by V2L1.6
      - dependency DAG traversal cycle detection cache lookup reuse invalidation or materialization owned by V2L2
      - adapter/model registry shipping license benchmark routing execution or scheduling behavior owned by later lots
    depends_on: [V2L1.4]
    read_before:
      - docs/02_ARCHITECTURE.md
      - docs/15_PRODUCTION_RUNTIME.md
      - docs/23_ENGINEERING_EXECUTION.md
      - docs/24_SYSTEM_INVARIANTS.md
      - docs/25_V2_ROADMAP.md
      - src/wre/domain/artifacts.py
      - src/wre/domain/producer_identity.py
      - src/wre/domain/provenance.py
    reuse:
      - ArtifactKind as the typed semantic kind of input/output artifacts
      - Sha256Digest as exact caller-supplied input-content/configuration/checkpoint digest vocabulary
      - ArtifactProducerIdentity as the complete currently-owned producer/config/model/checkpoint computation identity
      - immutable tuple and frozen dataclass conventions already used in the domain layer
    acceptance:
      - ArtifactInputFingerprint is immutable and contains exactly typed ArtifactKind plus caller-supplied Sha256Digest content identity, with no ArtifactId path timestamp or lookup semantics.
      - ArtifactKeyMaterial is immutable and contains output ArtifactKind, an immutable ordered tuple of ArtifactInputFingerprint values and ArtifactProducerIdentity.
      - Input order and duplicates are preserved because they can be semantically meaningful; the key layer performs no hidden sorting or deduplication.
      - ArtifactKey is a distinct immutable typed computation identity backed by Sha256Digest and is not interchangeable with ArtifactId or a raw content digest.
      - Key derivation uses one documented canonical UTF-8 serialization with an explicit WRE artifact-key schema/domain version marker before SHA-256 hashing.
      - Identical typed key material yields the same key across processes and machines; changing output kind input kind input digest input order producer implementation/version/revision configuration model or checkpoint changes the key.
      - ArtifactId SceneProjectId ProvenanceClass timestamps filesystem paths environment state and random values do not participate in the version-1 key preimage.
      - Hardware-sensitive fields are not guessed or represented through placeholders before V2L3.2; no claim of cross-hardware equivalence is added for adapters that later declare hardware-sensitive output.
      - Derivation performs no filesystem registry database cache network environment or runtime lookup and has no side effects.
      - No persistence dependency-DAG cache reuse invalidation or materialization behavior is introduced.
    tests:
      - focused positive construction immutability equality and hashing tests for input fingerprints key material and ArtifactKey
      - deterministic golden-vector test for canonical version-1 serialization and derived SHA-256 key
      - tests proving changes to every owned computation-identity field alter the key
      - tests proving input ordering and duplicate preservation affect canonical key material exactly as supplied
      - negative tests for list/untyped tuple members wrong digest/kind/producer types and raw-string substitutions
      - regression tests proving ArtifactId ArtifactRef ProducerRef ProvenanceClass and retained V1 behavior remain unchanged
      - full fast CI
  - id: V2L1.6
    lot: V2L1
    title: Project and artifact metadata persistence
    status: done
    objective: Extend the retained local codec and SQLite persistence boundary so SceneProject and the minimal immutable V2 artifact metadata can round-trip deterministically without introducing dependency-graph, cache or materialization responsibilities.
    allowed_scope:
      - src/wre/domain/artifact_metadata.py
      - focused public-domain exports only if required for the new contract
      - src/wre/persistence/codec.py
      - src/wre/persistence/sqlite_store.py
      - src/wre/persistence/__init__.py only if public persistence exports require it
      - tests/test_artifact_metadata.py
      - focused additions to tests/test_persistence.py
      - V2L1 lot-review evidence and final state/component/review/work-item handoff metadata after implementation passes
    out_of_scope:
      - dependency-edge collections dependency lookup cycle detection DAG traversal or graph validation owned by V2L2.1 and V2L2.2
      - cache lookup reuse invalidation planning or key-to-artifact resolution owned by V2L2.3 and V2L2.4
      - artifact materialization paths URIs byte lengths output-content verification or corruption state owned by V2L2.5
      - source-media manifest collections coordinate frames quality modes user overrides scene clusters temporal groups master-scene versions runtime exports or warnings
      - mutating replacing deleting or listing persisted records beyond the retained immutable put/get contract
      - implicit project creation project-existence lookup or cross-record repair while storing artifact metadata
      - schema migration framework external database backend ORM or new dependency
      - changing retained V1 codec/store semantics record types duplicate detection or persistence conflict behavior
      - starting V2L2.1 product implementation in this work item
    depends_on: [V2L1.5]
    read_before:
      - docs/02_ARCHITECTURE.md
      - docs/15_PRODUCTION_RUNTIME.md
      - docs/23_ENGINEERING_EXECUTION.md
      - docs/24_SYSTEM_INVARIANTS.md
      - docs/25_V2_ROADMAP.md
      - src/wre/domain/projects.py
      - src/wre/domain/artifacts.py
      - src/wre/domain/artifact_keys.py
      - src/wre/domain/producer_identity.py
      - src/wre/domain/provenance.py
      - src/wre/persistence/codec.py
      - src/wre/persistence/sqlite_store.py
      - tests/test_persistence.py
    reuse:
      - retained SQLiteLocalStore generic immutable record table transaction and conflict semantics
      - retained canonical_json parse helpers and strict decoder style
      - SceneProject SceneProjectId ArtifactRef ArtifactKey ArtifactProducerIdentity and ProvenanceClass contracts from V2L1.1 through V2L1.5
      - existing database and record schema-version rejection behavior without a speculative migration framework
    acceptance:
      - ArtifactMetadata is a frozen immutable aggregate containing exactly SceneProjectId, ArtifactRef, ArtifactKey, ArtifactProducerIdentity and ProvenanceClass with runtime rejection of untyped members.
      - ArtifactMetadata contains no dependency collection materialization location output-content digest timestamps confidence quality review state or mutable catch-all metadata.
      - SceneProject has explicit deterministic encode/decode functions that preserve its typed identity exactly.
      - ArtifactMetadata has explicit deterministic encode/decode functions that preserve nested artifact kind/id, artifact key digest, producer/config/model/checkpoint identity and provenance class exactly.
      - Codec decoding fails closed on missing malformed wrong-type or unknown provenance values and never regenerates an ArtifactKey from partial metadata.
      - SQLiteLocalStore persists SceneProject under a dedicated stable record type keyed by SceneProjectId and ArtifactMetadata under a dedicated stable record type keyed by ArtifactId.
      - Identical SceneProject or ArtifactMetadata writes are idempotent; reusing the same stable record identity with different payload content raises the retained PersistenceConflictError.
      - Artifact persistence does not implicitly create require fetch repair or mutate a project record and does not add dependency indexes or cross-record graph semantics.
      - Existing database schema can store the new record types through the generic wre_records table without changing retained V1 record behavior or weakening schema-version checks.
      - Reopening the SQLite store round-trips both new record types exactly while existing observation camera metadata fragment and reconstruction-run round trips remain unchanged.
      - V2L1 lot review records PASS only after V2L1.1 through V2L1.6 acceptance and required CI/integration evidence are green.
      - V2L2.1 is expanded to a complete executable contract before becoming the sole next ready item; no V2L2 product implementation is bundled.
    tests:
      - focused ArtifactMetadata construction immutability and negative typed-member tests
      - SceneProject and ArtifactMetadata codec round-trip and malformed-payload tests
      - SQLite put/get/reopen idempotency missing-record and conflicting-identity tests for both new record types
      - coexistence regression with retained V1 record types in the same database
      - existing persistence tests and full fast CI
      - retained native PyCOLMAP integration lane for final V2L1 handoff
      - V2L1 lot-review checklist
  - id: V2L2.1
    lot: V2L2
    title: Artifact dependency DAG contract
    status: done
    objective: Define the minimal immutable in-memory directed artifact-dependency graph over exact ArtifactRef values, with explicit direct-edge semantics and fail-closed structural validation including cycle rejection, without persistence or cache behavior.
    allowed_scope:
      - src/wre/domain/artifact_graph.py
      - focused public-domain exports only if required for the new contract
      - tests/test_artifact_graph.py
    out_of_scope:
      - codec database persistence dependency lookup reverse indexes or stored graph queries owned by V2L2.2
      - cache lookup key-to-artifact resolution reuse policy or hit/miss semantics owned by V2L2.3
      - dependency-scoped invalidation planning deletion or garbage collection owned by V2L2.4
      - artifact materialization paths URIs content verification or corruption state owned by V2L2.5
      - inferring dependency edges from ArtifactKeyMaterial ArtifactMetadata filenames paths timestamps producers or filesystem state
      - project-existence checks cross-project policy or metadata repair through SQLiteLocalStore
      - graph mutation APIs incremental update semantics scheduler execution topological scheduling or job orchestration
      - adapter registries routing quality evaluation reconstruction behavior or external dependencies
      - starting V2L2.2 product implementation in this work item
    depends_on: [V2L1.6]
    read_before:
      - docs/02_ARCHITECTURE.md
      - docs/15_PRODUCTION_RUNTIME.md
      - docs/23_ENGINEERING_EXECUTION.md
      - docs/24_SYSTEM_INVARIANTS.md
      - docs/25_V2_ROADMAP.md
      - docs/reviews/V2L1-scene-project-artifact-identity-review.md
      - src/wre/domain/artifacts.py
      - src/wre/domain/artifact_metadata.py
      - src/wre/domain/artifact_keys.py
    reuse:
      - ArtifactRef as the exact typed graph-node identity rather than inventing a second dependency-reference type
      - frozen dataclass and immutable collection conventions already used in wre.domain
      - fail-closed positive-domain validation and focused deterministic pytest style
    acceptance:
      - ArtifactDependencyEdge is immutable and contains exactly an artifact ArtifactRef plus a dependency ArtifactRef, with the documented meaning that artifact directly depends on dependency.
      - ArtifactDependencyGraph is immutable and stores only exact declared ArtifactRef nodes plus direct ArtifactDependencyEdge values; raw strings dictionaries lists and untyped members are rejected.
      - Every edge endpoint must be present in the declared node set; the graph performs no registry database or filesystem lookup to repair missing nodes.
      - Self-dependency is rejected fail closed.
      - Any directed dependency cycle of length two or greater is rejected fail closed; disconnected acyclic components and isolated nodes remain valid.
      - Within one graph, the same logical ArtifactId cannot appear under conflicting ArtifactKind declarations.
      - Graph collection semantics are explicitly set-like and immutable; equality and hashing do not depend on caller insertion order and no hidden mutable adjacency state is exposed.
      - Cycle validation is iterative and accepts valid dependency chains beyond the Python recursion limit without weakening cycle rejection.
      - The contract represents direct dependency claims only and does not infer transitive edges from ArtifactKeyMaterial ArtifactMetadata producer identity filenames or paths.
      - No persistence dependency lookup reverse index cache invalidation materialization scheduling adapter routing or solver behavior is introduced.
      - Existing V2L1 project/artifact metadata persistence and retained V1 behavior remain unchanged.
    tests:
      - focused construction immutability equality and hashing tests for ArtifactDependencyEdge and ArtifactDependencyGraph
      - valid isolated chain diamond disconnected-DAG and long-chain cases
      - negative self-cycle two-node-cycle and longer-cycle cases
      - negative undeclared-endpoint and conflicting ArtifactId-to-ArtifactKind cases
      - negative wrong collection/member/raw-string substitutions
      - regression tests proving no V2L1 metadata or persistence behavior changes
      - full fast CI
  - id: V2L2.2
    lot: V2L2
    title: Artifact metadata store and dependency lookup
    status: done
    objective: Extend the retained local SQLite persistence boundary with deterministic immutable storage and exact lookup of validated direct artifact-dependency claims, while preserving V2L1 artifact metadata behavior and stopping before any cache lookup or invalidation policy.
    allowed_scope:
      - src/wre/persistence/codec.py
      - src/wre/persistence/sqlite_store.py
      - src/wre/persistence/__init__.py only if focused public persistence exports require it
      - tests/test_artifact_dependencies.py
      - focused additions to tests/test_persistence.py if required for retained-regression coverage
    out_of_scope:
      - changing ArtifactDependencyEdge or ArtifactDependencyGraph semantics validation or public identity contracts from V2L2.1
      - ArtifactKey-to-artifact lookup cache hit/miss reuse policy or candidate selection owned by V2L2.3
      - transitive dependency closure invalidation planning deletion garbage collection or destructive mutation owned by V2L2.4
      - artifact materialization path URI output-content digest byte verification corruption state or repair owned by V2L2.5
      - inferring dependencies from ArtifactKeyMaterial ArtifactMetadata producer identity filenames paths timestamps project state or filesystem content
      - implicitly creating repairing or mutating SceneProject or ArtifactMetadata rows while writing dependency records
      - adding mutable update/delete APIs for existing artifact dependency claims
      - schema migration framework ORM external database backend or new dependency
      - scheduler routing solver adapter-registry or runtime behavior
      - starting V2L2.3 product implementation in this work item
    depends_on: [V2L2.1]
    read_before:
      - docs/02_ARCHITECTURE.md
      - docs/15_PRODUCTION_RUNTIME.md
      - docs/23_ENGINEERING_EXECUTION.md
      - docs/24_SYSTEM_INVARIANTS.md
      - docs/25_V2_ROADMAP.md
      - src/wre/domain/artifact_graph.py
      - src/wre/domain/artifacts.py
      - src/wre/domain/artifact_metadata.py
      - src/wre/persistence/codec.py
      - src/wre/persistence/sqlite_store.py
      - tests/test_artifact_graph.py
      - tests/test_artifact_metadata.py
      - tests/test_persistence.py
    reuse:
      - validated ArtifactDependencyGraph as the only accepted graph-write input instead of accepting unvalidated raw edge collections
      - ArtifactRef as exact persisted node/dependency identity
      - retained SQLiteLocalStore generic wre_records table transaction and immutable conflict semantics
      - retained canonical_json and strict fail-closed decoder helpers
      - existing database and record schema versions without a speculative migration framework
    acceptance:
      - Dependency persistence records every declared graph node including isolated nodes as its exact ArtifactRef plus its immutable set of direct dependency ArtifactRef values; no transitive edges are synthesized.
      - The wire representation of set-like nodes/dependencies is deterministic through explicit canonical ordering while the public lookup contract returns immutable frozenset values.
      - SQLiteLocalStore accepts only a typed already-validated ArtifactDependencyGraph for graph persistence and writes all graph dependency records transactionally so a conflict or malformed existing record cannot leave a partial graph write.
      - Rewriting an identical graph or an overlapping graph whose existing node dependency records are identical is idempotent.
      - Reusing one ArtifactId with a different ArtifactKind or a different direct-dependency set raises PersistenceConflictError and rolls back the entire attempted graph write.
      - Exact direct-dependency lookup by ArtifactRef returns the stored immutable direct dependency set, returns None when no dependency record exists, and fails closed if the stored record identity/kind does not exactly match the requested ArtifactRef.
      - Exact direct-dependent lookup returns the immutable set of artifacts that directly depend on the requested ArtifactRef; an artifact with no direct dependents yields an empty frozenset and no transitive closure is performed.
      - Corrupted malformed unsupported-version or wrong-type dependency records fail closed through the retained persistence error boundary rather than being skipped or repaired.
      - Reopening the SQLite store preserves direct dependency and direct-dependent lookup exactly, including isolated nodes and empty dependency sets.
      - Dependency persistence does not require create fetch repair or mutate SceneProject or ArtifactMetadata records and does not resolve ArtifactKey values.
      - Existing SceneProject ArtifactMetadata observation camera metadata fragment reconstruction-run and V2L2.1 graph behavior remain unchanged.
      - No cache lookup reuse invalidation materialization scheduling routing or solver behavior is introduced.
    tests:
      - deterministic dependency-record codec tests including canonical ordering and empty dependency sets
      - SQLite graph-write reopen and exact direct-dependency lookup tests for isolated chain diamond and disconnected graphs
      - direct-dependent lookup tests proving only immediate dependents are returned
      - idempotent repeated and overlapping graph-write tests
      - transactional rollback tests for dependency-set and ArtifactKind conflicts
      - malformed-record identity-kind and unsupported-version fail-closed tests
      - regression tests for V2L1 metadata persistence V2L2.1 graph behavior and retained V1 persistence
      - full fast CI
  - id: V2L2.3
    lot: V2L2
    title: Canonical cache lookup
    status: done
    objective: Add deterministic exact candidate lookup for persisted ArtifactMetadata by canonical ArtifactKey so prior identical computations can be discovered without filename timestamp or fuzzy heuristics, while explicitly stopping before materialization verification reuse selection or invalidation.
    allowed_scope:
      - src/wre/persistence/sqlite_store.py
      - src/wre/persistence/__init__.py only if focused public persistence exports require it
      - tests/test_artifact_cache_lookup.py
      - focused additions to tests/test_persistence.py if required for retained-regression coverage
    out_of_scope:
      - changing ArtifactKey ArtifactMetadata ArtifactRef producer identity provenance or dependency-DAG semantics
      - recomputing deriving normalizing or partially matching ArtifactKey values inside the store
      - selecting one candidate as the winner or declaring any candidate reusable merely because its key matches
      - materialization path URI byte length output-content digest existence integrity or corruption verification owned by V2L2.5
      - dependency-scoped invalidation traversal planning deletion garbage collection or destructive mutation owned by V2L2.4
      - cache population side effects eviction ranking recency preference project preference or hidden mutable cache state
      - filename path timestamp mtime producer-only model-only configuration-only or other fuzzy fallback lookup
      - schema migration new table external database backend ORM or new dependency
      - scheduler routing solver adapter-registry quality runtime or V2L2.4+ behavior
    depends_on: [V2L2.2]
    read_before:
      - docs/02_ARCHITECTURE.md
      - docs/15_PRODUCTION_RUNTIME.md
      - docs/23_ENGINEERING_EXECUTION.md
      - docs/24_SYSTEM_INVARIANTS.md
      - docs/25_V2_ROADMAP.md
      - src/wre/domain/artifact_keys.py
      - src/wre/domain/artifact_metadata.py
      - src/wre/domain/artifacts.py
      - src/wre/persistence/sqlite_store.py
      - tests/test_artifact_keys.py
      - tests/test_artifact_metadata.py
      - tests/test_artifact_dependencies.py
      - tests/test_persistence.py
    reuse:
      - ArtifactKey as the only canonical computation-identity lookup input
      - persisted ArtifactMetadata as the candidate record returned to callers without inventing a second cache-entry identity type
      - retained SQLiteLocalStore generic wre_records table and strict ArtifactMetadata decoder
      - deterministic ordering and fail-closed corrupted-record handling conventions from V2L1.6 and V2L2.2
    acceptance:
      - SQLiteLocalStore exposes one typed lookup accepting exactly ArtifactKey and returning all persisted ArtifactMetadata records whose stored ArtifactKey equals it exactly.
      - No partial digest prefix filename timestamp project producer model checkpoint configuration provenance or ArtifactKind fallback matching is performed.
      - Zero exact matches returns an empty immutable tuple rather than None or a synthesized miss record.
      - Multiple exact matches are preserved rather than arbitrarily collapsed; candidates from different SceneProject values remain discoverable because ArtifactKey identity is computation-oriented rather than project-oriented.
      - Returned candidates use a documented deterministic order by project identity then ArtifactId then ArtifactKind so result order does not depend on SQLite row order or insertion history.
      - Lookup is read-only and does not create mutate repair rank evict or otherwise alter ArtifactMetadata dependency records projects or any other persisted state.
      - A matching ArtifactKey is only a cache candidate discovery signal; this item does not claim that bytes exist are healthy are compatible for resume or are safe to reuse before later materialization and policy checks.
      - Every artifact_metadata row inspected by the lookup is schema-version checked and decoded through the retained strict decoder; malformed payloads unsupported versions or record-id versus payload ArtifactId mismatches fail closed rather than being silently skipped.
      - Raw strings Sha256Digest values or other untyped substitutes are rejected at the public lookup boundary instead of being coerced into ArtifactKey.
      - Reopening SQLiteLocalStore preserves exact candidate lookup results and deterministic ordering.
      - Existing ArtifactMetadata put/get direct dependency lookups V2L2.1 graph behavior and retained V1 persistence remain unchanged.
      - No invalidation materialization scheduling routing solver adapter-registry quality or runtime behavior is introduced.
    tests:
      - exact-key no-match single-match and near-key non-match lookup tests
      - multiple exact-match tests including cross-project candidates and deterministic ordering independent of insertion order
      - reopen regression preserving lookup results
      - negative untyped lookup-input tests
      - malformed artifact_metadata unsupported-version and record-id/payload-identity mismatch fail-closed tests
      - regression tests for existing ArtifactMetadata and dependency persistence APIs
      - full fast CI
  - id: V2L2.4
    lot: V2L2
    title: Dependency scoped invalidation planning
    status: done
    objective: Define a pure immutable invalidation plan that computes exactly the changed ArtifactRef roots plus every artifact that directly or transitively depends on them in a validated ArtifactDependencyGraph, without deleting mutating persisting or scheduling anything.
    allowed_scope:
      - src/wre/domain/artifact_invalidation.py
      - focused public-domain exports in src/wre/domain/__init__.py
      - tests/test_artifact_invalidation.py
    out_of_scope:
      - SQLiteLocalStore persistence queries writes updates deletes or schema changes
      - deleting evicting garbage-collecting or mutating ArtifactMetadata dependency records cache candidates files or materializations
      - materialization path URI byte length content-digest health or corruption verification owned by V2L2.5
      - cache winner selection reuse execution retry scheduling topological job ordering or recomputation orchestration
      - deriving dependency edges from ArtifactKey ArtifactMetadata producers paths filenames timestamps or filesystem state
      - accepting undeclared roots or repairing incomplete graphs from persistence or external state
      - adapter registry routing quality solver runtime behavior or V2L2.5+ implementation
    depends_on: [V2L2.3]
    read_before:
      - docs/02_ARCHITECTURE.md
      - docs/15_PRODUCTION_RUNTIME.md
      - docs/23_ENGINEERING_EXECUTION.md
      - docs/24_SYSTEM_INVARIANTS.md
      - docs/25_V2_ROADMAP.md
      - src/wre/domain/artifact_graph.py
      - src/wre/domain/artifacts.py
      - tests/test_artifact_graph.py
      - tests/test_artifact_dependencies.py
      - tests/test_artifact_cache_lookup.py
    reuse:
      - ArtifactDependencyGraph as the already-validated authoritative dependency structure
      - ArtifactRef as exact root and affected-artifact identity
      - frozen dataclass and frozenset conventions already used in wre.domain
      - iterative graph traversal style so valid long dependency chains do not depend on Python recursion depth
    acceptance:
      - ArtifactInvalidationPlan is immutable and contains exactly an immutable root ArtifactRef set plus an immutable affected ArtifactRef set; affected includes every root and all downstream dependents selected by the planner.
      - The planner accepts only a typed ArtifactDependencyGraph and frozenset of ArtifactRef roots; mutable raw or untyped substitutes are rejected rather than coerced.
      - Every non-empty root must be an exact declared graph node; undeclared or kind-mismatched ArtifactRef roots fail closed and no registry database or filesystem lookup repairs them.
      - For edge semantics artifact depends on dependency, changing a dependency invalidates that artifact and every transitive dependent reachable through reverse edges.
      - Upstream dependencies of a changed artifact are not invalidated merely because the changed artifact depends on them.
      - Unrelated disconnected nodes and branches are excluded from the affected set.
      - Multiple roots produce the set union of their reverse dependency closures with duplicates removed; one root being downstream of another does not alter the final affected set.
      - An isolated declared root produces a plan containing only that root; an empty root frozenset produces an explicit empty no-op plan.
      - Traversal is iterative and accepts valid chains beyond Python recursion depth while preserving exact reverse-closure semantics.
      - Planning is pure and does not mutate the input graph, write persistence state, delete files, change cache metadata or expose hidden mutable adjacency state.
      - The plan carries no deletion order execution order reason timestamps policy priority materialization state or catch-all metadata; later orchestration may consume it without those concerns entering this contract.
      - Existing dependency graph persistence canonical cache-candidate lookup V2L1 metadata behavior and retained V1 behavior remain unchanged.
    tests:
      - immutable plan construction equality and hashing tests
      - chain diamond branched and disconnected reverse-closure cases
      - tests proving upstream dependencies and unrelated artifacts are excluded
      - multi-root union downstream-root isolated-root and empty-root cases
      - undeclared-root wrong-root-collection wrong-member and wrong-graph negative tests
      - long-chain regression beyond Python recursion depth
      - regression proving input graph remains unchanged and no persistence behavior is introduced
      - full fast CI
  - id: V2L2.5
    lot: V2L2
    title: Artifact materialization verification metadata
    status: done
    objective: Define and persist immutable local artifact materialization metadata for one or more exact output files and verify their presence size and SHA-256 under an explicit local root, producing a fail-closed typed verification outcome without repair deletion or cache-reuse selection.
    allowed_scope:
      - src/wre/domain/artifact_materialization.py
      - focused public-domain exports in src/wre/domain/__init__.py
      - src/wre/materialization.py
      - src/wre/persistence/codec.py
      - src/wre/persistence/sqlite_store.py
      - src/wre/persistence/__init__.py only if focused public persistence exports require it
      - tests/test_artifact_materialization.py
      - focused additions to tests/test_persistence.py if required for retained-regression coverage
    out_of_scope:
      - modifying ArtifactMetadata ArtifactKey ArtifactRef dependency-DAG or invalidation-plan semantics
      - remote HTTP object-store cloud URI download upload archive extraction directory hashing or distributed storage behavior
      - timestamps mtimes inode identity permissions ownership or filenames alone as integrity evidence
      - silently accepting absolute paths parent traversal dot segments backslashes duplicate paths or paths that resolve outside the supplied materialization root
      - cache candidate ranking winner selection automatic reuse recomputation retry eviction deletion garbage collection or repair owned by V2L2.6 or later orchestration
      - mutating files while verifying them or rewriting stored expected digests/sizes to match observed bytes
      - stable cross-adapter failure taxonomy policy owned by V2L4; this item exposes only materialization verification status
      - schema migration framework new database table ORM external backend or new dependency
      - routing scheduling solver adapter-registry quality runtime or V2L2.6+ implementation
    depends_on: [V2L2.4]
    read_before:
      - docs/02_ARCHITECTURE.md
      - docs/15_PRODUCTION_RUNTIME.md
      - docs/23_ENGINEERING_EXECUTION.md
      - docs/24_SYSTEM_INVARIANTS.md
      - docs/25_V2_ROADMAP.md
      - src/wre/domain/artifacts.py
      - src/wre/domain/artifact_metadata.py
      - src/wre/domain/artifact_invalidation.py
      - src/wre/domain/observations.py
      - src/wre/persistence/codec.py
      - src/wre/persistence/sqlite_store.py
      - tests/test_artifact_cache_lookup.py
      - tests/test_artifact_invalidation.py
      - tests/test_persistence.py
    reuse:
      - ArtifactRef as the exact artifact identity associated with one materialization manifest
      - Sha256Digest as the expected output-file content identity
      - retained frozen dataclass tuple and explicit symbolic-status conventions
      - retained canonical_json strict codec and SQLiteLocalStore immutable-record conflict semantics
      - streaming SHA-256 hashing patterns without introducing a new dependency
    acceptance:
      - ArtifactMaterializationEntry is immutable and contains exactly a canonical local relative POSIX path Sha256Digest and non-negative byte length; raw/untyped digest or boolean/negative lengths are rejected.
      - Materialization entry paths are non-empty normalized relative POSIX paths with no absolute root empty segment dot parent-traversal backslash NUL or platform-drive semantics; verification never constructs a path outside the caller-supplied root.
      - ArtifactMaterializationMetadata is immutable and contains exactly one ArtifactRef plus a non-empty canonical tuple of entries sorted lexicographically by relative path with unique paths; unsorted duplicate mutable or untyped entry collections fail closed rather than being silently normalized.
      - Materialization metadata is separate from ArtifactMetadata so computation identity/provenance metadata does not silently become filesystem state.
      - A closed materialization verification status has exactly VERIFIED MISSING and CORRUPTED states; it is not the future V2L4 cross-adapter failure taxonomy.
      - Local verification accepts only typed ArtifactMaterializationMetadata plus an explicit pathlib Path root, performs no database/cache lookup, and returns VERIFIED only when every declared entry resolves within the root, exists as a regular file, has the exact expected byte length and exact streaming SHA-256 digest.
      - If any declared file is absent the result is MISSING unless another declared entry is structurally unsafe non-regular or content/size mismatched, in which case CORRUPTED takes precedence; no observed bytes are used to repair expected metadata.
      - Paths whose resolved target escapes the materialization root or resolves to a non-regular file fail closed as CORRUPTED; verification does not follow an escaping symlink as a valid artifact.
      - Verification is read-only and does not create truncate rewrite chmod delete quarantine move or otherwise mutate files or metadata.
      - Deterministic codec round-trips exact ArtifactRef entry paths byte lengths and digests and rejects malformed duplicate unsorted or untyped wire values.
      - SQLiteLocalStore persists materialization metadata as an immutable dedicated record type keyed by ArtifactId in the retained generic wre_records table; identical writes are idempotent and conflicting kind path digest size or entry-set content raises PersistenceConflictError.
      - Exact materialization lookup by ArtifactRef returns None when absent and fails closed on record-id or ArtifactKind mismatch corrupted payload or unsupported record version; it does not require or create ArtifactMetadata SceneProject or dependency records.
      - Reopening SQLiteLocalStore preserves the exact materialization manifest and verification can be rerun against current bytes without mutating persisted expectations.
      - Existing canonical ArtifactKey candidate lookup dependency persistence invalidation planning V2L1 metadata and retained V1 behavior remain unchanged.
      - No candidate is declared reusable solely from ArtifactKey plus verification inside this item; integrated cache reuse/invalidation behavior remains V2L2.6.
    tests:
      - entry and manifest construction immutability hashing canonical-order and typed-member negative tests
      - path validation tests for empty absolute dot parent backslash NUL drive-like duplicate and unsorted paths
      - verifier tests for single-file and multi-file VERIFIED outcomes
      - missing-file MISSING outcome and mixed missing-plus-corrupt CORRUPTED precedence
      - byte-length mismatch digest mismatch non-regular target and root-escape/symlink fail-closed CORRUPTED cases
      - regression proving verification does not mutate bytes or metadata
      - deterministic codec round-trip plus malformed duplicate/unsorted wire payload tests
      - SQLite put/get/reopen idempotency conflict missing-record kind-mismatch corruption and unsupported-version tests
      - regression tests for cache candidate lookup dependency persistence invalidation planning and retained persistence
      - full fast CI
  - id: V2L2.6
    lot: V2L2
    title: Cache reuse and invalidation regression
    status: done
    objective: Close V2L2 with a deterministic integration regression fixture proving that exact ArtifactKey candidate discovery is insufficient without verified materialization, that changed computation identity does not reuse stale candidates, and that dependency invalidation scopes only downstream artifacts, then record the V2L2 lot review without introducing a cache manager or execution scheduler.
    allowed_scope:
      - tests/test_artifact_cache_regression.py
      - docs/reviews/V2L2-artifact-dag-cache-review.md
      - registry/reviews.yaml V2L2 review state and evidence
      - registry/components.yaml V2L2 completion and regression-test coverage
      - PROJECT_STATE.yaml final V2L2-to-V2L3 handoff metadata after review passes
      - registry/work-items/v2m0.yaml V2L2 completion and V2L3.1 activation metadata after review passes
    out_of_scope:
      - adding a production cache-manager cache-hit resolver reuse-policy service or automatic winner-selection API
      - ranking candidates by project recency path producer timestamp or any heuristic outside exact ArtifactKey identity
      - writing deleting evicting garbage-collecting repairing quarantining or mutating candidate metadata materialization metadata dependency records or files as part of lookup verification or invalidation planning
      - changing ArtifactKey ArtifactMetadata ArtifactDependencyGraph ArtifactInvalidationPlan or materialization verification semantics established by V2L2.1 through V2L2.5
      - recomputation execution retry checkpoint job ordering resource scheduling pipeline orchestration or route selection
      - remote object-store distributed cache cloud synchronization or network transfer behavior
      - adapter/model/shipping registry implementation owned by V2L3
      - stable cross-adapter failure taxonomy quality policy or benchmark behavior owned by V2L4
      - starting V2L3.1 product implementation in the V2L2 review PR
    depends_on: [V2L2.5]
    read_before:
      - docs/02_ARCHITECTURE.md
      - docs/15_PRODUCTION_RUNTIME.md
      - docs/23_ENGINEERING_EXECUTION.md
      - docs/24_SYSTEM_INVARIANTS.md
      - docs/25_V2_ROADMAP.md
      - src/wre/domain/artifact_keys.py
      - src/wre/domain/artifact_graph.py
      - src/wre/domain/artifact_invalidation.py
      - src/wre/domain/artifact_materialization.py
      - src/wre/materialization.py
      - src/wre/persistence/sqlite_store.py
      - tests/test_artifact_cache_lookup.py
      - tests/test_artifact_invalidation.py
      - tests/test_artifact_materialization.py
      - docs/reviews/V2L1-scene-project-artifact-identity-review.md
    reuse:
      - exact SQLiteLocalStore.find_artifact_metadata_by_key candidate discovery from V2L2.3
      - immutable persisted artifact materialization metadata and read-only local verifier from V2L2.5
      - pure dependency-scoped plan_artifact_invalidation behavior from V2L2.4
      - ArtifactKey derivation and exact producer/config/input identity from V2L1.5
      - retained SQLite immutable record semantics and native PyCOLMAP integration lane for final lot handoff confidence
    acceptance:
      - A regression fixture composes only public V2L2.1 through V2L2.5 contracts and introduces no production cache-policy implementation.
      - Multiple exact ArtifactKey candidates remain discoverable; the fixture demonstrates explicitly that key equality alone does not make any candidate reusable.
      - A candidate satisfies the fixture's verified-materialization precondition only when exact materialization metadata exists for its ArtifactRef and current local bytes verify VERIFIED under the supplied root.
      - Candidates with absent materialization metadata MISSING bytes or CORRUPTED bytes fail the verified-materialization precondition without filename timestamp project or other fallback matching.
      - Changing canonical computation identity such as ConfigurationIdentity yields a different ArtifactKey and does not discover stale candidates stored under the previous key.
      - Corrupting bytes after a previously VERIFIED check changes the next verification result to CORRUPTED while persisted ArtifactMetadata and materialization expectations remain unchanged.
      - For a validated artifact dependency DAG, invalidation of a changed root includes that root and exactly its downstream direct/transitive dependents while excluding upstream dependencies and disconnected artifacts.
      - Planning invalidation is proven non-destructive and does not delete or mutate cached ArtifactMetadata materialization metadata dependency records or local files.
      - Reopening SQLiteLocalStore preserves candidate materialization and dependency expectations and the integrated fixture remains deterministic.
      - The V2L2 lot review records PASS only after V2L2.1 through V2L2.6 acceptance evidence, full fast CI and the retained native PyCOLMAP integration lane are green.
      - V2L3.1 is expanded to a complete deny-by-default executable contract before becoming the sole next ready item; no V2L3 product implementation is bundled.
    tests:
      - integrated exact-key candidate plus VERIFIED materialization fixture
      - absent missing and corrupted materialization negative cases
      - changed canonical key stale-candidate miss regression
      - post-verification byte-corruption regression preserving persisted expectations
      - dependency-scoped downstream invalidation with explicit non-destructive persistence/file assertions
      - store reopen and deterministic integrated-fixture regression
      - full fast CI
      - retained native PyCOLMAP integration workflow for final V2L2 handoff
      - V2L2 lot review checklist
  - id: V2L3.1
    lot: V2L3
    title: Adapter capability descriptor
    status: done
    objective: Define the minimal immutable solver-independent adapter capability descriptor with one open semantic capability identity plus explicit supported input and produced output ArtifactKind sets, without registry execution hardware model shipping failure metrics resume routing or quality behavior.
    allowed_scope:
      - src/wre/domain/adapter_capabilities.py
      - focused public-domain exports only if required for the new contract
      - tests/test_adapter_capabilities.py
    out_of_scope:
      - adapter implementation identity versions revisions or changes to ProducerRef and ArtifactProducerIdentity
      - model checkpoint hardware or runtime requirements owned by V2L3.2
      - shipping license or reproducibility registry schema owned by V2L3.3
      - adapter failure metrics or resume declarations owned by V2L3.4
      - registry validation default approval or selection owned by V2L3.5
      - registering retained COLMAP FFmpeg or ExifRead baselines owned by V2L3.6
      - execution protocols callbacks subprocess plugin discovery imports network access model loading or solver invocation
      - routing quality benchmark scheduling cache persistence or artifact materialization behavior
      - solver-private Python classes filenames filesystem paths mutable metadata or catch-all dictionaries
      - starting V2L3.2 implementation in this work item
    depends_on: [V2L2.6]
    read_before:
      - docs/02_ARCHITECTURE.md
      - docs/14_TECHNOLOGY_SELECTION.md
      - docs/15_PRODUCTION_RUNTIME.md
      - docs/23_ENGINEERING_EXECUTION.md
      - docs/24_SYSTEM_INVARIANTS.md
      - docs/25_V2_ROADMAP.md
      - docs/reviews/V2L2-artifact-dag-cache-review.md
      - src/wre/domain/artifacts.py
      - src/wre/domain/producer_identity.py
    reuse:
      - ArtifactKind as the solver-independent semantic input and output vocabulary
      - existing conservative validated-token and immutable value-object conventions
      - frozen dataclass and frozenset immutable collection conventions
      - fail-closed deterministic focused pytest style
    acceptance:
      - AdapterCapabilityName is a distinct immutable open symbolic token that is solver independent and conservatively validated; it is not a closed enum of all future algorithms.
      - AdapterCapabilityDescriptor is immutable and contains exactly AdapterCapabilityName plus immutable supported input ArtifactKind values and immutable produced output ArtifactKind values.
      - Input and output collections are typed frozensets; mutable lists or sets raw strings wrong member types and untyped substitutions are rejected rather than coerced.
      - An empty input set is valid for a capability with no WRE-artifact prerequisite; an empty output set is rejected because a descriptor must declare at least one produced WRE artifact kind.
      - Input and output ArtifactKind values may overlap so refinement or normalization capabilities are not forbidden by core.
      - Set semantics make equality and hashing independent of caller insertion order and duplicate inputs cannot carry hidden meaning.
      - The descriptor declares supported semantic kinds only and does not imply cardinality ordering concrete invocation signatures compatibility between arbitrary combinations or successful execution.
      - Capability identity is explicitly supplied and is never inferred from solver module class file model names ArtifactKind values ProducerRef or environment state.
      - No hardware model checkpoint license shipping reproducibility guarantee metric failure class resume capability registry membership ranking routing persistence or execution fields are introduced.
      - Existing ArtifactKind ArtifactProducerIdentity V2L2 cache and DAG behavior and retained V1 behavior remain unchanged.
    tests:
      - focused construction immutability equality and hashing tests for capability name and descriptor
      - single and multiple input and output cases plus insertion-order-independent set semantics
      - explicit empty-input and overlapping input-output cases
      - negative invalid capability-token empty-output mutable-collection raw-string and wrong-member tests
      - regression proving ArtifactKind and ArtifactProducerIdentity semantics remain unchanged
      - full fast CI
  - id: V2L3.2
    lot: V2L3
    title: Model checkpoint hardware identity
    status: done
    objective: Reuse the exact ModelIdentity and CheckpointIdentity already owned by V2L1.3 and add only one immutable caller-supplied normalized HardwareRuntimeIdentity that can optionally participate in canonical ArtifactKeyMaterial when hardware/runtime can materially affect output bytes, while preserving existing key bytes when that identity is absent.
    allowed_scope:
      - src/wre/domain/hardware_identity.py
      - src/wre/domain/artifact_keys.py
      - focused public-domain exports only if required for the new contract
      - tests/test_hardware_identity.py
      - focused additions to tests/test_artifact_keys.py
    out_of_scope:
      - modifying or duplicating ModelIdentity CheckpointIdentity ConfigurationIdentity ProducerRef or ArtifactProducerIdentity semantics
      - automatically detecting probing normalizing or hashing the current machine GPU driver CUDA runtime operating system Python environment or dependency set
      - CPU core GPU count VRAM RAM scratch storage output storage network duration or scheduling resource declarations owned by later resource-scheduling capabilities
      - supported-hardware compatibility matrices adapter registry membership defaults or route eligibility
      - shipping license redistribution source-repository reproducibility-note or approval schema owned by V2L3.3
      - adapter failure metric or resume declarations owned by V2L3.4
      - registry validation default approval or candidate selection owned by V2L3.5
      - registering retained COLMAP FFmpeg ExifRead or any concrete solver/tool baseline owned by V2L3.6
      - changing ArtifactMetadata persistence codec SQLite cache lookup invalidation materialization routing quality benchmark or execution behavior
      - filesystem process environment network model loading checkpoint download or solver invocation
      - starting V2L3.3 implementation in this work item
    depends_on: [V2L3.1]
    read_before:
      - docs/02_ARCHITECTURE.md
      - docs/14_TECHNOLOGY_SELECTION.md
      - docs/15_PRODUCTION_RUNTIME.md
      - docs/23_ENGINEERING_EXECUTION.md
      - docs/24_SYSTEM_INVARIANTS.md
      - docs/25_V2_ROADMAP.md
      - src/wre/domain/producer_identity.py
      - src/wre/domain/artifact_keys.py
      - tests/test_producer_identity.py
      - tests/test_artifact_keys.py
    reuse:
      - existing ModelIdentity and CheckpointIdentity as the only model/checkpoint identity primitives rather than creating V2L3 duplicates
      - Sha256Digest as the exact digest vocabulary for an already-normalized hardware/runtime identity
      - ArtifactKeyMaterial and canonical version-1 key serialization from V2L1.5
      - frozen dataclass and fail-closed typed-value conventions
    acceptance:
      - HardwareRuntimeIdentity is a distinct immutable typed identity containing exactly one caller-supplied Sha256Digest of already-normalized hardware/runtime identity material; raw strings and wrong digest types are rejected.
      - This work item defines no hardware/runtime normalization algorithm and performs no machine environment or registry lookup; callers supply the exact identity only when their adapter policy declares output hardware-sensitive.
      - Existing ModelIdentity and CheckpointIdentity remain unchanged and are reused directly; no competing model or checkpoint type is added.
      - ArtifactKeyMaterial gains one optional HardwareRuntimeIdentity field defaulting to None and rejects untyped non-None substitutes.
      - When hardware-runtime identity is None, canonical artifact-key bytes and the existing V2L1.5 golden ArtifactKey remain byte-for-byte unchanged so previously derived hardware-insensitive keys are stable.
      - When hardware-runtime identity is present, canonical serialization adds exactly one deterministic hardware-runtime digest field and otherwise preserves the version-1 preimage structure.
      - Identical computation material plus identical HardwareRuntimeIdentity yields the same ArtifactKey; changing only the hardware-runtime digest changes the ArtifactKey.
      - The core does not infer that an adapter is hardware-sensitive or hardware-insensitive, does not auto-populate the identity, and does not claim arbitrary cross-hardware equivalence.
      - No scheduler resource requirement compatibility matrix registry shipping license failure metric resume routing quality benchmark persistence cache policy or execution behavior is introduced.
      - Existing ArtifactKey candidate lookup V2L2 DAG/cache behavior ArtifactProducerIdentity and retained V1 behavior remain unchanged.
    tests:
      - focused HardwareRuntimeIdentity construction equality hashing immutability and wrong-type tests
      - ArtifactKeyMaterial optional hardware-runtime typed-boundary tests
      - regression proving None preserves the exact existing canonical golden bytes and ArtifactKey digest
      - deterministic present-hardware canonical serialization test
      - tests proving only hardware-runtime digest changes alter the key while identical identity remains stable
      - regression proving ModelIdentity CheckpointIdentity and ArtifactProducerIdentity behavior remains unchanged
      - full fast CI
  - id: V2L3.3
    lot: V2L3
    title: Shipping license reproducibility registry schema
    status: done
    objective: Define only a versioned machine-readable adapter/model registry envelope and closed entry schema for capability identity, exact producer/model/checkpoint references, dependency references, explicit artifact-key hardware policy, license review and shipping status, and reproducibility notes without adding concrete production entries or semantic registry validation.
    allowed_scope:
      - registry/adapter-models.yaml
      - registry/schemas/adapter-model-registry.schema.json
      - tests/test_adapter_model_registry_schema.py
    out_of_scope:
      - adding concrete COLMAP FFmpeg ExifRead or any other adapter model tool or baseline entry owned by V2L3.6
      - modifying reclassifying or duplicating version license source integration or review evidence already owned by registry/dependencies.yaml
      - production semantic or cross-registry validation including dependency existence entry uniqueness canonical ordering floating-alias checks and shipping-policy cross-field enforcement owned by V2L3.5
      - adapter failure taxonomy metric or resume declarations owned by V2L3.4
      - benchmark records quality policy default promotion ranking fallback or routing behavior
      - CPU core GPU count VRAM RAM scratch storage output storage network duration scheduling requirements or supported-hardware compatibility matrices
      - probing normalizing or computing HardwareRuntimeIdentity from a real machine or runtime
      - downloads imports plugin discovery subprocess execution model loading checkpoint resolution or network access
      - adding a new schema-validation runtime dependency or changing pyproject.toml uv.lock or registry/dependencies.yaml
      - starting V2L3.4 implementation in this work item
    depends_on: [V2L3.2]
    read_before:
      - docs/02_ARCHITECTURE.md
      - docs/14_TECHNOLOGY_SELECTION.md
      - docs/15_PRODUCTION_RUNTIME.md
      - docs/23_ENGINEERING_EXECUTION.md
      - docs/24_SYSTEM_INVARIANTS.md
      - docs/25_V2_ROADMAP.md
      - registry/dependencies.yaml
      - registry/schemas/fixture.schema.json
      - src/wre/domain/adapter_capabilities.py
      - src/wre/domain/producer_identity.py
      - src/wre/domain/hardware_identity.py
    reuse:
      - registry/dependencies.yaml keys as dependency evidence references instead of copying dependency version license or source records
      - AdapterCapabilityName and ArtifactKind wire-token semantics for capability input and output fields
      - ProducerRef ModelIdentity and CheckpointIdentity field shapes plus the existing lowercase Sha256Digest wire form
      - V2L3.2 hardware-sensitive artifact-key policy concept without storing one machine-specific hardware digest in the registry
      - existing closed JSON-schema conventions plus json and PyYAML already available in the development environment
    acceptance:
      - registry/adapter-models.yaml is a versioned initial envelope containing exactly schema_version 1 and an empty entries array, with no production approval or baseline entries.
      - The JSON schema uses Draft 2020-12, rejects undeclared root and nested fields with additionalProperties false, and requires exactly schema_version plus entries at the root.
      - A future registry entry structurally requires a stable explicit adapter_id, capability descriptor, exact producer identity, dependency_refs, nullable model and checkpoint identities, artifact_key_hardware_policy, license metadata, shipping_status and reproducibility_notes.
      - Capability input_kinds output_kinds and dependency_refs use array wire forms with uniqueItems true; output_kinds is non-empty while input_kinds may be empty, matching V2L3.1 semantics without introducing combination or cardinality inference.
      - Producer model and checkpoint fields mirror the existing explicit identity vocabulary; checkpoint SHA-256 uses exactly 64 lowercase hexadecimal characters and no field allows a floating latest alias by default-value inference.
      - artifact_key_hardware_policy is a closed enum containing exactly required and omitted; the registry stores policy only and never embeds probes or a machine-specific HardwareRuntimeIdentity digest.
      - License metadata is specific to the adapter model or checkpoint record and contains direct declaration plus review state transitive notes and redistribution notes; dependency_refs remain references to registry/dependencies.yaml rather than copies of dependency license evidence.
      - shipping_status is a closed enum containing exactly approved experimental benchmark-only and blocked.
      - reproducibility_notes is explicit text metadata and does not execute detect download or validate an environment.
      - V2L3.3 contains no failure metric resume benchmark routing default-selection execution or concrete baseline fields and creates no adapter/model entries.
      - Cross-entry uniqueness dependency-reference existence floating-alias rejection model/checkpoint coupling and shipping-policy semantic checks remain explicitly deferred to V2L3.5 rather than hidden in ad hoc runtime behavior.
      - Existing registry/dependencies.yaml AdapterCapabilityDescriptor ArtifactProducerIdentity HardwareRuntimeIdentity ArtifactKey behavior and retained V1 behavior remain unchanged.
    tests:
      - parse the JSON schema and empty YAML registry envelope deterministically
      - structural tests for Draft 2020-12 root required fields and additionalProperties false on root and nested objects
      - tests for closed shipping-status and hardware-policy enums exact lowercase SHA-256 pattern non-empty output kinds and uniqueItems set-like arrays
      - tests proving the initial registry contains no concrete entries and the schema contains no V2L3.4 failure metric or resume declarations
      - regression proving registry/dependencies.yaml remains a separate unchanged evidence source rather than duplicated into adapter-models.yaml
      - full fast CI
  - id: V2L3.4
    lot: V2L3
    title: Adapter failure metrics resume declarations
    status: done
    objective: Extend only the adapter/model registry entry contract with adapter-local raw failure signal names, exposed metric names and a minimal declared resume mode so later orchestration can reason over declared adapter surfaces without prematurely defining stable WRE failure taxonomy, metric semantics or checkpoint execution state.
    allowed_scope:
      - registry/schemas/adapter-model-registry.schema.json
      - focused additions to tests/test_adapter_model_registry_schema.py
    out_of_scope:
      - stable cross-adapter failure categories or mapping raw signals to WRE failure taxonomy owned by V2L4.1
      - metric values units dimensions directionality aggregations thresholds policies vectors or quality decisions owned by V2L4.2 through V2L4.5
      - checkpoint path hash payload last completed substage interruption reason compatibility record persistence resume execution retry or distributed checkpoint behavior owned by later checkpoint/orchestration capabilities
      - CPU core GPU class/count VRAM RAM scratch storage output storage network duration or scheduler resource requirements owned by V2L46
      - semantic or cross-registry validation entry uniqueness dependency existence floating alias rejection canonical ordering or shipping-policy enforcement owned by V2L3.5
      - concrete COLMAP FFmpeg ExifRead or other adapter/model entries owned by V2L3.6
      - changing the empty registry envelope dependency evidence producer/model/checkpoint identity ArtifactKey or hardware identity semantics
      - benchmark routing default selection fallback scheduling subprocess plugin loading network model download or solver execution
      - new runtime dependency pyproject.toml uv.lock changes or a schema-validation library
      - starting V2L3.5 implementation in this work item
    depends_on: [V2L3.3]
    read_before:
      - docs/02_ARCHITECTURE.md
      - docs/15_PRODUCTION_RUNTIME.md
      - docs/23_ENGINEERING_EXECUTION.md
      - docs/24_SYSTEM_INVARIANTS.md
      - docs/25_V2_ROADMAP.md
      - registry/adapter-models.yaml
      - registry/schemas/adapter-model-registry.schema.json
      - tests/test_adapter_model_registry_schema.py
    reuse:
      - existing open lowercase token definition in the adapter-model registry schema for adapter-local failure-signal and metric-name vocabulary
      - existing closed entry schema and focused structural test style from V2L3.3
      - empty adapter-model registry envelope unchanged until concrete baseline registration in V2L3.6
      - later V2L4 failure and metric contracts as explicit future owners rather than duplicating their semantics here
    acceptance:
      - The closed adapter registry entry schema requires exactly three additional declaration fields failure_signals, metric_names and resume_mode alongside the V2L3.3 fields.
      - failure_signals is an array of unique open token strings representing adapter-local raw failure signal names only; it may be empty and carries no stable WRE failure-category meaning or fallback policy.
      - metric_names is an array of unique open token strings representing names of metrics an adapter can expose; it may be empty and carries no values units directionality thresholds aggregation or quality-gate semantics.
      - resume_mode is a required closed enum containing exactly unsupported and checkpoint, with no implicit default or truthy/falsy coercion.
      - Declaring checkpoint resume capability does not store a checkpoint path digest payload substage interruption reason or compatibility decision and does not execute resume behavior.
      - The schema remains Draft 2020-12 with additionalProperties false at root and every object boundary already closed by V2L3.3.
      - registry/adapter-models.yaml remains exactly the empty versioned envelope and no concrete adapter/model entry is introduced.
      - V2L3.4 defines no stable failure taxonomy, failure mapping, metric vector/value schema, checkpoint storage format, scheduler resource contract, benchmark or runtime execution behavior.
      - Existing V2L3.1 capability declarations V2L3.2 hardware-sensitive artifact identity V2L3.3 shipping/license/reproducibility metadata registry/dependencies.yaml and retained V1 behavior remain unchanged.
    tests:
      - structural test proving entry required/properties add exactly failure_signals metric_names and resume_mode
      - uniqueItems token-array tests for failure_signals and metric_names including explicit empty-array allowance
      - closed resume_mode enum test proving exactly unsupported and checkpoint with no default
      - negative structural regression proving no stable failure taxonomy metric value threshold unit checkpoint path/hash or scheduler resource fields enter the schema
      - regression proving registry/adapter-models.yaml remains empty and existing V2L3.3 schema fields remain unchanged
      - full fast CI
  - id: V2L3.5
    lot: V2L3
    title: Production registry validator
    status: ready
    objective: Extend the existing fail-closed repository metadata validator so adapter/model registry entries are validated against the exact V2L3.4 structural contract and deterministic production semantics, including canonical ordering, exact identities, dependency-reference existence and minimal shipping/license consistency, without selecting, loading or executing any adapter.
    allowed_scope:
      - src/wre/repo_validation.py
      - focused additions to tests/test_repo_validation.py
    out_of_scope:
      - modifying the V2L3.4 adapter-model registry schema or adding fields to it
      - adding concrete COLMAP FFmpeg ExifRead model checkpoint or other adapter registry entries owned by V2L3.6
      - modifying registry/dependencies.yaml dependency version license source role integration or review evidence
      - introducing a generic JSON Schema engine schema-validation dependency pyproject.toml or uv.lock change
      - default adapter selection ranking route eligibility fallback benchmark promotion or quality policy
      - stable WRE failure taxonomy or raw-signal mapping owned by V2L4.1
      - metric values units dimensions directionality thresholds aggregation vectors or quality decisions owned by V2L4.2 through V2L4.5
      - checkpoint persistence paths hashes payloads resume execution retry or scheduler resource behavior
      - downloads imports plugin discovery subprocess execution model loading network access or runtime environment probing
      - starting V2L3.6 baseline registration in this work item
    depends_on: [V2L3.4]
    read_before:
      - docs/14_TECHNOLOGY_SELECTION.md
      - docs/15_PRODUCTION_RUNTIME.md
      - docs/23_ENGINEERING_EXECUTION.md
      - docs/24_SYSTEM_INVARIANTS.md
      - docs/25_V2_ROADMAP.md
      - registry/adapter-models.yaml
      - registry/schemas/adapter-model-registry.schema.json
      - registry/dependencies.yaml
      - src/wre/repo_validation.py
      - tests/test_repo_validation.py
      - src/wre/domain/producer_identity.py
      - src/wre/domain/runs.py
    reuse:
      - existing validate_repository error-accumulation and repository-metadata failure boundary rather than adding a parallel validator command
      - existing temporary copied-repository mutation pattern in tests/test_repo_validation.py
      - V2L3.4 schema field vocabulary and closed enums as the single registry contract source
      - registry/dependencies.yaml keys and license_review values as dependency cross-reference evidence
      - existing exact identity semantics including lowercase SHA-256 and ModelIdentity rejection of floating latest
    acceptance:
      - The current empty schema_version 1 adapter-model registry validates successfully through validate_repository with no special-case bypass that would skip validation once entries exist.
      - Missing unreadable malformed wrong-root-type or unsupported-schema-version adapter-model registry data fails closed with deterministic repository-validation errors rather than being ignored.
      - Every registry entry must be a mapping with exactly the V2L3.4 required fields; missing or undeclared fields wrong scalar/container types invalid tokens closed-enum values blank required text and malformed checkpoint SHA-256 are rejected.
      - adapter_id values are unique and the entries array is in canonical lexicographic adapter_id order; the validator never silently sorts or deduplicates input.
      - capability.input_kinds capability.output_kinds dependency_refs failure_signals and metric_names are canonical lexicographically sorted unique token arrays; output_kinds remains non-empty while the other arrays may be empty as declared by their owning contracts.
      - Every dependency_refs value must exactly name an existing registry/dependencies.yaml dependency key; unknown references fail closed and dependency metadata is never copied into the adapter registry.
      - Producer and model version fields reject the case-insensitive floating alias latest; model/checkpoint structures preserve the exact V2L1.3 identity rules, including checkpoint requiring model and checkpoint SHA-256 being exactly 64 lowercase hexadecimal characters.
      - shipping_status approved requires the entry license.review to be approved and every referenced dependency to have license_review approved; pending license review may remain experimental or benchmark-only but cannot be approved.
      - An entry whose own license.review is blocked must have shipping_status blocked; validation reports the inconsistency and never promotes or mutates the entry.
      - Validation is pure repository inspection: it performs no network filesystem discovery outside declared metadata files, model download environment probing adapter import execution routing selection or benchmark lookup.
      - Existing roadmap component review and pull-request-template validation behavior remains unchanged and existing valid repository metadata still passes.
      - V2L3.6 is expanded to a full executable contract before becoming the sole next ready item; this work item does not add any concrete registry baseline.
    tests:
      - current empty-registry repository validation regression
      - valid synthetic registry-entry fixture covering native and optional model/checkpoint shapes without committing a real baseline entry
      - malformed root schema-version missing/extra field wrong-type invalid-token enum blank-text and malformed-SHA negative cases
      - duplicate and unsorted adapter_id plus duplicate/unsorted set-like collection rejection tests
      - unknown dependency-reference and dependency evidence separation tests
      - floating-latest and checkpoint-without-model negative tests
      - approved shipping with pending or blocked entry/dependency license-review negative tests plus allowed experimental/benchmark-only pending-review cases
      - regression tests for existing roadmap component review and active-work-item validation
      - full fast CI
  - {id: V2L3.6, lot: V2L3, title: Register retained production baselines, status: planned, depends_on: [V2L3.5]}
  - {id: V2L4.1, lot: V2L4, title: Stable failure taxonomy, status: planned, depends_on: [V2L3.6]}
  - {id: V2L4.2, lot: V2L4, title: Multidimensional metric vector, status: planned, depends_on: [V2L4.1]}
  - {id: V2L4.3, lot: V2L4, title: Quality decision contract, status: planned, depends_on: [V2L4.2]}
  - {id: V2L4.4, lot: V2L4, title: Benchmark record schema, status: planned, depends_on: [V2L4.3]}
  - {id: V2L4.5, lot: V2L4, title: Deterministic quality policy evaluator, status: planned, depends_on: [V2L4.4]}
  - {id: V2L4.6, lot: V2L4, title: Unknown metric and failure negative tests, status: planned, depends_on: [V2L4.5]}
