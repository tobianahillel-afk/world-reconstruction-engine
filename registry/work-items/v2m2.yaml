schema_version: 1
milestone: V2M2
work_items:
  - id: V2L11.1
    lot: V2L11
    title: Canonical CameraSolution contract
    status: done
    objective: Define the minimal immutable solver-independent per-observation CameraSolution contract with explicit local coordinate frame projection-model identity image dimensions finite intrinsic parameters camera-from-local-frame rigid extrinsics optional exact uncertainty-artifact hooks and canonical metrics without solving cameras asserting metric/world placement or creating any depth point or geometry assembly product.
    allowed_scope:
      - src/wre/domain/camera_solutions.py
      - focused exports in src/wre/domain/__init__.py
      - tests/test_camera_solutions.py
      - focused lifecycle updates to registry/components.yaml PROJECT_STATE.yaml and this work-item registry only after implementation evidence is green so V2L11.2 can be activated without implementing it
    out_of_scope:
      - changing legacy CameraCalibrationEstimate CameraPoseEstimate SparseReconstructionEstimate LocalFrameId ImageDimensions ArtifactRef MetricVector or existing camera metadata semantics
      - reusing or wrapping solver-native COLMAP camera image reconstruction objects in the stable contract
      - converting legacy SparseReconstructionEstimate or COLMAP import output which belongs to V2L11.5 and V2L12
      - physical CameraId identity inference calibration sharing rig modeling rolling-shutter pose curves or multi-camera identity resolution
      - metric-scale inference world Earth ECEF ENU geographic anchoring cross-local-frame alignment or fragment placement
      - DepthField PointMap GeometrySolution SurfaceModel dense geometry physical surface or any V2L11.2 through V2L16 behavior
      - covariance estimation uncertainty computation quality-gate execution route selection adapter/model execution persistence or I/O
      - starting V2L11.2 implementation in this work item
    depends_on: [V2L10.6]
    read_before:
      - docs/02_ARCHITECTURE.md
      - docs/03_PIPELINE.md
      - docs/04_DATA_MODEL.md
      - docs/19_COLMAP_RECONSTRUCTION_IMPORT.md
      - docs/23_ENGINEERING_EXECUTION.md
      - docs/24_SYSTEM_INVARIANTS.md
      - docs/25_V2_ROADMAP.md
      - registry/v1-v2-migration.yaml
      - docs/reviews/V2L10-routing-v2m1-review.md
      - src/wre/domain/cameras.py
      - src/wre/domain/fragments.py
      - src/wre/domain/estimated_geometry.py
      - src/wre/domain/artifacts.py
      - src/wre/domain/metrics.py
      - src/wre/domain/__init__.py
      - tests/test_cameras.py
      - tests/test_fragments.py
      - tests/test_estimated_geometry.py
    reuse:
      - ObservationId as the exact source observation whose camera is solved
      - LocalFrameId as the explicit unanchored local coordinate-frame identity without importing legacy sparse-model lifecycle semantics
      - ImageDimensions for positive pixel dimensions
      - ArtifactRef for optional exact uncertainty/covariance artifact hooks rather than embedding a solver-specific covariance layout
      - MetricVector for canonical typed finite camera-quality measurements rather than adding a generic score map
      - the retained camera-from-local-frame convention documented by L3.6 while defining a new V2 canonical contract independent of legacy estimate classes
    acceptance:
      - Add immutable CameraSolutionId using the established 1-128 character opaque identifier syntax.
      - Add immutable CameraProjectionModelName using a 1-128 character lowercase semantic token syntax so projection parameter semantics are explicit without freezing a vendor enum.
      - Add immutable CameraSolution containing exactly solution_id CameraSolutionId observation_id ObservationId local_frame_id LocalFrameId projection_model CameraProjectionModelName dimensions ImageDimensions intrinsic_parameters tuple of float rotation_matrix immutable 3x3 tuple translation_xyz immutable 3-vector uncertainty_artifacts tuple of ArtifactRef and metrics MetricVector.
      - intrinsic_parameters must be a non-empty immutable tuple of exact finite float values; bool integers NaN and infinity fail closed rather than being silently normalized.
      - rotation_matrix must be an immutable finite 3x3 proper rotation with unit orthogonal rows and determinant plus one within the established 1e-6 numeric tolerance.
      - translation_xyz must be an immutable finite 3-vector of exact float values; bool integers NaN and infinity fail closed rather than being silently normalized.
      - 'Extrinsic semantics are fixed and tested as camera-from-local-frame: x_camera equals rotation_matrix multiplied by x_local plus translation_xyz. No world-from-camera alias camera-center field or implicit inversion is exposed.'
      - local_frame_id names only the solution coordinate frame and makes no metric scale Earth alignment geographic placement or compatibility claim with another LocalFrameId.
      - projection_model plus intrinsic_parameters are solver-independent descriptive camera calibration values and do not assert physical CameraId identity.
      - uncertainty_artifacts may be empty; when present they must be immutable canonical unique ArtifactRef values ordered by ArtifactId then ArtifactKind and conflicting kinds for one ArtifactId fail closed.
      - metrics must be the exact caller-supplied MetricVector object and no total score confidence rank threshold or quality decision is derived by CameraSolution.
      - CameraSolution retains exact caller-supplied identity observation frame projection dimensions uncertainty tuple and metrics after validation; validated numeric tuples remain immutable values.
      - The contract contains no solver adapter model checkpoint route quality mode scale status world transform GPS timestamp depth points surface appearance dynamic state historical state or generic metadata field.
      - No persistence codec registry mutation filesystem network subprocess external dependency or camera-solving execution surface is introduced.
    tests:
      - CameraSolutionId equality hashing immutability syntax validation and string conversion
      - CameraProjectionModelName equality hashing immutability lowercase-token validation and string conversion
      - CameraSolution frozen exact field shape and wrong-type rejection for every typed field
      - intrinsic parameter non-empty exact-float finite validation including bool integer NaN infinity rejection
      - rigid rotation identity and representative valid rotation acceptance plus malformed shape non-finite non-unit non-orthogonal reflection and determinant failure cases
      - translation immutable exact-float finite validation including bool integer NaN infinity rejection
      - explicit camera-from-local-frame sign convention regression using a known rotation translation and local point
      - empty and populated canonical uncertainty artifact validation including duplicate decreasing wrong-type and conflicting-kind rejection
      - exact MetricVector and other caller-object retention
      - regression proving no physical CameraId metric/world/geographic scale depth geometry route solver execution persistence or generic metadata surface
      - full fast CI and CodeQL plus native lanes only where repository path triggers require them
  - id: V2L11.2
    lot: V2L11
    title: DepthField validity confidence contract
    status: done
    objective: Define the minimal immutable solver-independent per-observation DepthField semantic contract tied to one canonical CameraSolution with explicit image dimensions depth-value convention row-major depth samples validity and optional normalized confidence plus canonical metrics without back-projecting points fusing fields asserting metric or world scale or defining a production dense-depth storage format.
    allowed_scope:
      - src/wre/domain/depth_fields.py
      - focused exports in src/wre/domain/__init__.py
      - tests/test_depth_fields.py
      - focused lifecycle updates to registry/components.yaml PROJECT_STATE.yaml and this work-item registry only after implementation evidence is green so V2L11.3 can be activated without implementing it
    out_of_scope:
      - changing CameraSolution CameraSolutionId ObservationId ImageDimensions LocalFrameId MetricVector or legacy estimated-geometry semantics
      - changing the camera-from-local-frame convention or adding physical CameraId identity metric scale world Earth ECEF ENU geographic anchoring cross-local-frame alignment or fragment placement
      - solver-specific tensor array image-file EXR PFM NumPy Torch CUDA memory-map chunk compression persistence codec or production dense-depth artifact layout which belongs to later dense-depth work
      - depth inference monocular depth MVS PatchMatch learned models disparity conversion convention conversion confidence calibration or threshold learning
      - PointMap back-projection point generation GeometrySolution assembly fusion refinement SurfaceModel collision measurement or any V2L11.3 through V2L16 behavior
      - deriving pass fail quality decisions scalar total scores route selection retry fallback adapter execution model execution filesystem network subprocess or registry mutation
      - starting V2L11.3 implementation in this work item
    depends_on: [V2L11.1]
    read_before:
      - docs/02_ARCHITECTURE.md
      - docs/03_PIPELINE.md
      - docs/04_DATA_MODEL.md
      - docs/23_ENGINEERING_EXECUTION.md
      - docs/24_SYSTEM_INVARIANTS.md
      - docs/25_V2_ROADMAP.md
      - docs/28_PERFORMANCE_OPTIMIZATION_PLAYBOOK.md
      - docs/29_PERFORMANCE_INTEGRATION_MAP.md
      - registry/v1-v2-migration.yaml
      - src/wre/domain/camera_solutions.py
      - src/wre/domain/cameras.py
      - src/wre/domain/observations.py
      - src/wre/domain/metrics.py
      - src/wre/domain/__init__.py
      - tests/test_camera_solutions.py
      - tests/test_metrics.py
    reuse:
      - CameraSolutionId as the exact camera calibration and pose solution to which the depth raster is bound instead of duplicating intrinsics extrinsics or local-frame identity
      - ObservationId as the exact source observation whose pixel grid the field describes
      - ImageDimensions as the positive raster dimensions and canonical pixel-count source
      - MetricVector as canonical typed finite field-quality measurements without adding an untyped score dictionary
      - immutable tuple and exact-float fail-closed conventions already established by CameraSolution while keeping production dense-depth storage separately owned
    acceptance:
      - Add immutable DepthFieldId using the established 1-128 character opaque identifier syntax.
      - Add immutable DepthValueConventionName using the established 1-128 character lowercase semantic-token syntax so camera-z ray-distance inverse-depth or future canonicalized conventions can be named explicitly without freezing a solver or vendor enum.
      - Add immutable DepthField containing exactly depth_field_id DepthFieldId observation_id ObservationId camera_solution_id CameraSolutionId dimensions ImageDimensions depth_value_convention DepthValueConventionName depth_values tuple of float validity tuple of bool confidence tuple of float or None and metrics MetricVector.
      - depth_values validity and any present confidence are row-major pixel rasters whose exact length equals dimensions width_px multiplied by height_px; mutable collections short or long rasters and wrong member types fail closed rather than being reshaped padded or coerced.
      - every depth_values member must be an exact finite float; a valid pixel requires strictly positive depth and an invalid pixel requires canonical 0.0 so unsupported pixels never depend on NaN infinity negative values or arbitrary sentinel payloads.
      - validity members must be exact bool values and are the sole truth-bearing indication that the corresponding depth sample is supported; zero depth by itself is not interpreted without the validity contract.
      - confidence may be None to represent no producer confidence claim; when present it must be an immutable tuple of exact finite float values in the inclusive range zero to one with the same pixel count and invalid pixels must carry canonical 0.0 confidence.
      - confidence is a producer-supplied normalized support value only; DepthField does not interpret it as a calibrated probability derive a threshold rank aggregate confidence quality decision or route consequence.
      - depth_value_convention is descriptive and retained exactly; DepthField performs no camera-z ray-distance inverse-depth or unit conversion and consumers must not compare fields with different conventions as if values were interchangeable.
      - the referenced CameraSolution owns calibration extrinsics and local-frame identity; DepthField duplicates none of them and makes no claim that local depth units are meters or Earth/world aligned.
      - metrics must be the exact caller-supplied MetricVector object and no metrics are computed from depth validity or confidence by this value contract.
      - DepthField retains the exact caller-supplied identities dimensions convention raster tuples optional confidence tuple and metrics after validation and performs no registry filesystem database environment or network lookup.
      - The contract contains no LocalFrameId CameraId scale status world transform GPS point map point coordinates geometry solution surface appearance dynamic state historical state solver adapter model checkpoint route quality mode generic metadata or persistence field.
      - No production dense-depth serialization compression chunking memory mapping GPU tensor representation persistence I/O external dependency or depth-solving execution surface is introduced.
    tests:
      - DepthFieldId equality hashing immutability syntax validation and string conversion
      - DepthValueConventionName equality hashing immutability lowercase-token validation and string conversion
      - DepthField frozen exact field shape wrong-type rejection and exact caller-object retention
      - row-major raster pixel-count validation for depth validity and optional confidence including mutable short long and wrong-member negative cases
      - exact finite float depth validation including bool integer string NaN infinity negative and valid-zero rejection plus canonical invalid-zero enforcement
      - exact bool validity validation including integer and string rejection
      - absent confidence acceptance plus populated exact-float finite zero-to-one validation length checks and invalid-pixel zero-confidence enforcement
      - representative multiple depth-value convention tokens retained without implicit conversion normalization or equality conflation
      - regression proving no duplicated camera calibration extrinsics local-frame metric/world/geographic point geometry surface route solver persistence or generic metadata surface
      - module-surface regression proving no filesystem network subprocess database NumPy Torch PyCOLMAP or other external execution dependency is introduced
      - full fast CI and CodeQL plus native lanes only where repository path triggers require them
  - id: V2L11.3
    lot: V2L11
    title: PointMap contract
    status: done
    objective: Define the minimal immutable solver-independent PointMap contract for an ordered dense-or-sparse collection of local-frame 3D point predictions with explicit source-observation support optional per-point normalized confidence and canonical metrics without assembling a GeometrySolution resolving scale back-projecting DepthField values or introducing solver-specific point-cloud storage.
    allowed_scope:
      - src/wre/domain/point_maps.py
      - focused exports in src/wre/domain/__init__.py
      - tests/test_point_maps.py
      - focused lifecycle updates to registry/components.yaml PROJECT_STATE.yaml and this work-item registry only after implementation evidence is green so V2L11.4 can be activated without implementing it
    out_of_scope:
      - changing CameraSolution DepthField ObservationId LocalFrameId MetricVector or legacy Point3DEstimate SparseReconstructionEstimate semantics
      - deriving points by depth back-projection triangulation SfM MVS SLAM feed-forward model execution fusion or any other reconstruction algorithm
      - pixel-grid topology image dimensions pixel coordinates feature tracks correspondence tracks reprojection errors normals colors descriptors labels semantics materials or surface connectivity
      - metric-scale inference scale-status resolution world Earth ECEF ENU geographic anchoring cross-local-frame alignment fragment placement or coordinate conversion
      - GeometrySolution assembly competing-solution comparison SurfaceModel dense fusion collision measurement or any V2L11.4 through V2L16 behavior
      - solver-specific PLY LAS LAZ OBJ tensor array NumPy Torch CUDA Open3D point-cloud object memory-map chunk compression persistence codec or production point-storage representation
      - per-point calibrated probability threshold ranking quality decision route selection retry fallback adapter/model execution filesystem network subprocess or registry mutation
      - starting V2L11.4 implementation in this work item
    depends_on: [V2L11.2]
    read_before:
      - docs/02_ARCHITECTURE.md
      - docs/03_PIPELINE.md
      - docs/04_DATA_MODEL.md
      - docs/23_ENGINEERING_EXECUTION.md
      - docs/24_SYSTEM_INVARIANTS.md
      - docs/25_V2_ROADMAP.md
      - docs/27_RESEARCH_CANDIDATE_COVERAGE.md
      - docs/28_PERFORMANCE_OPTIMIZATION_PLAYBOOK.md
      - docs/29_PERFORMANCE_INTEGRATION_MAP.md
      - registry/v1-v2-migration.yaml
      - src/wre/domain/camera_solutions.py
      - src/wre/domain/depth_fields.py
      - src/wre/domain/fragments.py
      - src/wre/domain/observations.py
      - src/wre/domain/metrics.py
      - src/wre/domain/estimated_geometry.py
      - src/wre/domain/__init__.py
      - tests/test_camera_solutions.py
      - tests/test_depth_fields.py
      - tests/test_estimated_geometry.py
    reuse:
      - LocalFrameId as the explicit local coordinate-frame identity without importing legacy sparse-model lifecycle scale or fragment semantics
      - ObservationId as canonical source-support identity for observations that contributed evidence to the point map
      - MetricVector as canonical typed finite point-map measurements without adding an untyped score dictionary
      - the exact-float immutable-tuple fail-closed conventions established by CameraSolution and DepthField while deliberately not reusing legacy coercing Point3DEstimate helpers
      - artifact-DAG provenance and producer identity outside this value contract rather than embedding solver model checkpoint or persistence metadata into PointMap
    acceptance:
      - Add immutable PointMapId using the established 1-128 character opaque identifier syntax.
      - Add immutable PointMap containing exactly point_map_id PointMapId local_frame_id LocalFrameId source_observation_ids tuple of ObservationId positions_xyz tuple of immutable 3-float tuples confidence tuple of float or None and metrics MetricVector.
      - source_observation_ids must be a non-empty immutable tuple of exact ObservationId values in canonical ascending ObservationId order with no duplicates; decreasing duplicate mutable or wrong-type support fails closed and input is never silently sorted.
      - positions_xyz must be an immutable tuple whose members are immutable 3-tuples of exact finite float values; bool integers strings NaN infinity malformed vectors and mutable nested values fail closed rather than being coerced.
      - positions_xyz may be empty so an explicit no-point prediction can remain truthful without fabricating geometry; PointMap does not turn an empty result into a failure category or quality decision.
      - the point sequence is retained exactly as supplied and is not sorted deduplicated fused clustered or interpreted as pixel order; equal coordinates may coexist because this semantic contract does not assert point identity correspondence or topology.
      - confidence may be None to represent no producer confidence claim; when present it must be an immutable tuple with exactly one exact finite float per point in the inclusive range zero to one and is retained exactly.
      - confidence is producer-supplied normalized support only; PointMap does not interpret it as a calibrated probability derive thresholds ranks aggregate confidence quality decisions or route consequences.
      - local_frame_id names only the coordinate frame of positions_xyz and makes no metric scale Earth alignment geographic placement fragment-membership or compatibility claim with another LocalFrameId.
      - source_observation_ids express aggregate source support only and do not create per-point tracks feature correspondences camera identity pixel alignment or triangulation provenance.
      - metrics must be the exact caller-supplied MetricVector object and no metric is computed from point count coordinates source support or confidence by this value contract.
      - PointMap retains exact caller-supplied point-map identity frame source-support tuple point sequence optional confidence tuple and metrics after validation and performs no registry filesystem database environment or network lookup.
      - The contract contains no CameraId CameraSolutionId DepthFieldId ImageDimensions scale status world transform GPS pixel coordinates tracks geometry-solution surface appearance dynamic state historical state solver adapter model checkpoint route quality mode generic metadata or persistence field.
      - No point generation back-projection triangulation serialization compression chunking memory mapping GPU tensor representation persistence I/O external dependency or solver execution surface is introduced.
    tests:
      - PointMapId equality hashing immutability syntax validation and string conversion
      - PointMap frozen exact field shape wrong-type rejection and exact caller-object retention
      - canonical non-empty source-observation validation including mutable duplicate decreasing and wrong-member rejection without silent sorting
      - empty positions acceptance plus immutable nested 3-vector shape exact-float finite validation including bool integer string NaN infinity and mutable-vector rejection
      - representative ordered positions retained exactly including repeated coordinate values without sorting deduplication fusion or topology inference
      - absent confidence acceptance plus populated exact-float finite zero-to-one validation exact point-count matching and caller-tuple retention
      - exact MetricVector retention with no derived count confidence quality or route decision
      - regression proving no camera/depth duplication metric/world/geographic scale pixel track GeometrySolution surface route solver persistence or generic metadata surface
      - module-surface regression proving no filesystem network subprocess database NumPy Torch Open3D PyCOLMAP or other external execution dependency is introduced
      - full fast CI and CodeQL plus native lanes only where repository path triggers require them
  - id: V2L11.4
    lot: V2L11
    title: GeometrySolution assembly local scale contract
    status: done
    objective: Define the minimal immutable solver-independent GeometrySolution assembly contract for one coherent local-frame reconstruction hypothesis referencing canonical camera depth and point products with explicit unresolved-or-metric local-scale semantics and canonical metrics without reconstructing geometry resolving scale performing world anchoring comparing competing hypotheses or defining a physical surface.
    allowed_scope:
      - src/wre/domain/geometry_solutions.py
      - focused exports in src/wre/domain/__init__.py
      - tests/test_geometry_solutions.py
      - focused lifecycle updates to registry/components.yaml PROJECT_STATE.yaml and this work-item registry only after implementation evidence is green so V2L11.5 can be activated without implementing it
    out_of_scope:
      - changing CameraSolution DepthField PointMap LocalFrameId MetricVector or legacy LocalScaleStatus SparseReconstructionEstimate semantics
      - converting legacy SparseReconstructionEstimate data or COLMAP import output which belongs to V2L11.5 and V2L12
      - estimating cameras depth points scale alignment transforms metric scale or any reconstruction refinement optimization bundle adjustment triangulation fusion or solver execution
      - world Earth ECEF ENU geographic anchoring absolute placement cross-local-frame alignment fragment creation fragment membership or fragment merging
      - comparing ranking selecting merging or reconciling competing GeometrySolution hypotheses which belongs to V2L14
      - SurfaceModel mesh SDF collision navigation measurement appearance material environment dynamic historical or runtime representation behavior
      - embedding child CameraSolution DepthField or PointMap payloads solver-native objects tensors point arrays depth rasters tracks correspondences or other heavy geometry data in GeometrySolution
      - artifact persistence codec database filesystem network subprocess adapter/model execution route selection fallback retry quality-policy execution or registry mutation
      - starting V2L11.5 implementation in this work item
    depends_on: [V2L11.3]
    read_before:
      - docs/02_ARCHITECTURE.md
      - docs/03_PIPELINE.md
      - docs/04_DATA_MODEL.md
      - docs/19_COLMAP_RECONSTRUCTION_IMPORT.md
      - docs/23_ENGINEERING_EXECUTION.md
      - docs/24_SYSTEM_INVARIANTS.md
      - docs/25_V2_ROADMAP.md
      - docs/26_V2_PRODUCT_ARCHITECTURE_FREEZE.md
      - registry/v1-v2-migration.yaml
      - src/wre/domain/camera_solutions.py
      - src/wre/domain/depth_fields.py
      - src/wre/domain/point_maps.py
      - src/wre/domain/fragments.py
      - src/wre/domain/metrics.py
      - src/wre/domain/estimated_geometry.py
      - src/wre/domain/__init__.py
      - tests/test_camera_solutions.py
      - tests/test_depth_fields.py
      - tests/test_point_maps.py
      - tests/test_estimated_geometry.py
    reuse:
      - LocalFrameId as the explicit coordinate-frame identity for the assembled hypothesis without implying fragment identity global placement or compatibility with any other local frame
      - CameraSolutionId DepthFieldId and PointMapId as canonical lightweight child-product references instead of duplicating camera depth or point payloads inside GeometrySolution
      - MetricVector as canonical typed finite geometry measurements without adding untyped score maps scalar confidence or policy outcomes
      - the legacy unresolved-versus-metric scale distinction only as a semantic donor while defining a new canonical GeometryScaleStatus independent of legacy LocalScaleStatus compatibility
      - artifact-DAG provenance producer identity dependency validation and persistence outside this immutable value contract
    acceptance:
      - Add immutable GeometrySolutionId using the established 1-128 character opaque identifier syntax.
      - Add GeometryScaleStatus as a stable string enum containing exactly UNRESOLVED with value unresolved and METRIC with value metric.
      - Add immutable GeometrySolution containing exactly geometry_solution_id GeometrySolutionId local_frame_id LocalFrameId scale_status GeometryScaleStatus camera_solution_ids tuple of CameraSolutionId depth_field_ids tuple of DepthFieldId point_map_ids tuple of PointMapId and metrics MetricVector.
      - camera_solution_ids must be a non-empty immutable tuple of exact CameraSolutionId values in canonical ascending identifier order with no duplicates; mutable duplicate decreasing or wrong-type input fails closed and is never silently sorted.
      - depth_field_ids and point_map_ids must each be immutable tuples of exact typed identifiers in canonical ascending identifier order with no duplicates; either tuple may be empty but they may not both be empty because a GeometrySolution must assemble at least one canonical geometry carrier beyond cameras.
      - child reference tuples are retained exactly as supplied after validation and GeometrySolution performs no registry artifact-store or object dereference to inspect child payloads infer observation membership or silently repair cross-product relationships.
      - local_frame_id declares the single local coordinate frame of the coherent hypothesis; producers may list only camera depth and point products already interpreted in that frame and any cross-frame alignment belongs to a separate explicit operation.
      - GeometryScaleStatus.UNRESOLVED means local distances have an arbitrary unresolved scale and must not be interpreted as meters; GeometryScaleStatus.METRIC means one local-frame distance unit is one meter while origin orientation Earth alignment and global placement remain unspecified.
      - GeometrySolution contains no scale factor because metric scale is represented by geometry already expressed in meter-valued local coordinates; converting or rescaling coordinates is outside this value contract.
      - scale status is descriptive truth-bearing state only and GeometrySolution performs no scale inference calibration GPS GCP anchor lookup coordinate conversion or metric upgrade.
      - metrics must be the exact caller-supplied MetricVector object and no total score aggregate confidence preferred flag rank threshold quality decision or route consequence is derived.
      - GeometrySolution retains exact caller-supplied identity frame scale status child-reference tuples and metrics after validation and performs no filesystem database environment network or external dependency lookup.
      - The contract contains no ObservationId CameraId raw intrinsics extrinsics depth raster point coordinates tracks correspondences fragment identity world transform GPS anchor solver adapter model checkpoint route quality mode surface appearance generic metadata timestamp or persistence field.
      - No reconstruction assembly algorithm beyond validating references no legacy conversion no geometry computation no serialization no external dependency and no solver execution surface is introduced.
    tests:
      - GeometrySolutionId equality hashing immutability syntax validation and string conversion
      - GeometryScaleStatus exact enum members and values with wrong-type scale-status rejection in GeometrySolution
      - GeometrySolution frozen exact field shape wrong-core-type rejection and exact caller-object retention
      - canonical non-empty camera-solution reference validation including mutable duplicate decreasing and wrong-member rejection without silent sorting
      - canonical depth-field and point-map reference validation including empty valid tuples plus mutable duplicate decreasing and wrong-member rejection
      - rejection when both depth_field_ids and point_map_ids are empty plus acceptance of representative depth-only point-only and mixed geometry-carrier assemblies
      - local-frame and scale-semantics regressions proving unresolved scale is not metric and metric scale does not imply Earth world global placement orientation or origin
      - exact MetricVector retention with no derived score confidence ranking quality or route decision
      - regression proving no embedded child payload observation membership raw camera depth point coordinate track fragment world anchor surface solver persistence or generic metadata surface
      - module-surface regression proving no filesystem network subprocess database NumPy Torch Open3D PyCOLMAP or other external execution dependency is introduced
      - full fast CI and CodeQL plus native lanes only where repository path triggers require them
  - id: V2L11.5
    lot: V2L11
    title: One way legacy SparseReconstructionEstimate conversion
    status: done
    objective: Define and implement one deterministic in-memory one-way conversion from the retained legacy SparseReconstructionEstimate donor representation into canonical V2 CameraSolution PointMap and GeometrySolution products while preserving local-frame camera-from-local extrinsics and explicit scale status without modifying legacy contracts reading solver-native artifacts inventing missing evidence or creating a permanent bidirectional compatibility layer.
    allowed_scope:
      - src/wre/reconstruction/legacy_geometry_conversion.py
      - focused exports in src/wre/reconstruction/__init__.py
      - tests/test_legacy_geometry_conversion.py
      - focused lifecycle updates to registry/components.yaml PROJECT_STATE.yaml and this work-item registry only after implementation evidence is green so V2L11.6 can be activated without implementing it
    out_of_scope:
      - changing legacy SparseReconstructionEstimate CameraCalibrationEstimate CameraPoseEstimate Point3DEstimate LocalScaleStatus or any canonical CameraSolution PointMap GeometrySolution contract
      - reverse V2-to-legacy conversion compatibility wrappers persistence migration database schema changes or making legacy estimated geometry a permanent V2 API
      - reading COLMAP native sparse-model files invoking PyCOLMAP modifying src/wre/reconstruction/colmap_import.py or implementing the V2 COLMAP adapter work owned by V2L12
      - physical CameraId inference calibration-sharing identity rig modeling rolling-shutter modeling or device identity claims
      - depth generation DepthField synthesis triangulation point generation fusion refinement bundle adjustment geometry optimization scale inference coordinate rescaling cross-frame alignment or metric upgrade
      - world Earth ECEF ENU geographic anchoring GPS GCP reference-fragment placement fragment creation fragment membership or fragment merging
      - translating legacy point tracks feature indices or reprojection errors into new canonical fields that do not exist; such source-only evidence must not be silently reinterpreted
      - SurfaceModel appearance material environment dynamic historical runtime compilation quality-policy route fallback retry adapter/model execution or new external dependencies
      - starting V2L11.6 or any V2L12 implementation in this work item
    depends_on: [V2L11.4]
    read_before:
      - docs/02_ARCHITECTURE.md
      - docs/03_PIPELINE.md
      - docs/04_DATA_MODEL.md
      - docs/19_COLMAP_RECONSTRUCTION_IMPORT.md
      - docs/23_ENGINEERING_EXECUTION.md
      - docs/24_SYSTEM_INVARIANTS.md
      - docs/25_V2_ROADMAP.md
      - docs/26_V2_PRODUCT_ARCHITECTURE_FREEZE.md
      - registry/v1-v2-migration.yaml
      - docs/reviews/L3-M1-colmap-baseline-review.md
      - src/wre/domain/estimated_geometry.py
      - src/wre/domain/camera_solutions.py
      - src/wre/domain/point_maps.py
      - src/wre/domain/geometry_solutions.py
      - src/wre/domain/cameras.py
      - src/wre/domain/metrics.py
      - src/wre/reconstruction/colmap_import.py
      - src/wre/reconstruction/__init__.py
      - tests/test_estimated_geometry.py
      - tests/test_camera_solutions.py
      - tests/test_point_maps.py
      - tests/test_geometry_solutions.py
      - tests/test_colmap_reconstruction_import.py
    reuse:
      - SparseReconstructionEstimate as immutable donor input only; its validated local frame camera poses calibration references scale status point ordering and observation membership may be consumed without mutating or extending the legacy types
      - CameraSolution as the canonical per-observation target preserving each legacy pose camera-from-local-frame transform and referenced calibration values without promoting calibration sharing into physical CameraId identity
      - PointMap as one canonical aggregate point carrier using the legacy estimate observation membership and legacy canonical point-id order while intentionally omitting unsupported per-point confidence
      - GeometrySolution as the canonical assembled hypothesis referencing the converted camera solutions and one converted PointMap in the same LocalFrameId with mapped scale semantics
      - empty MetricVector uncertainty-artifact tuples and absent PointMap confidence to represent no additional canonical measurement or uncertainty claim rather than fabricating metrics
      - standard-library SHA-256 only for deterministic target identities derived from explicit legacy identity inputs; no external dependency is required
    acceptance:
      - Add immutable LegacySparseGeometryConversionResult containing exactly source_estimate_id SparseReconstructionEstimateId camera_solutions tuple of CameraSolution point_map PointMap and geometry_solution GeometrySolution.
      - Add convert_sparse_reconstruction_estimate accepting exactly one SparseReconstructionEstimate and rejecting wrong input types without filesystem registry environment solver or artifact-store lookup.
      - The conversion is deterministic and source-immutable; repeated conversion of the same validated input yields equal target values and the source estimate child tuples objects and values are not rewritten or reordered by the converter.
      - Derive opaque target IDs deterministically with standard-library SHA-256 from explicit role-prefixed source identities so each CameraSolution identity depends on the source estimate identity plus its ObservationId while the PointMap and GeometrySolution identities depend on the source estimate identity and distinct role prefixes.
      - Emit exactly one CameraSolution per legacy CameraPoseEstimate and preserve the exact ObservationId LocalFrameId camera-from-local-frame rotation_matrix translation_xyz referenced calibration image dimensions and finite parameter tuple.
      - Convert the referenced legacy calibration projection_model to the canonical CameraProjectionModelName by lowercase normalization only; unsupported characters whitespace or otherwise invalid canonical tokens fail closed rather than being stripped replaced guessed or mapped through a vendor table.
      - Shared legacy calibration estimates may descriptively populate multiple per-observation CameraSolution values but must not create physical CameraId calibration-sharing rig or device identity claims.
      - Converted CameraSolution uncertainty_artifacts are empty and metrics are empty MetricVector values; legacy prior-focal flags provenance or other source-only metadata are not promoted into unrelated canonical fields.
      - Emit exactly one PointMap in the legacy LocalFrameId with source_observation_ids equal to the legacy estimate observation membership and positions_xyz in the legacy estimate canonical Point3DEstimate order; confidence is None and metrics are empty.
      - An estimate with zero legacy points still converts to an explicit empty PointMap and valid GeometrySolution rather than fabricating points or converting the outcome into a hidden failure.
      - Legacy point tracks feature indices reprojection errors and per-object DerivedArtifactProvenance remain source-only evidence and are not embedded into PointMap GeometrySolution or invented metrics because canonical contracts do not own those fields.
      - Map LocalScaleStatus.UNRESOLVED exactly to GeometryScaleStatus.UNRESOLVED and LocalScaleStatus.METRIC exactly to GeometryScaleStatus.METRIC without rescaling coordinates translations or point positions and without adding a scale factor.
      - Emit one GeometrySolution in the same LocalFrameId with canonical ascending converted CameraSolutionId references no depth-field references exactly one converted PointMapId and empty metrics.
      - camera_solutions in the conversion result are in canonical ascending CameraSolutionId order and geometry_solution.camera_solution_ids exactly matches that order.
      - The conversion performs no world Earth geographic placement fragment acceptance geometry optimization scale inference point fusion depth synthesis persistence serialization subprocess network or external solver execution.
    tests:
      - wrong input type rejection plus exact frozen LegacySparseGeometryConversionResult field shape
      - deterministic role-separated ID derivation across repeated conversion and distinct source estimate identities
      - representative uppercase legacy projection model normalization to the lowercase canonical token plus fail-closed invalid projection-model syntax
      - one CameraSolution per legacy pose with exact observation local-frame intrinsics dimensions and camera-from-local rotation translation preservation including shared-calibration reuse without CameraId inference
      - unresolved and metric scale-status mapping with exact unchanged camera translations and point coordinates proving conversion does not rescale or anchor geometry
      - populated-point conversion preserving legacy canonical point order source observation support absent confidence and empty metrics
      - zero-point conversion producing an empty PointMap and valid GeometrySolution without synthetic coordinates
      - GeometrySolution child-reference canonical ordering exact PointMap linkage no depth references and exact same local frame
      - source immutability regression proving conversion does not mutate legacy calibrations poses points tracks provenance or their ordering
      - regression proving legacy tracks feature indices reprojection error prior-focal flag provenance physical camera identity world placement surface solver persistence and generic metadata are not promoted into canonical outputs
      - module-surface regression proving no filesystem network subprocess database PyCOLMAP NumPy Torch Open3D or other external execution dependency is introduced
      - full fast CI and CodeQL plus native lanes only where repository path triggers require them
  - id: V2L11.6
    lot: V2L11
    title: Coordinate convention regression fixture and lot review
    status: done
    objective: Add one deterministic fast regression fixture that composes the accepted V2L11 camera point-map geometry and one-way legacy-conversion contracts around an asymmetric non-identity camera-from-local-frame transform so transpose inverse sign frame and scale regressions fail closed, then close the V2L11 lot review without introducing a coordinate-conversion engine or beginning the V2L12 classical adapter.
    allowed_scope:
      - tests/test_geometry_coordinate_conventions.py
      - docs/reviews/V2L11-canonical-geometry-contracts-review.md
      - focused corrections to V2L11-owned canonical geometry or legacy-conversion files only if the fixture demonstrates an existing V2L11 contract violation and without API or responsibility expansion
      - focused lifecycle and review updates to registry/components.yaml registry/reviews.yaml PROJECT_STATE.yaml and this work-item registry only after fixture evidence and the V2L11 lot review are green so V2L12.1 can be activated without implementing it
    out_of_scope:
      - a new coordinate transform alignment library generic SE3 math API or reusable world-frame conversion subsystem
      - changing accepted CameraSolution DepthField PointMap GeometrySolution or one-way legacy-conversion semantics unless required to fix a demonstrated V2L11 regression
      - native COLMAP artifact reading or execution V2L12 adapter descriptors feature matching verification mapper normalization or any V2L12 product implementation
      - alignment between different LocalFrameId values fragment creation fragment membership fragment merging or competing-geometry selection
      - world Earth ECEF ENU geographic GPS GCP reference-fragment anchoring absolute placement or CRS conversion
      - metric-scale inference coordinate rescaling bundle adjustment triangulation depth generation geometry refinement fusion or optimization
      - SurfaceModel mesh SDF collision navigation measurement appearance material environment dynamic historical or runtime representation work
      - persistence serialization database filesystem network model execution routing fallback or quality-policy execution
      - starting V2L12.1 implementation in this work item
    depends_on: [V2L11.5]
    read_before:
      - docs/02_ARCHITECTURE.md
      - docs/03_PIPELINE.md
      - docs/06_TESTING.md
      - docs/19_COLMAP_RECONSTRUCTION_IMPORT.md
      - docs/23_ENGINEERING_EXECUTION.md
      - docs/24_SYSTEM_INVARIANTS.md
      - docs/25_V2_ROADMAP.md
      - docs/26_V2_PRODUCT_ARCHITECTURE_FREEZE.md
      - registry/reviews.yaml
      - registry/v1-v2-migration.yaml
      - docs/reviews/L3-M1-colmap-baseline-review.md
      - src/wre/domain/camera_solutions.py
      - src/wre/domain/depth_fields.py
      - src/wre/domain/point_maps.py
      - src/wre/domain/geometry_solutions.py
      - src/wre/domain/estimated_geometry.py
      - src/wre/reconstruction/legacy_geometry_conversion.py
      - src/wre/reconstruction/colmap_import.py
      - tests/test_camera_solutions.py
      - tests/test_depth_fields.py
      - tests/test_point_maps.py
      - tests/test_geometry_solutions.py
      - tests/test_legacy_geometry_conversion.py
      - tests/test_colmap_reconstruction_import.py
    reuse:
      - CameraSolution camera-from-local-frame semantics exactly expressed as x_camera equals rotation_matrix multiplied by x_local plus translation_xyz
      - LocalFrameId as explicit local coordinate identity without metric Earth geographic or cross-frame compatibility implications
      - PointMap and GeometrySolution as canonical local-frame geometry values whose coordinates and child references must remain unchanged by convention checks
      - the V2L11.5 one-way legacy bridge as donor regression evidence that historical camera-from-local-frame poses and point coordinates reach the same canonical convention without inversion or rescaling
      - one asymmetric proper rotation nonzero translation and non-symmetric local points chosen as deterministic arithmetic evidence against accidental transpose inverse sign or axis assumptions
      - the fast deterministic fixture pattern and existing lot-review evidence process without adding a native external dependency
    acceptance:
      - Add one dedicated deterministic fast fixture using a non-identity proper rotation with nonzero translation and at least two non-symmetric local-frame points so identity transpose inverse sign-flip and accidental symmetric cases cannot pass trivially.
      - Construct a canonical CameraSolution and verify with explicit scalar arithmetic that a chosen local point satisfies x_camera equals rotation_matrix multiplied by x_local plus translation_xyz with the expected camera coordinates.
      - Prove representative incorrect transpose inverse or translation-sign interpretations do not equal the expected camera coordinates and are not accepted as alternative conventions.
      - Thread one exact LocalFrameId through CameraSolution PointMap and GeometrySolution and prove none of those contracts renames the local frame into world Earth geographic fragment or another implicit coordinate domain.
      - Prove PointMap positions remain exactly the supplied local coordinates in their supplied order and the fixture performs no hidden rotation translation sorting fusion or coordinate conversion on those points.
      - Exercise both GeometryScaleStatus.UNRESOLVED and GeometryScaleStatus.METRIC and prove changing only scale status does not alter camera extrinsics point coordinates frame identity or create any origin orientation Earth alignment or global-placement claim.
      - Build an equivalent retained legacy SparseReconstructionEstimate with the same asymmetric camera-from-local-frame transform and local points then run convert_sparse_reconstruction_estimate and prove the converted CameraSolution rotation translation PointMap coordinates LocalFrameId and scale mapping are preserved without inversion transpose sign change or rescaling.
      - Apply the same explicit local-point camera equation to the converted CameraSolution and verify it yields the same expected camera coordinates as the directly constructed canonical CameraSolution.
      - The regression fixture introduces no NumPy Torch Open3D PyCOLMAP filesystem database subprocess network solver execution coordinate-alignment engine or external dependency.
      - Add a V2L11 lot review documenting composition across V2L11.1 through V2L11.6 including canonical camera depth point and geometry boundaries local-scale semantics the one-way legacy bridge coordinate convention evidence and explicit remaining non-capabilities.
      - The V2L11 review must explicitly confirm that local frame does not imply metric scale or world placement unresolved scale remains valid physical surface is still absent and V2L12 solver normalization has not begun.
      - Only after the fixture and review evidence are green may registry/reviews.yaml mark V2L11 passed canonical_geometry_contracts become implemented PROJECT_STATE hand off to V2L12.1 and the V2L12.1 work item become ready without adding any V2L12 implementation.
    tests:
      - asymmetric direct CameraSolution camera-from-local-frame arithmetic with exact expected camera coordinates
      - regression proving transpose inverse translation-sign and identity-like alternative interpretations differ from the canonical result
      - exact LocalFrameId continuity across CameraSolution PointMap and GeometrySolution with no world Earth fragment or geographic claim
      - exact PointMap local-coordinate and ordering preservation with no hidden conversion
      - unresolved-versus-metric scale-status regression proving status does not mutate transforms coordinates frame identity or world semantics
      - equivalent legacy estimate conversion preserving rotation translation local points local frame and scale mapping
      - direct-versus-converted canonical camera equation equivalence on the same local point
      - module and fixture surface regression proving no external math solver native IO persistence or coordinate-alignment dependency is introduced
      - V2L11 lot review evidence covers V2L11.1 through V2L11.6 and the explicit handoff boundary to V2L12.1
      - full fast CI and CodeQL plus native lanes only where repository path triggers require them
  - id: V2L12.1
    lot: V2L12
    title: Current COLMAP adapter descriptor and V2 artifact normalization
    status: done
    objective: Define one pure solver-independent V2 descriptor for the current approved PyCOLMAP/COLMAP 4.2.0 precision-geometry baseline by reusing the existing AdapterCapabilityDescriptor ArtifactKind ArtifactRef producer and artifact-key contracts, with exact image-observation inputs and canonical V2 camera point-map and geometry output kinds, while retaining the legacy sparse-SfM registry entry as donor evidence and without importing or executing PyCOLMAP or beginning the feature matching verification or mapper work owned by later V2L12 items.
    allowed_scope:
      - src/wre/reconstruction/colmap_adapter.py
      - focused exports in src/wre/reconstruction/__init__.py
      - tests/test_colmap_adapter.py
      - focused registry/adapter-models.yaml addition for one distinct V2 precision COLMAP adapter entry while preserving the retained legacy colmap.sparse_sfm entry
      - focused lifecycle updates to registry/components.yaml PROJECT_STATE.yaml and this work-item registry only after implementation evidence is green so V2L12.2 can be activated without implementing it
    out_of_scope:
      - changing AdapterCapabilityDescriptor AdapterCapabilityName ArtifactKind ArtifactRef ArtifactKey ArtifactProducerIdentity or other accepted V2 core contracts
      - deleting renaming rewriting or treating the retained colmap.sparse_sfm registry entry and geometry.sparse_reconstruction_estimate output as the canonical V2 target
      - feature extraction pair matching geometric verification database migration cache integration or execution owned by V2L12.2
      - incremental global or hierarchical mapping bundle adjustment native sparse-model reading canonical CameraSolution PointMap or GeometrySolution materialization owned by V2L12.3
      - disconnected zero-model or multi-model outcome policy owned by V2L12.4
      - real end-to-end classical-route comparison fixture or V2L12 lot review owned by V2L12.5
      - importing PyCOLMAP inspecting the live COLMAP environment invoking a solver subprocess filesystem database network or artifact-store access from the descriptor module
      - changing the approved PyCOLMAP 4.2.0 COLMAP 4.2.0 dependency version license packaging distribution or integration mode
      - physical CameraId inference calibration sharing rig identity metric-scale inference coordinate rescaling world Earth ECEF ENU geographic anchoring GPS GCP alignment fragment placement or cross-local-frame compatibility
      - DepthField generation dense reconstruction fusion SurfaceModel mesh collision navigation appearance materials environment dynamic historical runtime routing fallback or quality-gate execution
      - starting V2L12.2 implementation in this work item
    depends_on: [V2L11.6]
    read_before:
      - docs/02_ARCHITECTURE.md
      - docs/03_PIPELINE.md
      - docs/14_TECHNOLOGY_SELECTION.md
      - docs/19_COLMAP_RECONSTRUCTION_IMPORT.md
      - docs/23_ENGINEERING_EXECUTION.md
      - docs/24_SYSTEM_INVARIANTS.md
      - docs/25_V2_ROADMAP.md
      - docs/26_V2_PRODUCT_ARCHITECTURE_FREEZE.md
      - docs/reviews/V2L11-canonical-geometry-contracts-review.md
      - registry/adapter-models.yaml
      - registry/dependencies.yaml
      - registry/v1-v2-migration.yaml
      - registry/schemas/adapter-model-registry.schema.json
      - src/wre/domain/adapter_capabilities.py
      - src/wre/domain/artifacts.py
      - src/wre/domain/artifact_keys.py
      - src/wre/domain/producer_identity.py
      - src/wre/reconstruction/colmap_environment.py
      - src/wre/reconstruction/colmap_features.py
      - src/wre/reconstruction/colmap_matching.py
      - src/wre/reconstruction/colmap_verification.py
      - src/wre/reconstruction/colmap_reconstruction.py
      - src/wre/reconstruction/colmap_import.py
      - tests/test_adapter_capabilities.py
      - tests/test_adapter_model_registry_schema.py
      - tests/test_adapter_model_registry_baselines.py
    reuse:
      - AdapterCapabilityDescriptor and AdapterCapabilityName as the existing solver-independent semantic adapter contract rather than defining a competing COLMAP-specific descriptor type
      - ArtifactKind and ArtifactRef as the V2 input/output artifact vocabulary and exact artifact-reference identity
      - ArtifactProducerIdentity ProducerRef ArtifactKeyMaterial and hardware-runtime key semantics as the existing reproducibility/cache identity boundary rather than inventing solver-private cache identity
      - the approved registry/dependencies.yaml colmap record and SUPPORTED_PYCOLMAP_VERSION/SUPPORTED_COLMAP_VERSION constants as evidence for the exact current 4.2.0 external environment without loading it during pure descriptor construction
      - the retained colmap.sparse_sfm registry entry as historical donor evidence only; V2L12.1 adds a distinct V2 precision-geometry entry and does not mutate donor semantics
      - canonical V2L11 CameraSolution PointMap and GeometrySolution artifact responsibilities as output semantics while leaving actual product materialization to V2L12.3
    acceptance:
      - Add a pure current-COLMAP V2 descriptor module whose import and construction require no PyCOLMAP installation live-environment inspection filesystem database network subprocess registry read or solver execution.
      - The descriptor uses the stable adapter identity colmap.precision_geometry and the open capability name geometry.precision_sfm.
      - The capability input kinds are exactly the immutable set containing image.observation.
      - The capability output kinds are exactly the immutable set containing geometry.camera_solution geometry.point_map and geometry.solution; geometry.sparse_reconstruction_estimate is not a V2 output kind.
      - Reuse the existing AdapterCapabilityDescriptor contract directly; do not wrap subclass alias or duplicate its validation semantics in a new generic adapter framework.
      - Expose the approved dependency identity declaratively as colmap with exact PyCOLMAP version 4.2.0 and COLMAP version COLMAP 4.2.0 without accepting floating latest alternate versions or silently probing the environment.
      - The V2 descriptor declares no model or checkpoint because the current classical baseline is an external solver/toolchain rather than a learned checkpoint.
      - Add one distinct registry adapter entry for colmap.precision_geometry whose capability input/output kinds version/dependency/model/checkpoint hardware-key policy shipping status and reproducibility notes agree with the pure module contract and approved dependency record.
      - Preserve the existing colmap.sparse_sfm registry entry semantically unchanged so legacy donor artifacts remain addressable and no migration-by-overwrite occurs.
      - If an input-normalization helper is exposed it accepts only a non-empty immutable tuple of unique canonically ordered ArtifactRef values whose ArtifactKind is exactly image.observation retains the exact tuple after validation and fails closed on mutable unordered duplicate foreign-kind or non-canonical inputs without sorting or coercion.
      - V2L12.1 does not create CameraSolution PointMap GeometrySolution values and does not read native COLMAP output; those execution and materialization responsibilities remain V2L12.3.
      - The descriptor module contains no feature matcher verifier mapper bundle-adjustment retry fallback routing quality scale anchoring surface appearance or persistence responsibility.
      - Existing legacy COLMAP feature matching verification reconstruction import and V2L11 geometry behavior remain unchanged.
    tests:
      - exact stable V2 adapter identity capability name and immutable input output ArtifactKind set regression
      - explicit regression proving geometry.sparse_reconstruction_estimate is absent from V2 output kinds
      - module import and descriptor construction without PyCOLMAP or live environment inspection
      - exact approved colmap dependency and 4.2.0 PyCOLMAP COLMAP version declaration with no model or checkpoint identity
      - registry-to-module parity for V2 adapter capability dependency version hardware-key policy shipping model checkpoint and reproducibility fields
      - regression proving the retained legacy colmap.sparse_sfm registry entry remains semantically unchanged and distinct from colmap.precision_geometry
      - focused input-normalization canonical tuple wrong-kind duplicate mutable and non-canonical-order tests only if the helper is part of the implementation
      - module-surface regression proving no filesystem network database subprocess PyCOLMAP import solver execution mapper feature matcher verifier or native-model parsing dependency is introduced
      - full fast CI and CodeQL plus native lanes only where repository path triggers require them
  - id: V2L12.2
    lot: V2L12
    title: Current COLMAP evidence stages use V2 artifact identities
    status: done
    objective: Put the retained deterministic COLMAP feature extraction raw pair matching and geometric verification donor stages behind explicit V2 evidence-artifact planning and publication contracts so every stage has solver-independent ArtifactKind input/output semantics canonical content-addressed ArtifactKey material exact producer/configuration/hardware identity and verifiable materialization metadata while preserving the existing PyCOLMAP 4.2.0 donor algorithms and deferring mapper geometry emission to V2L12.3.
    allowed_scope:
      - src/wre/reconstruction/colmap_evidence_artifacts.py
      - focused exports in src/wre/reconstruction/__init__.py
      - focused compatibility hooks in src/wre/reconstruction/colmap_features.py src/wre/reconstruction/colmap_matching.py and src/wre/reconstruction/colmap_verification.py only when required to expose already-computed canonical configuration or result evidence without changing solver behavior
      - tests/test_colmap_evidence_artifacts.py
      - focused registry/adapter-models.yaml additions for current COLMAP feature matching and geometric-verification evidence capabilities
      - focused lifecycle updates to registry/components.yaml PROJECT_STATE.yaml and this work-item registry only after implementation evidence is green so V2L12.3 can be activated without implementing it
    out_of_scope:
      - changing V2L12.1 colmap.precision_geometry capability output semantics or promoting it from experimental before the later route regression closes
      - changing existing V2 ArtifactRef ArtifactKind ArtifactKey ArtifactInputFingerprint ArtifactMetadata ArtifactMaterializationMetadata ArtifactProducerIdentity HardwareRuntimeIdentity or persistence/cache semantics
      - replacing or deleting the retained ColmapFeatureExtractionRequest/Result ColmapPairMatchingRequest/Result or ColmapGeometricVerificationRequest/Result donor contracts
      - changing SIFT extraction exhaustive matching geometric-verification thresholds random seeds CPU determinism PyCOLMAP calls COLMAP database schema or current safety/integrity checks
      - introducing a new generic adapter cache framework CorrespondenceSet domain model or solver-private object into core
      - automatic cache lookup persistence mutation invalidation execution scheduling retry fallback or resume behavior; V2L2 generic cache services remain separate
      - incremental global hierarchical mapping bundle adjustment native sparse-model import canonical CameraSolution PointMap GeometrySolution materialization or local-frame creation owned by V2L12.3
      - disconnected zero-model or multi-model outcome policy owned by V2L12.4
      - end-to-end route comparison fixture V2L12 lot review or shipping promotion owned by V2L12.5
      - metric-scale solving rescaling world Earth ECEF ENU geographic GPS GCP anchoring fragment alignment SurfaceModel dense MVS appearance dynamic historical or runtime work
      - starting V2L12.3 implementation in this work item
    depends_on: [V2L12.1]
    read_before:
      - docs/02_ARCHITECTURE.md
      - docs/03_PIPELINE.md
      - docs/14_TECHNOLOGY_SELECTION.md
      - docs/23_ENGINEERING_EXECUTION.md
      - docs/24_SYSTEM_INVARIANTS.md
      - docs/25_V2_ROADMAP.md
      - docs/26_V2_PRODUCT_ARCHITECTURE_FREEZE.md
      - docs/reviews/V2L2-artifact-dag-cache-review.md
      - docs/reviews/V2L11-canonical-geometry-contracts-review.md
      - registry/adapter-models.yaml
      - registry/dependencies.yaml
      - src/wre/domain/artifacts.py
      - src/wre/domain/artifact_keys.py
      - src/wre/domain/artifact_metadata.py
      - src/wre/domain/artifact_materialization.py
      - src/wre/domain/hardware_identity.py
      - src/wre/domain/producer_identity.py
      - src/wre/domain/provenance.py
      - src/wre/reconstruction/colmap_adapter.py
      - src/wre/reconstruction/colmap_environment.py
      - src/wre/reconstruction/colmap_features.py
      - src/wre/reconstruction/colmap_matching.py
      - src/wre/reconstruction/colmap_verification.py
      - tests/test_artifact_keys.py
      - tests/test_artifact_metadata.py
      - tests/test_artifact_materialization.py
      - tests/test_colmap_features.py
      - tests/test_colmap_matching.py
      - tests/test_colmap_geometric_verification.py
    reuse:
      - ArtifactKind and ArtifactRef as solver-independent evidence identities rather than COLMAP database table names or Python result classes
      - ArtifactInputFingerprint ArtifactKeyMaterial and derive_artifact_key as the only canonical computation-cache identity path with caller-supplied ordered input fingerprints
      - ArtifactProducerIdentity plus exact existing ColmapFeatureExtractionConfig ColmapPairMatchingConfig and ColmapGeometricVerificationConfig SHA-256 values as stage configuration identity
      - explicit HardwareRuntimeIdentity in key material because the current COLMAP adapter registry policy is hardware-sensitive
      - ArtifactMetadata with OBSERVED_RECONSTRUCTED provenance and ArtifactMaterializationMetadata built from exact already-computed result database SHA-256 and byte length rather than rehashing or reading files in the pure publication layer
      - existing donor producer identities pycolmap.extract_features pycolmap.match_exhaustive and pycolmap.geometric_verification with exact PyCOLMAP 4.2.0 version semantics
      - existing result provenance and database-integrity checks as donor evidence while V2 identities become the production-facing cache boundary
    acceptance:
      - 'Add one pure evidence-artifact module defining exactly three solver-independent output ArtifactKind values: evidence.local_features evidence.pair_matches and evidence.geometric_verification.'
      - Add immutable stage descriptors for current COLMAP feature extraction pair matching and geometric verification using existing AdapterCapabilityDescriptor rather than a new adapter framework.
      - Register distinct experimental adapter entries colmap.local_features colmap.pair_matching and colmap.geometric_verification with exact approved colmap dependency reference no model/checkpoint required hardware-key policy current producer identity and reproducibility notes; do not alter the retained colmap.sparse_sfm donor entry.
      - Feature-stage artifact planning accepts only a non-empty canonically ordered immutable tuple of image.observation ArtifactInputFingerprint values plus the exact feature configuration identity and explicit HardwareRuntimeIdentity.
      - Matching-stage artifact planning consumes exactly one evidence.local_features ArtifactInputFingerprint and the exact matching configuration identity plus explicit HardwareRuntimeIdentity.
      - Verification-stage artifact planning consumes exactly one evidence.pair_matches ArtifactInputFingerprint and the exact verification configuration identity plus explicit HardwareRuntimeIdentity.
      - Every stage derives ArtifactKey only through ArtifactKeyMaterial and derive_artifact_key; no filename timestamp path database row ID ReconstructionRunId or mutable object identity participates in the cache key.
      - Producer identity contains exact stage implementation and PyCOLMAP 4.2.0 version with null model/checkpoint; configuration identity uses the existing canonical config SHA-256 without reserializing a second competing config format.
      - Input fingerprints must already be canonical and fail closed on mutable collections duplicates wrong ArtifactKind members or non-canonical feature-input ordering; the module does not silently sort or coerce them.
      - Publication helpers accept a caller-supplied output ArtifactRef of the exact stage output kind a SceneProjectId the derived ArtifactKey/producer identity and the already-computed donor result, then emit ArtifactMetadata plus ArtifactMaterializationMetadata whose one database entry preserves the result SHA-256 and byte length exactly.
      - Publication does not read hash or mutate the result database path; relative materialization path is explicit canonical POSIX caller input and the generic ArtifactMaterializationMetadata validator remains authoritative.
      - Stage publication retains OBSERVED_RECONSTRUCTED provenance and does not promote local matches or two-view geometry into accepted scene truth CameraSolution GeometrySolution SurfaceModel or world placement.
      - Existing donor extraction matching verification execution APIs remain usable and semantically unchanged; V2 wrappers may compose them but must not alter current solver calls options safety checks or result ordering.
      - The evidence-artifact module imports no PyCOLMAP sqlite3 subprocess socket requests persistence store router quality or mapper execution dependency.
      - No mapper or bundle-adjustment execution canonical camera/geometry output native sparse-model reading local-frame creation or V2L12.3 behavior is added.
    tests:
      - exact three evidence ArtifactKind and three AdapterCapabilityDescriptor input/output semantics
      - exact producer implementation PyCOLMAP 4.2.0 configuration SHA and explicit HardwareRuntimeIdentity participation in deterministic ArtifactKey golden/regression cases
      - feature-input canonical ordering duplicate mutable wrong-kind and empty fail-closed cases
      - matching and verification exact single-parent-kind fail-closed cases
      - key regression proving configuration input digest producer or hardware identity changes invalidate only the corresponding stage key and filenames paths run IDs do not enter key material
      - publication regression for feature matching and verification donor results preserving exact result database SHA-256 byte length output ArtifactRef project identity producer key and OBSERVED_RECONSTRUCTED provenance
      - canonical relative materialization-path rejection delegated to ArtifactMaterializationMetadata with no hidden path normalization
      - registry-to-module parity for all three new stage entries and regression proving colmap.sparse_sfm remains unchanged
      - regression proving retained donor execution request/result APIs and deterministic configuration digests remain unchanged
      - module-surface regression proving no PyCOLMAP sqlite persistence router quality mapper or solver execution dependency is introduced in the pure evidence-artifact layer
      - full fast CI CodeQL native COLMAP integration and any registry-triggered compatibility lanes
  - id: V2L12.3
    lot: V2L12
    title: COLMAP incremental and global mappers emit canonical geometry
    status: done
    objective: Implement explicit pinned PyCOLMAP 4.2.0 incremental and global precision-mapper boundaries whose audited native sparse models are normalized directly into canonical V2 CameraSolution PointMap and GeometrySolution values one source model at a time in deterministic explicit LocalFrameId domains with unresolved local scale while preserving the existing incremental donor behavior keeping global-mapper source evidence immutable and avoiding any dependency on SparseReconstructionEstimate as the V2 production target.
    allowed_scope:
      - src/wre/reconstruction/colmap_canonical_geometry.py
      - src/wre/reconstruction/colmap_global_reconstruction.py
      - focused exports in src/wre/reconstruction/__init__.py
      - focused reuse hooks in src/wre/reconstruction/colmap_reconstruction.py and src/wre/reconstruction/colmap_import.py only when needed to share audited native-model identity or file-verification logic without changing retained donor semantics
      - tests/test_colmap_canonical_geometry.py
      - tests/test_colmap_global_reconstruction.py
      - .github/workflows/colmap-integration.yml only to include the two V2L12.3 test files in the existing exact PyCOLMAP 4.2.0 native lane so required real global-mapping acceptance evidence actually executes
      - focused registry/adapter-models.yaml additions for distinct current incremental and global executable precision-geometry adapter entries while preserving colmap.precision_geometry and colmap.sparse_sfm
      - focused lifecycle updates to registry/components.yaml PROJECT_STATE.yaml and this work-item registry only after implementation evidence is green so V2L12.4 can be activated without implementing it
    out_of_scope:
      - deleting rewriting or changing the retained ColmapIncrementalReconstructionRequest ColmapIncrementalReconstructionResult ColmapReconstructionImportRequest ColmapReconstructionImportResult or legacy sparse-reconstruction contracts except bounded sharing refactors proven semantically identical
      - using SparseReconstructionEstimate convert_sparse_reconstruction_estimate or legacy CameraCalibrationEstimate CameraPoseEstimate Point3DEstimate values as the V2 production output path
      - changing canonical CameraSolution PointMap GeometrySolution LocalFrameId MetricVector ArtifactKind ArtifactKey or provenance contracts
      - feature extraction pair matching or geometric verification semantics already closed by V2L12.2
      - selecting merging ranking aligning or reconciling multiple disconnected mapper models or defining the zero-model route outcome owned by V2L12.4
      - end-to-end route comparison shipping promotion or V2L12 lot review owned by V2L12.5
      - hierarchical mapping large-collection routing learned geometry hybrid consensus MVS dense depth fusion SurfaceModel mesh collision navigation appearance dynamic historical or runtime work
      - inferring physical CameraId calibration ownership rig identity rolling-shutter semantics metric scale scale factors world Earth ECEF ENU geographic GPS GCP anchoring fragment placement or cross-local-frame compatibility
      - silently enabling GPU Caspar or another accelerated bundle-adjustment backend under the reference incremental or global adapter identity
      - automatic cache lookup persistence mutation scheduling fallback retry quality-gate execution or generalized mapper orchestration
      - starting V2L12.4 implementation in this work item
    depends_on: [V2L12.2]
    read_before:
      - docs/02_ARCHITECTURE.md
      - docs/03_PIPELINE.md
      - docs/14_TECHNOLOGY_SELECTION.md
      - docs/19_COLMAP_RECONSTRUCTION_IMPORT.md
      - docs/23_ENGINEERING_EXECUTION.md
      - docs/24_SYSTEM_INVARIANTS.md
      - docs/25_V2_ROADMAP.md
      - docs/26_V2_PRODUCT_ARCHITECTURE_FREEZE.md
      - docs/reviews/V2L11-canonical-geometry-contracts-review.md
      - registry/adapter-models.yaml
      - registry/dependencies.yaml
      - src/wre/domain/camera_solutions.py
      - src/wre/domain/point_maps.py
      - src/wre/domain/geometry_solutions.py
      - src/wre/domain/fragments.py
      - src/wre/domain/metrics.py
      - src/wre/domain/observations.py
      - src/wre/reconstruction/colmap_adapter.py
      - src/wre/reconstruction/colmap_evidence_artifacts.py
      - src/wre/reconstruction/colmap_environment.py
      - src/wre/reconstruction/colmap_features.py
      - src/wre/reconstruction/colmap_verification.py
      - src/wre/reconstruction/colmap_reconstruction.py
      - src/wre/reconstruction/colmap_import.py
      - src/wre/reconstruction/legacy_geometry_conversion.py
      - tests/test_colmap_incremental_reconstruction.py
      - tests/test_colmap_reconstruction_import.py
      - tests/test_colmap_track_normalization.py
      - tests/test_geometry_coordinate_conventions.py
    reuse:
      - the exact approved PyCOLMAP 4.2.0 environment and existing deterministic incremental-mapping donor as the retained classical reference rather than replacing its solver behavior
      - PyCOLMAP 4.2.0 global_mapping and GlobalPipelineOptions as a distinct current global SfM specialist with a separate adapter identity and explicit deterministic CPU reference profile
      - ColmapSparseModelArtifact and its audited canonical native-file manifest as solver-private evidence that may be read only after exact membership hash and byte-length verification
      - the existing feature-stage image-name to ObservationId mapping as the source of registered-view observation identity without inferring physical CameraId
      - V2L11 CameraSolution PointMap GeometrySolution GeometryScaleStatus and camera-from-local-frame coordinate convention as the only canonical geometry target
      - the retained legacy COLMAP importer and V2L11 legacy bridge only as regression evidence for equivalent source models not as an intermediate production representation
      - empty MetricVector values where no canonical metric is explicitly owned rather than inventing confidence or repackaging donor-only track and reprojection metadata
      - explicit hardware-sensitive adapter identity so any future accelerated BA profile cannot alias the deterministic CPU reference key or capability
    acceptance:
      - Add a direct native-COLMAP-to-canonical-V2 normalization boundary that never constructs SparseReconstructionEstimate CameraCalibrationEstimate CameraPoseEstimate Point3DEstimate or another legacy geometry value.
      - Normalize exactly one audited ColmapSparseModelArtifact at a time and return one immutable adapter-local aggregate containing its source model index and identity plus canonical CameraSolution values one canonical PointMap and one canonical GeometrySolution.
      - Derive one stable LocalFrameId from the audited source-model content identity and retain that exact object across every CameraSolution PointMap and GeometrySolution emitted for the model.
      - Every registered COLMAP image maps to exactly one known ObservationId and emits exactly one CameraSolution using image.cam_from_world as camera-from-local-frame without inversion transpose translation sign change rescaling or world Earth renaming.
      - CameraSolution image dimensions and intrinsic parameter sequence come from the registered COLMAP camera values as finite exact floats and the projection-model token is lowercased only before canonical token validation with no vendor alias table or physical CameraId inference.
      - PointMap source observations are the canonical registered-model observation membership and positions are the native 3D point coordinates in deterministic source point-ID order with no dedup fusion rescaling topology inference or synthetic confidence.
      - GeometrySolution uses GeometryScaleStatus.UNRESOLVED references the exact emitted CameraSolution identifiers and PointMap identifier contains no depth fields and carries no metric world geographic or fragment claim.
      - Native model directories are read only after exact audited relative-file membership SHA-256 and byte-length verification; symlinks changed bytes missing files extra files malformed camera values malformed poses unknown image names and unsupported canonical projection syntax fail closed.
      - Preserve the established non-identity camera-from-local-frame regression and add a direct-native canonicalization regression that agrees with the retained legacy donor plus V2L11 bridge on camera rotations translations local points observation membership and unresolved scale without requiring production use of that bridge.
      - Add a deterministic current incremental V2 adapter entry distinct from colmap.precision_geometry and colmap.sparse_sfm whose semantic inputs are image.observation plus evidence.geometric_verification and whose outputs are geometry.camera_solution geometry.point_map and geometry.solution.
      - Add a deterministic current global V2 adapter entry using the exact pinned PyCOLMAP 4.2.0 global_mapping capability with the same canonical output kinds and a distinct adapter and capability identity from the incremental route.
      - The global reference route must never mutate the caller verification database. Any view-graph calibration or global-mapper database mutation occurs only on a fresh verified workspace copy whose source SHA-256 and byte length match the supplied verification result before copying.
      - The global reference configuration uses explicit deterministic random-seed and thread settings and disables exposed GPU acceleration for positioning and bundle adjustment. Any accelerated or Caspar profile must use a different explicit configuration adapter or hardware identity and is not silently selected.
      - Global and incremental native output models use the same audited ColmapSparseModelArtifact file-manifest boundary before canonical geometry normalization so mapper choice cannot alter core geometry semantics.
      - Multiple returned models may each be normalized independently but this item does not choose merge rank align discard or reinterpret components and does not define the no-model outcome. Those route-level outcomes remain V2L12.4.
      - Existing incremental donor execution import safety tests and legacy donor semantics remain unchanged except for explicitly shared helper refactors whose pre-existing regressions prove identical behavior.
      - No mapper output is promoted to physical SurfaceModel accepted world placement or metric geometry merely because canonical values were constructed.
    tests:
      - exact canonical aggregate field and immutability regression for one native model
      - exact camera count observation mapping dimensions intrinsic sequence projection token and camera-from-local-frame rotation translation regression
      - exact deterministic native point-ID ordering PointMap source membership empty-confidence and unresolved GeometrySolution assembly regression
      - stable source-model content identity and LocalFrameId regression with changed native file digest producing a different identity rather than a hidden alias
      - fail-closed native manifest membership hash byte-length symlink unknown image malformed camera malformed pose and unsupported projection cases
      - direct native canonicalization versus retained legacy importer plus V2L11 bridge equivalence fixture for camera pose local points membership and unresolved scale
      - existing incremental donor suite remains green with no changed solver calls options ordering or safety semantics
      - exact PyCOLMAP 4.2.0 global_mapping native-lane fixture with deterministic CPU configuration and audited model files
      - global workspace-copy regression proving source verification database bytes remain unchanged even when the global preparation path calibrates or mutates its private copy
      - registry parity proving distinct experimental incremental and global executable mapper entries exact approved colmap dependency null model checkpoint required hardware policy and canonical V2 output kinds
      - regression proving colmap.precision_geometry and retained colmap.sparse_sfm entries remain semantically unchanged
      - module-surface regression proving canonicalization owns no routing fallback quality scale anchoring surface appearance dynamic or persistence responsibility
      - full fast CI CodeQL native PyCOLMAP integration and any registry-triggered compatibility lanes
  - id: V2L12.4
    lot: V2L12
    title: Disconnected and zero model explicit outcomes
    status: done
    objective: Represent the outcome of either current COLMAP precision mapper explicitly after V2L12.3 so zero native models remain a truthful no-geometry outcome one audited model remains one canonical local-frame solution and multiple audited models remain an ordered set of disconnected independent canonical local-frame solutions without selecting ranking merging aligning discarding retrying or inventing geometry.
    allowed_scope:
      - src/wre/reconstruction/colmap_geometry_outcomes.py
      - focused exports in src/wre/reconstruction/__init__.py
      - tests/test_colmap_geometry_outcomes.py
      - focused lifecycle updates to registry/components.yaml PROJECT_STATE.yaml and this work-item registry only after implementation evidence is green so V2L12.5 can be activated without implementing it
    out_of_scope:
      - changing PyCOLMAP incremental_mapping global_mapping view-graph calibration deterministic CPU options native-model auditing or direct native-to-canonical normalization implemented by V2L12.3
      - changing retained legacy sparse reconstruction import donor contracts or V2L11 canonical CameraSolution PointMap GeometrySolution LocalFrameId GeometryScaleStatus contracts
      - choosing a preferred mapper or preferred model ranking scoring filtering discarding merging aligning registering rescaling or reconciling disconnected models
      - converting a no-model result into fallback cameras points empty GeometrySolution generated geometry or another plausible-looking success
      - retry fallback escalation quality-gate router scheduling cache persistence benchmark or default-promotion policy
      - metric-scale solving world Earth ECEF ENU geographic GPS GCP anchoring fragment placement cross-local-frame compatibility SurfaceModel MVS dense depth appearance dynamic historical or runtime work
      - end-to-end current-route comparison shipping promotion V2L12 lot review or any V2L12.5 implementation
      - starting V2L12.5 implementation in this work item
    depends_on: [V2L12.3]
    read_before:
      - docs/02_ARCHITECTURE.md
      - docs/03_PIPELINE.md
      - docs/23_ENGINEERING_EXECUTION.md
      - docs/24_SYSTEM_INVARIANTS.md
      - docs/25_V2_ROADMAP.md
      - docs/26_V2_PRODUCT_ARCHITECTURE_FREEZE.md
      - docs/reviews/V2L11-canonical-geometry-contracts-review.md
      - registry/adapter-models.yaml
      - src/wre/domain/geometry_solutions.py
      - src/wre/domain/fragments.py
      - src/wre/reconstruction/colmap_canonical_geometry.py
      - src/wre/reconstruction/colmap_global_reconstruction.py
      - src/wre/reconstruction/colmap_reconstruction.py
      - tests/test_colmap_canonical_geometry.py
      - tests/test_colmap_global_reconstruction.py
      - tests/test_colmap_incremental_reconstruction.py
    reuse:
      - CanonicalColmapSparseModel as the exact per-model canonical V2 payload already established by V2L12.3 rather than introducing another geometry representation
      - ColmapSparseModelArtifact model index and audited source-model content identity as the source evidence that must correspond exactly to each canonical model
      - the two distinct current executable adapter identities colmap.incremental_precision_geometry and colmap.global_precision_geometry as mapper provenance without introducing a generalized mapper registry or route selector
      - positive-domain immutable outcome states for no model one model and disconnected models rather than inferring route truth from bool has_reconstruction alone
      - canonical mapper model-index order from the audited V2L12.3 results with no hidden sorting ranking or coercion at the outcome boundary
    acceptance:
      - Add one immutable adapter-local COLMAP geometry outcome contract with exactly three semantic states representing no native model exactly one independent model and multiple disconnected independent models.
      - The outcome records which current executable precision mapper identity produced the source result and accepts only colmap.incremental_precision_geometry or colmap.global_precision_geometry without guessing or collapsing those identities.
      - The no-model state contains no CanonicalColmapSparseModel CameraSolution PointMap or GeometrySolution value and does not manufacture placeholder empty or generated geometry.
      - The single-model state contains exactly one CanonicalColmapSparseModel and preserves its source model index source content identity LocalFrameId cameras PointMap GeometrySolution and unresolved scale unchanged.
      - The disconnected-model state contains at least two CanonicalColmapSparseModel values in the exact audited source model-index order and preserves every distinct LocalFrameId and source content identity without merge alignment selection ranking filtering or cross-frame compatibility claims.
      - Outcome construction cross-checks the supplied audited ColmapSparseModelArtifact tuple against canonical models by exact count source model index and source-model content identity so missing duplicated reordered foreign or mismatched canonical models fail closed rather than being repaired silently.
      - The audited native model tuple must already be immutable unique and canonically ordered exactly as emitted by the V2L12.3 mapper boundary; mutable collections duplicate indices or non-canonical order fail closed without sorting or coercion.
      - The outcome state is derived only from the exact audited model count after all structural validation; callers cannot label zero one or multiple models with a contradictory state.
      - Existing incremental and global mapper execution canonicalization donor and registry semantics remain unchanged and this item performs no PyCOLMAP native model filesystem database or solver execution.
      - No quality decision retry fallback router preferred-solution policy metric/world placement SurfaceModel or V2L12.5 comparison behavior is introduced.
    tests:
      - exact immutable outcome-state vocabulary and field-shape regression
      - incremental and global source adapter identity acceptance plus unknown or legacy adapter identity rejection
      - zero-model outcome regression proving no canonical geometry payload or placeholder is created
      - single-model outcome regression preserving the exact CanonicalColmapSparseModel object and all child local-frame unresolved-scale semantics
      - disconnected two-or-more model regression preserving exact source order independent LocalFrameId identities and canonical payload objects without ranking merge alignment or filtering
      - mutable duplicate non-canonical audited model tuple rejection without sorting or coercion
      - canonical model count index order source-content-identity duplicate missing extra and mismatch fail-closed regressions
      - contradictory manually constructed outcome state rejection so state always agrees with validated model cardinality
      - module-surface regression proving no PyCOLMAP filesystem database legacy geometry routing quality surface appearance dynamic persistence or V2L12.5 dependency enters the pure outcome layer
      - full fast CI CodeQL native COLMAP integration and any registry-triggered compatibility lanes
  - id: V2L12.5
    lot: V2L12
    title: COLMAP current route regression comparison and lot review
    status: done
    objective: Add one deterministic real PyCOLMAP 4.2.0 regression and comparison fixture that exercises the retained incremental donor and the current incremental and global V2 precision geometry paths over the same controlled verified source model evidence, proves canonical geometry and explicit outcome semantics without selecting a winner, records an evidence-based shipping-status decision for the V2L12 adapters, and closes the V2L12 lot review without implementing feed-forward geometry or competing-solution policy.
    allowed_scope:
      - tests/test_colmap_precision_route_regression.py
      - .github/workflows/colmap-integration.yml only to execute the V2L12.5 regression in the existing exact PyCOLMAP 4.2.0 native lane
      - docs/reviews/V2L12-classical-precision-geometry-review.md
      - registry/adapter-models.yaml only for evidence-backed shipping-status and reproducibility-note updates to V2L12-owned COLMAP adapters without changing their capability input output producer dependency model checkpoint or hardware-key semantics
      - focused corrections to V2L12-owned reconstruction files only if the comparison fixture demonstrates an existing V2L12 contract violation and without adding a new solver route or responsibility
      - focused lifecycle and review updates to registry/components.yaml registry/reviews.yaml PROJECT_STATE.yaml and this work-item registry only after fixture and lot-review evidence are green so V2L13.1 can be activated without implementing it
    out_of_scope:
      - introducing another classical solver mapper matching method bundle-adjustment backend learned model checkpoint or external dependency
      - hierarchical mapping large-collection submaps retrieval routing fallback retry scheduler cache executor or generalized mapper orchestration
      - ranking incremental versus global as an overall winner selecting a default route from dataset characteristics or defining route-choice heuristics
      - comparing or fusing independent disconnected models across LocalFrameId values aligning frames resolving scale merging points or choosing one component
      - changing CameraSolution PointMap GeometrySolution ColmapGeometryOutcome artifact identity provenance coordinate convention or local-scale contracts
      - metric-scale solving rescaling world Earth ECEF ENU geographic GPS GCP anchoring fragment placement SurfaceModel MVS dense depth appearance dynamic historical or runtime work
      - feed-forward geometry adapter candidate integration preview comparison or any V2L13 implementation
      - precision global refinement hybrid solver comparison consensus policy or any V2L14 implementation
      - claiming that a shipping-status promotion makes one route globally preferred or that COLMAP remains the permanent best algorithm
      - starting V2L13.1 implementation in this work item
    depends_on: [V2L12.4]
    read_before:
      - docs/02_ARCHITECTURE.md
      - docs/03_PIPELINE.md
      - docs/06_TESTING.md
      - docs/14_TECHNOLOGY_SELECTION.md
      - docs/19_COLMAP_RECONSTRUCTION_IMPORT.md
      - docs/23_ENGINEERING_EXECUTION.md
      - docs/24_SYSTEM_INVARIANTS.md
      - docs/25_V2_ROADMAP.md
      - docs/26_V2_PRODUCT_ARCHITECTURE_FREEZE.md
      - docs/reviews/L3-M1-colmap-baseline-review.md
      - docs/reviews/V2L11-canonical-geometry-contracts-review.md
      - registry/adapter-models.yaml
      - registry/dependencies.yaml
      - registry/reviews.yaml
      - src/wre/reconstruction/colmap_features.py
      - src/wre/reconstruction/colmap_matching.py
      - src/wre/reconstruction/colmap_verification.py
      - src/wre/reconstruction/colmap_reconstruction.py
      - src/wre/reconstruction/colmap_import.py
      - src/wre/reconstruction/legacy_geometry_conversion.py
      - src/wre/reconstruction/colmap_canonical_geometry.py
      - src/wre/reconstruction/colmap_global_reconstruction.py
      - src/wre/reconstruction/colmap_geometry_outcomes.py
      - tests/test_colmap_incremental_reconstruction.py
      - tests/test_colmap_reconstruction_import.py
      - tests/test_colmap_canonical_geometry.py
      - tests/test_colmap_global_reconstruction.py
      - tests/test_colmap_geometry_outcomes.py
    reuse:
      - exact external PyCOLMAP 4.2.0 and the existing native COLMAP integration lane rather than adding another solver or environment
      - one controlled PyCOLMAP synthetic verified database and corresponding deterministic image observations as shared source evidence for retained and current mapper paths
      - reconstruct_colmap_incrementally as the retained deterministic incremental execution donor and ColmapReconstructionImportResult plus convert_sparse_reconstruction_estimate only as historical regression evidence
      - canonicalize_colmap_sparse_model as the direct current native-to-V2 normalization path for both incremental and global native models
      - reconstruct_colmap_globally as the distinct current global specialist using its verified private database copy
      - ColmapGeometryOutcome as the only V2L12 route-result cardinality boundary for no model one model and disconnected models
      - existing adapter registry shipping_status values as maturity metadata only and not route selection or benchmark ranking
      - the established lot-review document and registry process used by V2L11 without creating a new review framework
    acceptance:
      - Add one dedicated exact PyCOLMAP 4.2.0 native regression fixture using the same controlled source evidence to exercise the retained incremental donor path the direct current incremental V2 path and the current global V2 path.
      - The fixture must verify source verification database immutability for both mapper executions and exact audited ColmapSparseModelArtifact file manifests before canonical normalization.
      - For at least one retained incremental source model compare direct native canonicalization with the retained importer plus V2L11 bridge and prove equal registered observation membership camera rotation translation projection intrinsics local point coordinates and unresolved scale semantics without requiring identical derived opaque target IDs.
      - Wrap current incremental and global canonical model tuples in ColmapGeometryOutcome and prove state is derived from exact model cardinality with no placeholder geometry merge rank alignment or hidden selection.
      - The fixture may report structural comparison facts such as model count registered observation count point count and per-model membership but must not compute an overall winner score default-route recommendation or cross-local-frame geometric error where frames are not explicitly aligned.
      - The global and incremental current routes retain distinct adapter identities and every canonical model remains in its own deterministic LocalFrameId with GeometryScaleStatus.UNRESOLVED unless an existing accepted contract explicitly proves metric scale.
      - Existing feature matching verification evidence stages remain separately covered by their native tests and the lot review must state that the route-comparison fixture begins from verified source evidence rather than pretending a synthetic database is an image-feature-quality benchmark.
      - Record one explicit shipping-status decision for colmap.precision_geometry colmap.incremental_precision_geometry colmap.global_precision_geometry colmap.local_features colmap.pair_matching and colmap.geometric_verification based on the complete V2L12 evidence. A promotion may only change experimental to approved and must be justified per adapter; leaving an adapter experimental is valid and must have an explicit limitation.
      - No shipping-status decision may alter capability producer dependency model checkpoint hardware identity failure policy or imply route preference.
      - Add a V2L12 lot review documenting composition across V2L12.1 through V2L12.5 including V2 artifact identities direct canonical geometry incremental and global execution zero single disconnected outcome semantics comparison evidence shipping decisions and explicit remaining non-capabilities.
      - The V2L12 review must explicitly confirm that COLMAP is the current classical precision baseline rather than the permanent whole-engine algorithm; feed-forward geometry hybrid global refinement dense depth physical surface appearance dynamic chronology and runtime remain later work.
      - Only after the native fixture and review evidence are green may registry/reviews.yaml mark V2L12 passed precision_geometry_baseline become implemented PROJECT_STATE hand off to V2L13.1 and the V2L13.1 work item become ready without adding any V2L13 implementation.
    tests:
      - exact shared-source retained incremental direct-incremental and current-global native execution regression
      - retained importer plus V2L11 bridge versus direct native canonical incremental equivalence on pose intrinsics local points membership and unresolved scale
      - exact parent verification database byte-identity preservation across mapper executions
      - exact audited native model manifest and canonical source-model identity checks
      - explicit ColmapGeometryOutcome state and model-order preservation for current incremental and global results
      - regression proving comparison records no winner default route cross-frame alignment metric upgrade surface or later-layer claim
      - adapter registry shipping-status decision regression with unchanged capability producer dependency model checkpoint and hardware-policy semantics
      - V2L12 lot review evidence covers V2L12.1 through V2L12.5 and the explicit handoff boundary to V2L13.1
      - full fast CI CodeQL exact PyCOLMAP 4.2.0 native integration and any registry-triggered compatibility lanes
  - id: V2L13.1
    lot: V2L13
    title: Feed forward geometry adapter interface
    status: done
    objective: Define the pure solver-independent V2 feed-forward geometry adapter boundary over canonical decoded image artifacts and canonical CameraSolution DepthField PointMap and GeometrySolution values so later learned candidates can normalize camera depth and optional point geometry without leaking model-private tensors hidden neural state or checkpoint-specific structures into core contracts and without selecting or integrating any learned model yet.
    allowed_scope:
      - src/wre/reconstruction/feed_forward_geometry.py
      - focused exports in src/wre/reconstruction/__init__.py
      - tests/test_feed_forward_geometry_adapter.py
      - focused lifecycle updates to registry/components.yaml PROJECT_STATE.yaml and this work-item registry only after implementation evidence is green so V2L13.2 can be activated without implementing it
    out_of_scope:
      - integrating downloading importing executing benchmarking or selecting DA3 VGGT-omega Pi3 Pi3X MapAnything CUT3R Uni3R TokenSplat or any other learned repository model checkpoint or runtime
      - adding Torch CUDA TensorRT ONNX JAX NumPy model-serving or another external dependency to the feed-forward interface module
      - adding or promoting a concrete adapter-model registry entry before V2L13.2 performs candidate freshness license checkpoint and runtime review
      - changing CameraSolution DepthField PointMap GeometrySolution LocalFrameId GeometryScaleStatus MetricVector ArtifactRef ArtifactKind or decoded-image-pyramid contracts
      - reading raw media decoding resizing recoloring normalizing tensors or inspecting filesystem model environment hardware network persistence or cache state
      - inferring physical CameraId rig identity rolling-shutter semantics metric scale absolute scale factor world Earth ECEF ENU geographic GPS GCP placement or cross-local-frame compatibility
      - silently converting arbitrary predicted units to metric or treating a model claim as metric evidence without a candidate-specific accepted contract
      - generating a SurfaceModel mesh collision navigation measurement appearance material environment dynamic historical MasterScene or RuntimeScene product
      - route selection quality-gate fallback retry scheduler resource-budget execution default promotion or comparison against COLMAP
      - defining learned execution profiles compile mixed-precision GPU profiling or performance benchmarks owned by V2L13.6
      - starting V2L13.2 implementation in this work item
    depends_on: [V2L12.5]
    read_before:
      - docs/01_PRODUCT.md
      - docs/02_ARCHITECTURE.md
      - docs/03_PIPELINE.md
      - docs/06_TESTING.md
      - docs/14_TECHNOLOGY_SELECTION.md
      - docs/23_ENGINEERING_EXECUTION.md
      - docs/24_SYSTEM_INVARIANTS.md
      - docs/25_V2_ROADMAP.md
      - docs/26_V2_PRODUCT_ARCHITECTURE_FREEZE.md
      - docs/27_RESEARCH_CANDIDATE_COVERAGE.md
      - docs/28_PERFORMANCE_OPTIMIZATION_PLAYBOOK.md
      - docs/29_PERFORMANCE_INTEGRATION_MAP.md
      - docs/reviews/V2L11-canonical-geometry-contracts-review.md
      - docs/reviews/V2L12-classical-precision-geometry-review.md
      - registry/adapter-models.yaml
      - registry/dependencies.yaml
      - src/wre/domain/adapter_capabilities.py
      - src/wre/domain/artifacts.py
      - src/wre/domain/camera_solutions.py
      - src/wre/domain/depth_fields.py
      - src/wre/domain/point_maps.py
      - src/wre/domain/geometry_solutions.py
      - src/wre/domain/fragments.py
      - src/wre/domain/metrics.py
      - src/wre/ingestion/decoded_images.py
      - src/wre/reconstruction/colmap_adapter.py
      - src/wre/reconstruction/colmap_canonical_geometry.py
      - tests/test_camera_solutions.py
      - tests/test_depth_fields.py
      - tests/test_point_maps.py
      - tests/test_geometry_solutions.py
      - tests/test_colmap_adapter.py
    reuse:
      - AdapterCapabilityDescriptor and AdapterCapabilityName as the existing semantic capability contract rather than defining a second generic adapter framework
      - media.decoded_image_pyramid ArtifactRef values from V2L6 as the reusable canonical image-byte input boundary so learned candidates do not re-read raw media or define private decode truth
      - CameraSolution as the only canonical per-observation camera output including camera-from-local-frame semantics and explicit LocalFrameId
      - DepthField as the canonical per-observation dense depth carrier with explicit validity confidence and open depth-value convention
      - PointMap as an optional canonical local-frame point carrier when a candidate natively supports point predictions without making it mandatory for every feed-forward method
      - GeometrySolution as the only assembled feed-forward geometry hypothesis referencing exactly the emitted canonical child identities with explicit unresolved or independently-supported metric local scale
      - immutable positive-domain aggregate validation rather than generic dictionaries model tensors or optional bags
    acceptance:
      - Add one pure feed-forward geometry module whose import and contract construction require no learned framework model checkpoint GPU filesystem network persistence cache registry or solver execution.
      - Define one stable feed-forward geometry capability name and descriptor consuming only media.decoded_image_pyramid artifacts and exposing the canonical geometry responsibilities required by later candidate adapters without reusing the legacy geometry.sparse_reconstruction_estimate kind.
      - If an input normalization helper is exposed it accepts only a non-empty immutable tuple of unique canonically ordered media.decoded_image_pyramid ArtifactRef values and fails closed on mutable empty duplicate wrong-kind or non-canonical inputs without sorting coercion or filesystem access.
      - Define one immutable normalized feed-forward geometry result aggregate containing canonical camera_solutions depth_fields point_maps and exactly one geometry_solution with no model-private tensor latent state feature track checkpoint runtime or generic metadata field.
      - camera_solutions depth_fields and point_maps are immutable tuples containing only their accepted V2L11 domain values; at least one camera solution is required and the aggregate must contain at least one canonical geometry carrier through depth_fields or point_maps.
      - CameraSolution values must share exactly one LocalFrameId with every emitted PointMap and the GeometrySolution; the aggregate does not align or reconcile multiple local frames.
      - CameraSolution observation IDs must be unique and canonical; each emitted DepthField observation must correspond to exactly one emitted CameraSolution and reference that exact camera solution ID with matching image dimensions.
      - PointMap source observations must stay inside the emitted camera observation membership and every emitted PointMap must use the exact aggregate LocalFrameId.
      - GeometrySolution must reuse the exact aggregate LocalFrameId and reference exactly all emitted CameraSolutionId DepthFieldId and PointMapId values in their canonical contract order with no missing foreign duplicate or unreferenced child.
      - The aggregate preserves GeometryScaleStatus exactly as supplied by the candidate-specific adapter and performs no scale inference coordinate rescaling metric promotion world placement or unit conversion.
      - An unresolved GeometrySolution remains valid; a metric GeometrySolution is accepted structurally only when the candidate adapter explicitly supplies that already-validated canonical status and V2L13.1 itself adds no metric-evidence claim.
      - Repeated validation of the same canonical values returns equal values and does not clone mutate reorder normalize convert or derive new child geometry.
      - The feed-forward interface makes no quality mode route default classical fallback or preferred-candidate decision and does not compare against the V2L12 COLMAP baseline.
      - No adapter-model registry candidate entry external dependency checkpoint identity model preprocessing execution profile quality metric or benchmark is introduced by V2L13.1.
    tests:
      - stable pure capability descriptor input and output kind regression with no legacy sparse estimate output
      - decoded-image input immutable canonical tuple empty wrong-kind duplicate and non-canonical fail-closed tests if an input helper is implemented
      - exact frozen normalized-result field shape and wrong child-type regressions
      - one unresolved local-frame fixture with multiple cameras and depths proving exact camera-depth observation ID dimension and LocalFrameId relationships
      - optional PointMap fixture proving source membership exact local frame and GeometrySolution linkage without requiring point output for depth-only candidates
      - zero geometry-carrier rejection plus missing foreign duplicate or unreferenced CameraSolution DepthField and PointMap child-reference fail-closed regressions
      - exact LocalFrameId continuity and regression proving no world Earth geographic fragment or cross-frame compatibility claim
      - unresolved and metric status preservation test proving the interface performs no rescaling unit conversion or implicit metric promotion
      - source immutability regression proving aggregate validation does not reorder or mutate supplied canonical child values
      - module-surface regression proving no Torch CUDA TensorRT ONNX JAX NumPy model checkpoint filesystem network persistence routing quality surface appearance dynamic or V2L13.2 dependency enters the pure interface layer
      - full fast CI and CodeQL plus native lanes only where repository path triggers require them
  - id: V2L13.2
    lot: V2L13
    title: First approved feed forward preview candidate
    status: done
    objective: Integrate Depth Anything 3 DA3-BASE as the first bounded feed-forward PREVIEW candidate behind the V2L13.1 contract by defining candidate-specific immutable native prediction semantics and direct normalization into canonical CameraSolution DepthField optional PointMap and GeometrySolution values, while keeping actual external model environment checkpoint execution hardware profile and authoritative reproducibility pinning in V2L13.3.
    candidate_refresh:
      reviewed_at: '2026-09-22'
      selected_family: Depth Anything 3
      selected_variant: DA3-BASE
      upstream_repository: ByteDance-Seed/Depth-Anything-3
      observed_source_revision: 3d835ec1a5802d64a8b8b15f817a1ab54809bfe4
      observed_checkpoint_identifier: depth-anything/DA3-BASE
      observed_checkpoint_sha256: e01067dc1659613083d9145a9a2547ccdbe6ccbbf83c4fe7b3e8a4e2bdae78b5
      source_license: Apache-2.0
      checkpoint_license: Apache-2.0
      selection_notes: >-
        DA3-BASE is selected only as the first integration candidate because its current public API
        exposes relative depth confidence OpenCV world-to-camera extrinsics and pinhole intrinsics,
        its 0.12B variant is materially smaller than one-billion-parameter preview alternatives, and
        both reviewed source and checkpoint report Apache-2.0 terms. This is not a benchmark winner,
        default promotion, metric-scale claim or permanent specialist decision. VGGT-Omega remains
        noncommercial/gated under its current FAIR research license, Pi3/Pi3X public weights remain
        noncommercial, and MapAnything Apache remains a valid heavier comparison candidate.
    allowed_scope:
      - src/wre/reconstruction/da3_preview.py
      - focused exports in src/wre/reconstruction/__init__.py
      - tests/test_da3_preview_adapter.py
      - focused lifecycle updates to registry/components.yaml PROJECT_STATE.yaml and this work-item registry only after implementation evidence is green so V2L13.3 can be activated without implementing it
    out_of_scope:
      - importing executing downloading or vendoring Depth Anything 3 torch xformers huggingface_hub safetensors Pillow OpenCV NumPy or another learned runtime dependency inside WRE core
      - adding an authoritative dependency adapter-model checkpoint hardware or shipping registry entry before V2L13.3
      - treating the observed source revision checkpoint digest or current upstream license evidence in candidate_refresh as the final V2L13.3 reproducibility manifest
      - integrating DA3-SMALL DA3-LARGE DA3-GIANT nested metric Gaussian streaming or another DA3 variant
      - integrating MapAnything VGGT VGGT-Omega Pi3 Pi3X CUT3R Uni3R TokenSplat or another learned candidate
      - changing FeedForwardGeometryResult CameraSolution DepthField PointMap GeometrySolution LocalFrameId GeometryScaleStatus decoded-image or ArtifactRef contracts
      - raw media reading decoding resize crop color normalization tensor conversion batching device transfer inference mixed precision compilation or model output persistence
      - inferring metric scale from DA3-BASE because upstream exposes metric variants; DA3-BASE prediction is treated as unresolved-scale unless later explicit candidate evidence proves otherwise
      - using predicted world wording to claim Earth geographic global project or cross-run world identity
      - route selection PREVIEW default promotion quality scoring fallback retry comparison against COLMAP or any V2L13.4 V2L13.5 V2L13.6 behavior
      - physical SurfaceModel mesh collision measurement appearance dynamic chronology MasterScene RuntimeScene or later milestone work
      - starting V2L13.3 implementation in this work item
    depends_on: [V2L13.1]
    read_before:
      - docs/01_PRODUCT.md
      - docs/02_ARCHITECTURE.md
      - docs/03_PIPELINE.md
      - docs/06_TESTING.md
      - docs/14_TECHNOLOGY_SELECTION.md
      - docs/23_ENGINEERING_EXECUTION.md
      - docs/24_SYSTEM_INVARIANTS.md
      - docs/25_V2_ROADMAP.md
      - docs/26_V2_PRODUCT_ARCHITECTURE_FREEZE.md
      - docs/27_RESEARCH_CANDIDATE_COVERAGE.md
      - docs/28_PERFORMANCE_OPTIMIZATION_PLAYBOOK.md
      - docs/29_PERFORMANCE_INTEGRATION_MAP.md
      - docs/reviews/V2L12-classical-precision-geometry-review.md
      - registry/dependencies.yaml
      - registry/adapter-models.yaml
      - src/wre/reconstruction/feed_forward_geometry.py
      - src/wre/domain/camera_solutions.py
      - src/wre/domain/depth_fields.py
      - src/wre/domain/point_maps.py
      - src/wre/domain/geometry_solutions.py
      - src/wre/domain/fragments.py
      - src/wre/domain/metrics.py
      - tests/test_feed_forward_geometry_adapter.py
    external_evidence:
      - ByteDance-Seed/Depth-Anything-3 README.md at revision 3d835ec1a5802d64a8b8b15f817a1ab54809bfe4
      - ByteDance-Seed/Depth-Anything-3 docs/API.md at revision 3d835ec1a5802d64a8b8b15f817a1ab54809bfe4
      - ByteDance-Seed/Depth-Anything-3 src/depth_anything_3/specs.py at revision 3d835ec1a5802d64a8b8b15f817a1ab54809bfe4
      - ByteDance-Seed/Depth-Anything-3 src/depth_anything_3/api.py at revision 3d835ec1a5802d64a8b8b15f817a1ab54809bfe4
      - Hugging Face depth-anything/DA3-BASE model card and model.safetensors identity observed on 2026-09-22
    reuse:
      - FeedForwardGeometryResult as the exact normalized aggregate boundary; DA3-specific values must disappear after normalization
      - the reviewed DA3 Prediction semantics depth N-H-W confidence N-H-W extrinsics N-by-4-by-4 world-to-camera and intrinsics N-by-3-by-3 as candidate-private input semantics
      - DA3 world-to-camera OpenCV extrinsics as the same mathematical camera-from-local direction required by CameraSolution, copied without inversion transpose sign flip or world/Earth promotion
      - canonical pinhole intrinsics fx fy cx cy extracted from the candidate matrix without solver-private calibration objects
      - one explicit deterministic LocalFrameId namespace for one candidate prediction set, treated only as a candidate-local scene frame
      - GeometryScaleStatus.UNRESOLVED for DA3-BASE because the reviewed Any-view BASE checkpoint is not the metric/nested series
      - pure Python immutable tuples and exact finite-float validation so V2L13.2 does not require NumPy Torch or model execution
    acceptance:
      - Add one pure DA3 preview normalization module whose import and tests require no DA3 repository learned framework checkpoint GPU filesystem network or external environment.
      - Define one immutable candidate-native prediction value with exact observation identity image dimensions world-to-camera 3x3 rotation translation pinhole 3x3 intrinsics row-major depth validity/confidence data and no generic metadata latent tensors features model object path or mutable arrays.
      - Candidate-native tuples must be immutable non-empty canonically ordered by ObservationId and contain unique observations; malformed mutable duplicate unsorted wrong-type NaN infinity or shape-invalid evidence fails closed without reordering coercion or repair.
      - Validate pinhole intrinsics as finite exact floats with homogeneous matrix structure and positive focal lengths; preserve fx fy cx cy exactly into canonical CameraSolution intrinsic_parameters without hidden rescaling.
      - Validate every candidate world-to-camera rotation as the canonical CameraSolution camera-from-local rotation convention and copy rotation and translation directly into canonical values without inversion transpose sign change axis swap coordinate rescaling or renaming the local frame to world.
      - Validate each depth raster against its exact image dimensions; valid samples are finite strictly positive floats and invalid samples use canonical 0.0, with optional confidence finite in zero-to-one and canonical 0.0 on invalid pixels.
      - Emit exactly one canonical CameraSolution and one DepthField per candidate observation, all sharing one explicit deterministic LocalFrameId; DA3-specific upstream world wording creates no Earth project-world or cross-run compatibility claim.
      - Use one explicit open canonical depth convention token owned by this adapter and document whether the candidate values are interpreted as camera-z or another exact reviewed DA3 convention; do not guess a convention from exporter behavior if upstream evidence is insufficient.
      - DA3-BASE canonical GeometrySolution scale_status must be UNRESOLVED and V2L13.2 must contain a negative regression preventing metric promotion from checkpoint family names scale-factor fields or other absent metric-series evidence.
      - PointMap output is optional; if V2L13.2 cannot derive a point map without adding an unreviewed depth-unprojection convention it must emit no PointMap and rely on canonical DepthField values only.
      - GeometrySolution must reference exactly every emitted CameraSolution and DepthField and any emitted PointMap, then the result must validate through FeedForwardGeometryResult without a DA3-specific downstream branch.
      - Stable role-separated canonical output IDs and LocalFrameId must derive only from an explicit immutable normalization identity supplied by the caller plus observation roles, never from Python object identity filesystem path timestamps hardware or mutable upstream values.
      - Repeating normalization over equal candidate-native values and the same normalization identity must produce equal canonical values byte-for-byte at the Python value layer.
      - The module must not expose execution download checkpoint registry route quality benchmark surface appearance dynamic or V2L13.3 symbols.
    tests:
      - candidate-native prediction exact frozen field shape immutable tuple canonical ordering uniqueness type finite-float matrix and raster validation regressions
      - asymmetric world-to-camera fixture proving direct R-x-plus-t CameraSolution semantics and rejecting transpose inverse translation-sign and hidden-axis alternatives
      - pinhole intrinsic extraction exactness fixture for fx fy cx cy plus malformed homogeneous matrix and non-positive focal negatives
      - depth validity confidence boundary fixtures including invalid-zero canonicalization contract and no silent masking threshold
      - deterministic output LocalFrameId CameraSolutionId DepthFieldId GeometrySolutionId derivation from caller normalization identity with repeat equality
      - multi-observation normalization proving one camera and one depth per observation exact observation linkage dimensions and one shared local frame
      - DA3-BASE unresolved-scale regression proving no metric world Earth geographic or cross-run claim
      - regression proving no PointMap is fabricated unless the accepted reviewed depth convention supports explicit unprojection within this item
      - FeedForwardGeometryResult integration regression proving exact child references and no candidate-private object leaks past normalization
      - module-surface regression proving no Torch xformers huggingface NumPy filesystem network model execution registry routing quality comparison surface appearance dynamic or V2L13.3 dependency enters the adapter
      - full fast CI and CodeQL plus native lanes only where repository path triggers require them
  - id: V2L13.3
    lot: V2L13
    title: Feed forward checkpoint hardware license reproducibility
    status: done
    objective: Establish the authoritative DA3-BASE external execution and reproducibility boundary for the V2 feed-forward PREVIEW candidate by pinning one exact reviewed source revision and checkpoint identity, one explicit reference runtime/hardware profile, direct and transitive license evidence, verified canonical decoded-image inputs, deterministic candidate execution into the V2L13.2 native prediction boundary, and exact producer/model/checkpoint/configuration/hardware identity without quality scoring default promotion or performance-profile optimization.
    allowed_scope:
      - src/wre/reconstruction/da3_runtime.py
      - focused reuse or identity hooks in src/wre/reconstruction/da3_preview.py without changing its canonical normalization semantics
      - focused exports in src/wre/reconstruction/__init__.py
      - registry/dependencies.yaml for the exact optional DA3 external environment and reviewed direct/transitive license records actually required by the reference path
      - registry/adapter-models.yaml for one experimental DA3-BASE geometry.feed_forward adapter registration with exact producer model checkpoint hardware policy and license posture
      - tests/test_da3_runtime.py
      - focused updates to tests/test_adapter_model_registry_baselines.py only when required by the new explicit experimental adapter entry
      - one optional dedicated DA3 integration workflow or fixture if real candidate execution cannot belong in ordinary fast CI and can run without implicit source or checkpoint download
      - docs/14_TECHNOLOGY_SELECTION.md only for a focused note recording the exact first DA3 reference path as experimental and not benchmark-promoted
      - focused lifecycle updates to registry/components.yaml PROJECT_STATE.yaml and this work-item registry only after implementation evidence is green so V2L13.4 can be activated without implementing it
    out_of_scope:
      - changing FeedForwardGeometryResult Da3BaseObservationPrediction CameraSolution DepthField PointMap GeometrySolution LocalFrameId or GeometryScaleStatus semantics
      - changing the DA3-BASE relative-depth token world-to-camera copy semantics unresolved-scale policy no-PointMap decision or deterministic canonical ID rules established by V2L13.2
      - integrating another DA3 variant including SMALL LARGE GIANT METRIC NESTED Gaussian or streaming paths
      - integrating MapAnything VGGT VGGT-Omega Pi3 Pi3X CUT3R Uni3R TokenSplat or another feed-forward candidate
      - making torch numpy Pillow huggingface_hub safetensors xformers or DA3 mandatory core WRE dependencies; the learned runtime remains an optional exact external environment
      - implicit Git clone pip install torch.hub Hugging Face download checkpoint download model-code download URI resolution or any runtime network fallback
      - accepting floating latest main unpinned package versions unchecked source trees dirty executable shadows or checkpoint identities without exact local byte verification
      - training finetuning checkpoint conversion quantization TensorRT torch.compile CUDA graphs mixed precision alternatives or multiple execution profiles owned by V2L13.6
      - inventing metric scale from DA3-BASE or treating candidate local world as project Earth ECEF ENU geographic or cross-run world truth
      - geometry camera quality metrics controlled holdout evaluation comparison against COLMAP or default promotion owned by V2L13.4 and V2L13.5
      - PREVIEW router/default fallback retry QualityDecision route graph or automatic candidate selection
      - dense MVS fusion SurfaceModel appearance dynamic chronology MasterScene RuntimeScene or later milestone work
      - starting V2L13.4 implementation in this work item
    depends_on: [V2L13.2]
    read_before:
      - docs/01_PRODUCT.md
      - docs/02_ARCHITECTURE.md
      - docs/03_PIPELINE.md
      - docs/06_TESTING.md
      - docs/14_TECHNOLOGY_SELECTION.md
      - docs/15_PRODUCTION_RUNTIME.md
      - docs/23_ENGINEERING_EXECUTION.md
      - docs/24_SYSTEM_INVARIANTS.md
      - docs/25_V2_ROADMAP.md
      - docs/26_V2_PRODUCT_ARCHITECTURE_FREEZE.md
      - docs/27_RESEARCH_CANDIDATE_COVERAGE.md
      - docs/28_PERFORMANCE_OPTIMIZATION_PLAYBOOK.md
      - docs/29_PERFORMANCE_INTEGRATION_MAP.md
      - registry/dependencies.yaml
      - registry/adapter-models.yaml
      - src/wre/domain/artifact_materialization.py
      - src/wre/domain/decoded_images.py
      - src/wre/domain/hardware_identity.py
      - src/wre/domain/producer_identity.py
      - src/wre/ingestion/decoded_images.py
      - src/wre/materialization.py
      - src/wre/reconstruction/feed_forward_geometry.py
      - src/wre/reconstruction/da3_preview.py
      - src/wre/retrieval/selavpr_plus.py
      - tests/test_decoded_image_pyramids.py
      - tests/test_da3_preview_adapter.py
      - tests/test_selavpr_plus_retrieval.py
    external_evidence:
      - ByteDance-Seed/Depth-Anything-3 source repository at exact reviewed revision 3d835ec1a5802d64a8b8b15f817a1ab54809bfe4
      - ByteDance-Seed/Depth-Anything-3 LICENSE and the exact source files imported by the bounded DA3-BASE reference path
      - ByteDance-Seed/Depth-Anything-3 README.md docs/API.md src/depth_anything_3/api.py src/depth_anything_3/specs.py and input/output processor code at that revision
      - Hugging Face depth-anything/DA3-BASE model card config and model.safetensors with checkpoint SHA-256 e01067dc1659613083d9145a9a2547ccdbe6ccbbf83c4fe7b3e8a4e2bdae78b5
      - concrete package metadata and license texts for every runtime package admitted to the reference environment rather than the full optional demo server or training dependency surface
    reuse:
      - Da3BaseObservationPrediction and normalize_da3_base_preview as the only candidate-to-canonical semantic boundary; external execution ends by constructing the already-reviewed V2L13.2 value rather than writing canonical geometry directly
      - DecodedImagePyramidMaterializationResult plus verify_local_artifact_materialization as the canonical verified image input boundary so DA3 never reopens raw media
      - ModelIdentity CheckpointIdentity ArtifactProducerIdentity ConfigurationIdentity Sha256Digest and HardwareRuntimeIdentity as the exact reproducibility vocabulary
      - SelaVPR++ external-environment patterns for local source-tree audit exact checkpoint-byte verification safe no-network loading optional learned runtime separation and core-import independence, adapted only where DA3 actually requires different packages or preprocessing
      - source revision 3d835ec1a5802d64a8b8b15f817a1ab54809bfe4 and checkpoint depth-anything/DA3-BASE SHA-256 e01067dc1659613083d9145a9a2547ccdbe6ccbbf83c4fe7b3e8a4e2bdae78b5 as the reviewed starting identities; V2L13.3 must re-verify them authoritatively before registry approval
      - Apache-2.0 source and checkpoint terms as direct-license evidence only; transitive runtime licenses remain separately recorded and cannot be inferred from the top-level DA3 license
    acceptance:
      - Define one exact DA3-BASE ModelIdentity and CheckpointIdentity tied to source revision 3d835ec1a5802d64a8b8b15f817a1ab54809bfe4 and checkpoint identifier depth-anything/DA3-BASE with SHA-256 e01067dc1659613083d9145a9a2547ccdbe6ccbbf83c4fe7b3e8a4e2bdae78b5; floating tags latest aliases or unverified alternate files fail closed.
      - Define one immutable bounded reference execution config with a canonical ConfigurationIdentity covering every material DA3 inference choice used before Da3BaseObservationPrediction construction, including process resolution/method reference-view strategy pose head choice Gaussian disablement and any deterministic seed/thread/precision settings actually exposed by the selected reference runtime; no generic kwargs bag exists.
      - Select and document exactly one reference device/runtime profile after verifying the upstream path works there; encode its material environment/hardware identity explicitly and do not claim byte-equivalence across different devices drivers accelerators or math modes.
      - Keep the DA3 learned environment outside the mandatory core uv runtime; pin the exact concrete package versions used by the reference path and record direct/transitive license posture in registry/dependencies.yaml before registering the executable adapter as anything beyond experimental.
      - Source execution receives an explicit already-local DA3 source root or installed exact source environment and verifies the exact reviewed revision plus a bounded executable-source manifest before import; unexpected executable Python bytecode native-module shadows or source hash mismatch fail closed.
      - Checkpoint execution receives an explicit already-local regular model.safetensors file or equivalent locally materialized exact checkpoint asset whose bytes match CheckpointIdentity; no implicit Hugging Face torch.hub Git requests urllib or other network path is permitted.
      - Prefer the upstream safe safetensors loading path for the reviewed model; if another loader is required it must reject executable checkpoint payloads and validate expected model/config structure before use.
      - Each input is a VERIFIED canonical decoded-image pyramid materialization with exact source observation identity RGB8 packed source-pixel semantics and auditable level selection; the runtime does not reopen original image/video media.
      - Preprocessing and any upstream resize/crop behavior affecting returned dimensions intrinsics or depth are pinned explicitly enough that the produced Da3BaseObservationPrediction dimensions and pinhole intrinsics can be traced back to the exact processed candidate image semantics without silent EXIF autorotation or hidden caller-dependent defaults.
      - Real execution returns exactly one candidate-native prediction per input observation in canonical ObservationId order with finite reviewed world-to-camera pose pinhole intrinsics depth validity and optional confidence semantics accepted by Da3BaseObservationPrediction; malformed count shape NaN Inf unsupported camera output or missing required prediction values fail closed before canonical normalization.
      - DA3-BASE execution never sets metric scale and does not create PointMap values; it hands only accepted Da3BaseObservationPrediction values plus one explicit deterministic normalization identity into normalize_da3_base_preview.
      - The normalization identity used for canonical IDs is derived deterministically from exact producer/model/checkpoint/configuration/input artifact identities and material hardware identity where required, never from filesystem paths process IDs timestamps Python object identity or remote URLs.
      - Define an immutable DA3 execution result/evidence value retaining exact model checkpoint source/configuration/hardware environment identity and the normalized FeedForwardGeometryResult without leaking upstream tensors modules latent features or mutable NumPy/Torch values.
      - Register one experimental adapter entry whose capability consumes media.decoded_image_pyramid and emits geometry.camera_solution geometry.depth_field and geometry.solution, with exact dependency/model/checkpoint identities artifact_key_hardware_policy required and no geometry.point_map output claim for DA3-BASE.
      - The registry direct license records Apache-2.0 for the reviewed DA3 source and exact DA3-BASE checkpoint, while transitive notes enumerate the concrete reference runtime review and redistribution notes make clear that WRE does not implicitly fetch or bundle source/checkpoint bytes.
      - Ordinary WRE imports and fast unit tests remain runnable without Torch DA3 source checkpoint GPU or network; real-candidate execution lives in an explicitly provisioned optional integration lane or local fixture.
      - Repeating the pure reproducibility fixture with identical supplied candidate outputs producer/model/checkpoint/configuration/input/hardware identities yields equal normalization identity and equal canonical Python values; changing any identity dimension that is declared material changes the reproducibility identity rather than silently reusing artifacts.
      - No quality metric benchmark default promotion route decision performance optimization or V2L13.4 behavior is introduced.
    tests:
      - exact model checkpoint source-revision configuration and hardware identity value tests including floating latest wrong digest wrong type and unreviewed variant negatives
      - reference config exact field-set immutability canonical SHA determinism and unsupported hidden-default/precision/device option negatives
      - source-root exact-revision source-manifest executable-shadow and checkpoint hash/length/local-only fail-before-model-execution fixtures
      - dependency/environment inspection tests proving exact package versions and fail-closed mismatch behavior without importing the learned stack during ordinary module import
      - canonical decoded-image materialization verification tests covering corrupt bytes wrong observation identity wrong RGB layout/orientation/level and no raw-media reopen
      - fake runtime fixture proving exact DA3 inference arguments deterministic observation mapping one prediction per input and construction through Da3BaseObservationPrediction rather than direct canonical object manufacture
      - malformed fake outputs for wrong count shape NaN Inf unsupported pose/intrinsics depth/confidence proving failure before normalize_da3_base_preview
      - normalization-identity fixture proving exact producer model checkpoint configuration input and hardware identity participation with no path timestamp or URL dependence
      - result exact field-set immutability identity retention and regression proving no tensor ndarray module source path checkpoint path or mutable upstream object leaks to the stable result
      - adapter-model/dependency registry regression proving experimental status exact Apache-2.0 direct-license evidence required hardware-key policy and absence of PointMap output
      - core-import regression proving fast CI requires no Torch DA3 checkout checkpoint accelerator or network
      - optional real-candidate smoke fixture only when the exact reviewed external environment source checkpoint and declared hardware are explicitly provisioned; ordinary CI never downloads them
      - full fast CI CodeQL dependency review and any dedicated optional DA3 lane activated by the implementation
  - id: V2L13.4
    lot: V2L13
    title: Feed forward geometry camera quality metrics
    status: ready
    objective: Define one pure solver-independent feed-forward camera quality evaluator over canonical CameraSolution values and one explicit trusted reference-camera set so preview geometry can retain deterministic canonical MetricVector evidence for observation coverage relative pose and pinhole intrinsic quality without changing geometry values applying thresholds emitting QualityDecision comparing against COLMAP selecting routes or promoting a default.
    allowed_scope:
      - src/wre/reconstruction/feed_forward_camera_quality.py
      - focused exports in src/wre/reconstruction/__init__.py
      - tests/test_feed_forward_camera_quality.py
      - focused lifecycle updates to registry/components.yaml PROJECT_STATE.yaml and this work-item registry only after implementation evidence is green so V2L13.5 can be activated without implementing it
    out_of_scope:
      - changing CameraSolution FeedForwardGeometryResult Da3BaseObservationPrediction Da3ExecutionResult DepthField PointMap GeometrySolution LocalFrameId GeometryScaleStatus MetricVector or QualityDecision domain semantics
      - modifying DA3 source checkpoint execution preprocessing normalization confidence depth semantics or exact reproducibility profile established by V2L13.2 and V2L13.3
      - modifying retained/current COLMAP execution canonicalization outcome semantics shipping status or classical precision geometry implementation
      - evaluating depth point-map surface reprojection track-support image-feature appearance runtime or photometric quality
      - adding bundle adjustment pose refinement Sim3 alignment frame registration metric-scale solving world Earth ECEF ENU GPS GCP anchoring or cross-local-frame geometry mutation
      - deriving an absolute translation error directly across unrelated local frames or silently assuming candidate and reference local coordinates share an origin orientation or scale
      - comparing DA3 against COLMAP computing a route winner score setting quality thresholds emitting PASS RETRY ESCALATE or another QualityDecision or changing PREVIEW routing/default behavior
      - benchmark registry promotion holdout-dataset selection production scheduler cache persistence artifact publication or performance profiling
      - integrating another learned candidate or starting V2L13.5 implementation
    depends_on: [V2L13.3]
    read_before:
      - docs/01_PRODUCT.md
      - docs/02_ARCHITECTURE.md
      - docs/03_PIPELINE.md
      - docs/06_TESTING.md
      - docs/14_TECHNOLOGY_SELECTION.md
      - docs/23_ENGINEERING_EXECUTION.md
      - docs/24_SYSTEM_INVARIANTS.md
      - docs/25_V2_ROADMAP.md
      - docs/26_V2_PRODUCT_ARCHITECTURE_FREEZE.md
      - docs/28_PERFORMANCE_OPTIMIZATION_PLAYBOOK.md
      - docs/29_PERFORMANCE_INTEGRATION_MAP.md
      - src/wre/domain/metrics.py
      - src/wre/domain/quality.py
      - src/wre/domain/quality_policy.py
      - src/wre/domain/camera_solutions.py
      - src/wre/domain/fragments.py
      - src/wre/reconstruction/feed_forward_geometry.py
      - src/wre/reconstruction/da3_preview.py
      - src/wre/reconstruction/da3_runtime.py
      - tests/test_metrics.py
      - tests/test_quality_decisions.py
      - tests/test_quality_policy.py
      - tests/test_feed_forward_geometry_adapter.py
      - tests/test_da3_preview_adapter.py
      - tests/test_da3_runtime.py
    reuse:
      - CameraSolution camera-from-local convention x_camera equals R times x_local plus t and exact pinhole intrinsic tuple semantics as the only pose/calibration inputs; candidate-private DA3 tensors or upstream objects do not enter the evaluator
      - FeedForwardGeometryResult as the exact candidate camera collection boundary without copying rewriting refining or changing its canonical child values
      - MetricName MetricDescriptor MetricDimension MetricUnit MetricAggregation MetricDirection MetricProvenance MetricObservation and MetricVector as the sole metric vocabulary rather than introducing adapter-local score dictionaries
      - ArtifactProducerIdentity ConfigurationIdentity and ordered ArtifactRef inputs for exact evaluator provenance without timestamps filesystem paths Python object identity or mutable metadata
      - ObservationId intersection as the only candidate/reference correspondence key; physical camera identity scene similarity filenames list position or nearest-pose matching are not inferred
      - pairwise relative camera transforms derived from camera-from-local poses so rotation and translation-direction metrics remain invariant to a global proper rotation translation and positive local scale between candidate and reference frames
      - existing pure QualityPolicy machinery only as a later consumer of MetricVector values; this item emits measurements and never evaluates thresholds or QualityDecision
    acceptance:
      - Add one pure module whose import and fast tests require no Torch DA3 checkout checkpoint COLMAP NumPy network GPU native solver or filesystem fixture.
      - Define one immutable camera-quality input/evaluation boundary containing one FeedForwardGeometryResult candidate one non-empty immutable reference CameraSolution tuple and exact ordered metric-provenance ArtifactRef inputs; no generic metadata kwargs or mutable collections are accepted.
      - Reference cameras must be unique and canonically ordered by ObservationId share one reference LocalFrameId and use valid canonical CameraSolution values; candidate cameras remain exactly those owned by the supplied FeedForwardGeometryResult.
      - Match candidate and reference cameras only by exact ObservationId. Extra candidate observations never improve reference coverage and missing reference observations remain missing rather than being nearest-neighbor matched fabricated or reordered.
      - Emit geometry.camera.observation_coverage_ratio as shared-reference-observation count divided by total reference-camera count with unit ratio direction HIGHER_IS_BETTER and one exact deterministic aggregation token.
      - For every canonical shared observation pair derive relative rotation using the two camera-from-local rotations and measure the geodesic rotation-angle error in degrees between candidate and reference relative rotations. Aggregate all eligible pair errors with an explicitly implemented deterministic median into geometry.camera.relative_rotation_error_deg_median with LOWER_IS_BETTER.
      - For every canonical shared observation pair derive the directed camera-center baseline from the lexicographically first ObservationId to the second and express that direction in the first camera coordinate frame. Compare candidate and reference baseline directions by angular error in degrees so global local-frame rotation translation and positive scale cancel without estimating an alignment.
      - Pairs with exactly coincident camera centers in either candidate or reference are ineligible for translation-direction error rather than receiving a fabricated direction. Emit geometry.camera.translation_pair_coverage_ratio as eligible translation pairs divided by all shared observation pairs and emit geometry.camera.relative_translation_direction_error_deg_median only when at least one eligible pair exists.
      - For each shared pinhole camera with exactly four intrinsic parameters fx fy cx cy and equal image dimensions compute focal relative error from fx and fy against the trusted reference focal values and compute principal-point Euclidean pixel error normalized by the reference image diagonal. Aggregate each deterministic sample set by median into geometry.camera.focal_relative_error_median and geometry.camera.principal_point_error_normalized_median with LOWER_IS_BETTER.
      - Shared cameras using a non-pinhole projection token wrong intrinsic arity non-positive trusted-reference focal length or mismatched image dimensions fail closed instead of aliasing coercing resizing or silently omitting intrinsic samples.
      - The metric evaluator must not compare absolute candidate/reference translation vectors camera centers or LocalFrameId values directly because unrelated local frames may differ by proper rigid transform and positive scale.
      - Metric observations use exact stable descriptors dimensions units directions and aggregations and all carry one exact module-owned ArtifactProducerIdentity plus the exact caller-supplied ordered ArtifactRef provenance tuple. MetricVector output is canonical by MetricName and contains no duplicate names.
      - A zero-shared-observation evaluation is valid only for the explicit coverage metric; relative-pose and intrinsic metrics are absent rather than zero fabricated NaN or infinity. One shared observation may emit coverage and intrinsic metrics but no pairwise rotation or translation metrics.
      - Applying any common proper rigid transform and positive scale to every camera in one local frame without changing the represented physical cameras leaves all relative-pose quality metrics unchanged within the fixed numerical tolerance used by the evaluator.
      - The evaluator returns measurements only. It imports or constructs no QualityPolicy QualityEvaluation QualityDecision FailureCategory route fallback retry threshold winner score default-selection or V2L13.5 comparison behavior.
      - Candidate and reference CameraSolution values and their existing MetricVector fields remain byte-for-byte equal at the Python value layer before and after evaluation; L13.4 never annotates or rewrites geometry in place.
    tests:
      - immutable input/evaluation exact field-shape canonical-order uniqueness and provenance validation regressions
      - identity candidate/reference fixture proving full observation coverage and exact zero rotation translation-direction focal and principal-point errors
      - candidate-subset fixture proving observation coverage uses reference denominator and missing cameras are not fabricated
      - asymmetric multi-camera fixture proving exact pairwise camera-from-local relative-rotation math and deterministic median aggregation
      - global proper rigid-transform plus positive-scale invariance regression proving no absolute-frame or metric-scale assumption enters relative pose metrics
      - directed relative-translation angular-error fixture plus exactly coincident-center exclusion and translation-pair coverage regression
      - pinhole focal-relative and normalized-principal-point fixtures including unequal dimensions unsupported projection wrong arity and non-positive reference focal fail-closed negatives
      - zero-shared and one-shared observation regressions proving unavailable pairwise metrics are absent rather than zero NaN or infinity
      - exact stable MetricDescriptor and MetricProvenance regression including canonical MetricVector ordering and evaluator identity determinism
      - source immutability regression proving candidate reference cameras and pre-existing metrics are not copied with altered semantic values or mutated
      - module-surface regression proving no DA3 learned runtime COLMAP quality policy decision router threshold benchmark winner surface appearance dynamic persistence or V2L13.5 dependency enters the evaluator
      - full fast CI CodeQL and native/compatibility lanes only where repository path triggers require them
  - {id: V2L13.5, lot: V2L13, title: Preview versus classical baseline comparison, status: planned, depends_on: [V2L13.4]}
  - {id: V2L13.6, lot: V2L13, title: Learned execution profile and stage profiling benchmark, status: planned, depends_on: [V2L13.5]}
  - {id: V2L14.1, lot: V2L14, title: Competing GeometrySolution comparison contract, status: planned, depends_on: [V2L13.6]}
  - {id: V2L14.2, lot: V2L14, title: Geometry initialization refinement adapter boundary, status: planned, depends_on: [V2L14.1]}
  - {id: V2L14.3, lot: V2L14, title: Classical BA global refinement path, status: planned, depends_on: [V2L14.2]}
  - {id: V2L14.4, lot: V2L14, title: First approved hybrid global candidate, status: planned, depends_on: [V2L14.3]}
  - {id: V2L14.5, lot: V2L14, title: Geometry consensus disagreement metrics, status: planned, depends_on: [V2L14.4]}
  - {id: V2L14.6, lot: V2L14, title: Controlled geometry comparison benchmark, status: planned, depends_on: [V2L14.5]}
  - {id: V2L15.1, lot: V2L15, title: Dense depth artifact contract, status: planned, depends_on: [V2L14.6]}
  - {id: V2L15.2, lot: V2L15, title: COLMAP MVS PatchMatch donor baseline adapter, status: planned, depends_on: [V2L15.1]}
  - {id: V2L15.3, lot: V2L15, title: Depth consistency fusion contract, status: planned, depends_on: [V2L15.2]}
  - {id: V2L15.4, lot: V2L15, title: Learned depth prior adapter boundary, status: planned, depends_on: [V2L15.3]}
  - {id: V2L15.5, lot: V2L15, title: Dense depth coverage holes confidence, status: planned, depends_on: [V2L15.4]}
  - {id: V2L15.6, lot: V2L15, title: Dense depth benchmark, status: planned, depends_on: [V2L15.5]}
  - {id: V2L16.1, lot: V2L16, title: SurfaceModel contract and intended use metadata, status: planned, depends_on: [V2L15.6]}
  - {id: V2L16.2, lot: V2L16, title: Baseline depth fusion to surface adapter, status: planned, depends_on: [V2L16.1]}
  - {id: V2L16.3, lot: V2L16, title: Collision measurement suitability metrics, status: planned, depends_on: [V2L16.2]}
  - {id: V2L16.4, lot: V2L16, title: Hybrid surface candidate adapter boundary, status: planned, depends_on: [V2L16.3]}
  - {id: V2L16.5, lot: V2L16, title: Unsupported region and hole semantics, status: planned, depends_on: [V2L16.4]}
  - {id: V2L16.6, lot: V2L16, title: Physical surface benchmark, status: planned, depends_on: [V2L16.5]}
