schema_version: 1
entries:
  - adapter_id: colmap.geometric_verification
    capability:
      name: evidence.geometric_verification
      input_kinds: [evidence.pair_matches]
      output_kinds: [evidence.geometric_verification]
    producer:
      implementation: pycolmap.geometric_verification
      version: 4.2.0
      revision: null
    dependency_refs: [colmap]
    model: null
    checkpoint: null
    artifact_key_hardware_policy: required
    license:
      direct: BSD-3-Clause
      review: approved
      transitive_notes: "The approved dependency record notes that a concrete COLMAP build can include separately licensed dependencies; this evidence adapter does not broaden those terms."
      redistribution_notes: "WRE retains PyCOLMAP/COLMAP as an exact external solver environment; V2L12.2 adds V2 evidence identity only and does not approve alternate builds or bundled redistribution."
    shipping_status: experimental
    reproducibility_notes: "V2L12.2 identifies the deterministic PyCOLMAP 4.2.0 two-view verification database as evidence.geometric_verification through canonical V2 artifact-key and materialization contracts without promoting pair evidence to accepted scene geometry."
    failure_signals: []
    metric_names: []
    resume_mode: unsupported
  - adapter_id: colmap.global_precision_geometry
    capability:
      name: geometry.precision_sfm.global
      input_kinds: [evidence.geometric_verification, image.observation]
      output_kinds: [geometry.camera_solution, geometry.point_map, geometry.solution]
    producer:
      implementation: pycolmap.global_mapping
      version: 4.2.0
      revision: null
    dependency_refs: [colmap]
    model: null
    checkpoint: null
    artifact_key_hardware_policy: required
    license:
      direct: BSD-3-Clause
      review: approved
      transitive_notes: "The approved dependency record notes that a concrete COLMAP build can include separately licensed dependencies; this executable V2 adapter does not broaden those terms."
      redistribution_notes: "WRE retains PyCOLMAP/COLMAP as an exact external solver environment; V2L12.3 adds the current global mapper behind a private copied verification database and does not approve alternate builds or bundled redistribution."
    shipping_status: experimental
    reproducibility_notes: "V2L12.3 runs PyCOLMAP 4.2.0 global_mapping only on a fresh verified private copy of the geometric-verification database, with an explicit non-negative seed, one mapper thread, global-positioning GPU disabled, Ceres bundle adjustment selected and Ceres GPU disabled. View-graph calibration is also restricted to the private copy; PyCOLMAP 4.2.0 exposes its random seed but not its internal Ceres thread setting."
    failure_signals: []
    metric_names: []
    resume_mode: unsupported
  - adapter_id: colmap.incremental_precision_geometry
    capability:
      name: geometry.precision_sfm.incremental
      input_kinds: [evidence.geometric_verification, image.observation]
      output_kinds: [geometry.camera_solution, geometry.point_map, geometry.solution]
    producer:
      implementation: pycolmap.incremental_mapping
      version: 4.2.0
      revision: null
    dependency_refs: [colmap]
    model: null
    checkpoint: null
    artifact_key_hardware_policy: required
    license:
      direct: BSD-3-Clause
      review: approved
      transitive_notes: "The approved dependency record notes that a concrete COLMAP build can include separately licensed dependencies; this executable V2 adapter does not broaden those terms."
      redistribution_notes: "WRE retains PyCOLMAP/COLMAP as an exact external solver environment; V2L12.3 adds direct canonical geometry normalization and does not approve alternate builds or bundled redistribution."
    shipping_status: experimental
    reproducibility_notes: "V2L12.3 executes the retained deterministic PyCOLMAP 4.2.0 incremental mapper and normalizes each audited native sparse model directly into canonical CameraSolution, PointMap and GeometrySolution values in one explicit unresolved-scale local frame. The retained legacy sparse estimate is regression evidence only, not the V2 output path."
    failure_signals: []
    metric_names: []
    resume_mode: unsupported
  - adapter_id: colmap.local_features
    capability:
      name: evidence.local_features
      input_kinds: [image.observation]
      output_kinds: [evidence.local_features]
    producer:
      implementation: pycolmap.extract_features
      version: 4.2.0
      revision: null
    dependency_refs: [colmap]
    model: null
    checkpoint: null
    artifact_key_hardware_policy: required
    license:
      direct: BSD-3-Clause
      review: approved
      transitive_notes: "The approved dependency record notes that a concrete COLMAP build can include separately licensed dependencies; this evidence adapter does not broaden those terms."
      redistribution_notes: "WRE retains PyCOLMAP/COLMAP as an exact external solver environment; V2L12.2 adds V2 evidence identity only and does not approve alternate builds or bundled redistribution."
    shipping_status: experimental
    reproducibility_notes: "V2L12.2 identifies the deterministic PyCOLMAP 4.2.0 CPU SIFT feature database as evidence.local_features through canonical V2 artifact-key and materialization contracts. The adapter remains experimental and this registry entry does not execute COLMAP."
    failure_signals: []
    metric_names: []
    resume_mode: unsupported
  - adapter_id: colmap.pair_matching
    capability:
      name: evidence.pair_matching
      input_kinds: [evidence.local_features]
      output_kinds: [evidence.pair_matches]
    producer:
      implementation: pycolmap.match_exhaustive
      version: 4.2.0
      revision: null
    dependency_refs: [colmap]
    model: null
    checkpoint: null
    artifact_key_hardware_policy: required
    license:
      direct: BSD-3-Clause
      review: approved
      transitive_notes: "The approved dependency record notes that a concrete COLMAP build can include separately licensed dependencies; this evidence adapter does not broaden those terms."
      redistribution_notes: "WRE retains PyCOLMAP/COLMAP as an exact external solver environment; V2L12.2 adds V2 evidence identity only and does not approve alternate builds or bundled redistribution."
    shipping_status: experimental
    reproducibility_notes: "V2L12.2 identifies the deterministic PyCOLMAP 4.2.0 CPU exhaustive raw-match database as evidence.pair_matches through canonical V2 artifact-key and materialization contracts. Geometric verification remains a distinct downstream evidence stage."
    failure_signals: []
    metric_names: []
    resume_mode: unsupported
  - adapter_id: colmap.precision_geometry
    capability:
      name: geometry.precision_sfm
      input_kinds: [image.observation]
      output_kinds: [geometry.camera_solution, geometry.point_map, geometry.solution]
    producer:
      implementation: wre.reconstruction.colmap_adapter
      version: "1"
      revision: null
    dependency_refs: [colmap]
    model: null
    checkpoint: null
    artifact_key_hardware_policy: required
    license:
      direct: BSD-3-Clause
      review: approved
      transitive_notes: "The approved dependency record notes that a concrete COLMAP build can include separately licensed dependencies; this V2 descriptor does not broaden those terms."
      redistribution_notes: "WRE retains PyCOLMAP/COLMAP as an exact external solver environment; V2L12.1 adds only a pure descriptor and does not approve alternate builds or bundled redistribution."
    shipping_status: experimental
    reproducibility_notes: "V2L12.1 declares the current PyCOLMAP 4.2.0 / COLMAP 4.2.0 precision-geometry capability through canonical V2 artifact kinds only. It does not execute COLMAP or materialize CameraSolution, PointMap, or GeometrySolution values."
    failure_signals: []
    metric_names: []
    resume_mode: unsupported
  - adapter_id: colmap.sparse_sfm
    capability:
      name: geometry.sparse_sfm
      input_kinds: [image.observation]
      output_kinds: [geometry.sparse_reconstruction_estimate]
    producer:
      implementation: wre.colmap_reconstruction_importer
      version: "2"
      revision: null
    dependency_refs: [colmap]
    model: null
    checkpoint: null
    artifact_key_hardware_policy: required
    license:
      direct: BSD-3-Clause
      review: approved
      transitive_notes: "The approved dependency record notes that a concrete COLMAP build can include separately licensed dependencies; this registry entry does not broaden those terms."
      redistribution_notes: "WRE retains PyCOLMAP/COLMAP as an exact external solver environment and does not approve alternate builds or bundled redistribution through this entry."
    shipping_status: approved
    reproducibility_notes: "WRE accepts only the retained PyCOLMAP 4.2.0 / COLMAP 4.2.0 environment and imports its sparse reconstruction through wre.colmap_reconstruction_importer version 2."
    failure_signals: []
    metric_names: []
    resume_mode: unsupported
  - adapter_id: da3.base_preview
    capability:
      name: geometry.feed_forward
      input_kinds: [media.decoded_image_pyramid]
      output_kinds: [geometry.camera_solution, geometry.depth_field, geometry.solution]
    producer:
      implementation: wre.reconstruction.da3_runtime
      version: "1"
      revision: 3d835ec1a5802d64a8b8b15f817a1ab54809bfe4
    dependency_refs: [da3_base]
    model:
      name: depth_anything_3.da3_base
      version: "1"
      revision: 3d835ec1a5802d64a8b8b15f817a1ab54809bfe4
    checkpoint:
      identifier: depth-anything/DA3-BASE/model.safetensors
      sha256: e01067dc1659613083d9145a9a2547ccdbe6ccbbf83c4fe7b3e8a4e2bdae78b5
    artifact_key_hardware_policy: required
    license:
      direct: Apache-2.0
      review: pending
      transitive_notes: "Reviewed DA3 source and DA3-BASE checkpoint report Apache-2.0. The exact optional CPU float32 Python/Torch reference stack is recorded in registry/dependencies.yaml but remains under transitive redistribution review."
      redistribution_notes: "WRE accepts only already-local exact source and checkpoint material, verifies identity before execution, and neither bundles nor implicitly downloads DA3 source, weights, or the optional learned runtime."
    shipping_status: experimental
    reproducibility_notes: "V2L13.3 fixes the DA3-BASE source revision and checkpoint digest, a CPU float32 deterministic reference config, verified canonical decoded-image inputs, exact external package identity, caller-supplied hardware-runtime identity, and deterministic normalization identity. DA3 confidence uses exp(x)+1 and is therefore not coerced into canonical probability confidence; V2L13.3 emits confidence=None. Metric scale, PointMap, default promotion and accelerated profiles remain out of scope."
    failure_signals: [checkpoint_incompatibility, dependency_unavailable, source_identity_mismatch]
    metric_names: []
    resume_mode: unsupported
  - adapter_id: exifread.raw_exif
    capability:
      name: metadata.raw_exif
      input_kinds: [image.observation]
      output_kinds: [observation.metadata]
    producer:
      implementation: wre.ingestion.exif.ExifMetadataIngestor
      version: 3.5.1
      revision: null
    dependency_refs: [exifread]
    model: null
    checkpoint: null
    artifact_key_hardware_policy: omitted
    license:
      direct: BSD-3-Clause
      review: approved
      transitive_notes: "The dependency registry records ExifRead 3.5.1 with approved BSD-3-Clause review; no additional learned model or native solver dependency is introduced here."
      redistribution_notes: "This entry records the retained pinned library adapter only and does not broaden the dependency's license terms."
    shipping_status: approved
    reproducibility_notes: "ExifMetadataIngestor uses the pinned ExifRead 3.5.1 library to extract sorted raw EXIF entries without semantic GPS, time, or camera interpretation."
    failure_signals: []
    metric_names: []
    resume_mode: unsupported
  - adapter_id: ffmpeg.audio_correlation_sync
    capability:
      name: synchronization.audio_correlation
      input_kinds: [video.observation]
      output_kinds: [sync.hypothesis]
    producer:
      implementation: wre.synchronization.audio.FFmpegAudioCorrelationSynchronizer
      version: 6.1.1-3ubuntu5
      revision: null
    dependency_refs: [ffmpeg]
    model: null
    checkpoint: null
    artifact_key_hardware_policy: omitted
    license:
      direct: GPL-2.0-or-later
      review: approved
      transitive_notes: "The retained Ubuntu Noble FFmpeg package is already reviewed as an external GPL-2.0-or-later subprocess dependency; audio correlation adds only bounded canonical PCM extraction and Python standard-library arithmetic."
      redistribution_notes: "WRE invokes the exact ffmpeg/ffprobe package as external subprocesses and does not link or bundle the binaries; alternate builds or redistribution require fresh review."
    shipping_status: approved
    reproducibility_notes: "FFmpegAudioCorrelationSynchronizer requires matching ffmpeg and ffprobe 6.1.1-3ubuntu5 identities, verifies exact source bytes, extracts bounded mono s16le PCM at an explicit policy sample rate/duration, and uses deterministic bounded normalized correlation with explicit acceptance thresholds."
    failure_signals: []
    metric_names: []
    resume_mode: unsupported
  - adapter_id: ffmpeg.decoded_image_pyramid
    capability:
      name: media.decoded_image_pyramid
      input_kinds: [image.observation, video.frame_observation]
      output_kinds: [media.decoded_image_pyramid]
    producer:
      implementation: wre.ingestion.decoded_images.FFmpegDecodedImagePyramidMaterializer
      version: 6.1.1-3ubuntu5
      revision: null
    dependency_refs: [ffmpeg]
    model: null
    checkpoint: null
    artifact_key_hardware_policy: omitted
    license:
      direct: GPL-2.0-or-later
      review: approved
      transitive_notes: "The retained Ubuntu Noble FFmpeg package is already reviewed as an external GPL-2.0-or-later subprocess dependency; this reference decode capability does not change that build composition."
      redistribution_notes: "WRE invokes the exact ffmpeg/ffprobe package as external subprocesses and does not link or bundle the binaries; alternate builds, redistribution, or accelerated backends require fresh review."
    shipping_status: approved
    reproducibility_notes: "The reference materializer requires matching ffmpeg and ffprobe 6.1.1-3ubuntu5 identities, disables WRE autorotation semantics, emits packed RGB8 raw pyramid levels and records exact source/configuration identity."
    failure_signals: []
    metric_names: []
    resume_mode: unsupported
  - adapter_id: ffmpeg.keyframe_extraction
    capability:
      name: media.keyframe_extraction
      input_kinds: [video.observation]
      output_kinds: [video.frame_observation]
    producer:
      implementation: wre.ingestion.keyframes.LocalKeyframeExtractor
      version: 6.1.1-3ubuntu5
      revision: null
    dependency_refs: [ffmpeg]
    model: null
    checkpoint: null
    artifact_key_hardware_policy: omitted
    license:
      direct: GPL-2.0-or-later
      review: approved
      transitive_notes: "The selected Ubuntu Noble FFmpeg package contains GPL-licensed files; the approved dependency record therefore treats the resulting binaries as GPL-2.0-or-later."
      redistribution_notes: "WRE invokes the exact ffmpeg/ffprobe package only as external subprocesses and does not link or bundle the binaries; redistribution or a different build requires fresh review."
    shipping_status: approved
    reproducibility_notes: "LocalKeyframeExtractor requires matching ffmpeg and ffprobe 6.1.1-3ubuntu5 identities and deterministically selects source-frame timestamps before PNG extraction."
    failure_signals: []
    metric_names: []
    resume_mode: unsupported
  - adapter_id: selavpr_plus.visual_retrieval
    capability:
      name: retrieval.learned_pair_proposals
      input_kinds: [media.decoded_image_pyramid]
      output_kinds: [retrieval.pair_candidates]
    producer:
      implementation: wre.retrieval.selavpr_plus
      version: "1"
      revision: 56bd921cbd3d53e9c5f91d0aafff147f95fb362a
    dependency_refs: [selavpr_plus]
    model:
      name: selavpr_plus.base_gem_float
      version: "1"
      revision: 56bd921cbd3d53e9c5f91d0aafff147f95fb362a
    checkpoint:
      identifier: fenglu96/SelaVPRplusplus/SelaVPRplusplus_base.pth
      sha256: b048490dbd1c27dee67fce6faaec7bec267d19a044c85877af94b8588e596a62
    artifact_key_hardware_policy: required
    license:
      direct: MIT
      review: pending
      transitive_notes: "Direct SelaVPR++ source and official model repository are MIT; the exact optional PyTorch/NumPy/Faiss/tqdm environment remains under transitive review and is not part of core WRE."
      redistribution_notes: "Experimental evaluation accepts an already-local exact source checkout and checkpoint only; WRE does not bundle or implicitly download the upstream source, checkpoint, or optional ML environment."
    shipping_status: experimental
    reproducibility_notes: "Exact source revision, reviewed Python source-file set, executable model-subtree SHA-256 manifest, checkpoint SHA-256, canonical 322x322 ImageNet preprocessing, CPU device, safe weights-only loading and exact-search proposal semantics are fixed by V2L7.5; default promotion is deferred to V2L7.6."
    failure_signals: [checkpoint_incompatibility, dependency_unavailable]
    metric_names: []
    resume_mode: unsupported

